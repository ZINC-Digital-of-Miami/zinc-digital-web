---
import PreviewLayout from '../../layouts/PreviewLayout.astro';
import MockupPage from '../../components/MockupPage.astro';
import {activeRoutes} from '../../data/mockup';
const route=activeRoutes.find(r=>r.path==='/')!;
---
<PreviewLayout pairing="a" title={route.title+' | ZINC — Pairing A'} basePath="/design-preview/a"><MockupPage route={route} pairing="a" basePath="/design-preview/a" /></PreviewLayout>
