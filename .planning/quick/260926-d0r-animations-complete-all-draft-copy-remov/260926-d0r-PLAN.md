---
phase: quick-260926-d0r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # T1 (draft copy, CI fetch, checks)
  - src/layouts/PreviewLayout.astro
  - src/components/SiteFooter.astro
  - src/components/MockupPage.astro
  - src/data/mockup.ts
  - src/pages/404.astro
  - src/components/home/HomeBands.astro   # T1: three marker lines only (see ownership table)
  - scripts/check-site.mjs
  - scripts/prepare-mockup.mjs
  - .github/workflows/ci.yml
  # T2 (animations)
  - src/styles/home.css
  - src/components/CircuitThread.astro
  - .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion.mjs
  - .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion-results.json
  # T3 (responsive review, fixes, gate, evidence)
  - src/styles/base.css
  - src/components/TeamBand.astro
  - .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/shoot.mjs
  - .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/results.json
  - .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/sheets/
autonomous: true
requirements: [quick-260926-d0r]

estimate:
  tokens: 230000
  raw_tokens: 230000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No built page renders a bracketed DRAFT marker, the nav draft tag, or the words draft / editorial review / under review, except the nine sentences of migrated article prose that use 'draft' as ordinary English (measured 09:28 CT: all nine hits in posts.preview.json are blocks[].runs[].text body copy)"
    - "Every [RECEIPT: ...], [OWNER CONFIRM], [OWNER CONFIRMS EACH LINE], [LOGO FILES PENDING ...], [LOGO PENDING] marker and redaction bar that guards an unconfirmed number or claim still renders, and the non-sending demo disclosures still render"
    - "A push to main runs check-site.mjs's HEAD~1..HEAD U+00A7 scan (not the fail-closed all-files fallback); the scan itself is not weakened"
    - "With motion allowed, each sketch animation is observed changing over time in a real browser: hero word rise, the teal strike drawing across 'the scope.', the highlighter drawing under 'the business.', the Loop path draw with the pulse moving and the active layer scaling up, logo wall flip, U.S. Oil stamps, How we work typing then locking, team shuffle, and the circuit thread dot moving while staying on screen"
    - "Under prefers-reduced-motion (and with no JS) every animated element shows its finished state with no content hidden"
    - "Every homepage band and every template renders at 1920x1080, 1780x920, 1440x900, 1280x720, 1024x768, 768x1024, 390x844, 375x667 and 667x375 with no horizontal overflow, clipping, overlap, sub-44px tap target, undersized text, under-density image or thread collision, and the executor has read the screenshots"
    - "The gate (npm run check, npm run build, node scripts/check-site.mjs, npm run verify, evidence/motion.mjs, evidence/shoot.mjs) exits 0 on the exact tree that is pushed"
  artifacts:
    - path: scripts/check-site.mjs
      provides: "New SECTION asserting no draft copy renders in dist (visible text, title, meta content, alt/aria-label/title/placeholder attributes), with an exemption limited to exact posts.preview.json body runs on article routes"
      contains: "no draft copy"
    - path: .github/workflows/ci.yml
      provides: "origin/main fetch that no longer re-shallows HEAD~1 on push-to-main runs"
      contains: "--depth=2"
    - path: .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion.mjs
      provides: "Real-browser frame sampler proving each animation passes through intermediate values, plus a reduced-motion finished-state pass; non-zero exit on any failure"
    - path: .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/shoot.mjs
      provides: "Responsive capture and metrics across 9 sizes x every template, contact sheets per size"
    - path: .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/results.json
      provides: "Zero-violation responsive metrics record"
  key_links:
    - from: "src/components/home/HomeBands.astro module script (adds html.anim, then .in on .home-hero)"
      to: "src/styles/home.css strike/highlighter rules"
      via: "transition declared on the state the element transitions INTO (the .in state), hidden state on :not(.in)"
      pattern: "html.anim .home-hero .mark-strike::after"
    - from: ".github/workflows/ci.yml 'Fetch origin/main' step"
      to: "scripts/check-site.mjs HEAD-is-origin/main branch (git rev-parse --verify HEAD~1)"
      via: "fetch depth that keeps HEAD~1 reachable"
      pattern: "rev-parse.*HEAD~1"
    - from: "src/data/posts.preview.json blocks[].runs[].text"
      to: "scripts/check-site.mjs no-draft-copy exemption"
      via: "exemption set is read from the JSON at run time, never hand-listed"
      pattern: "posts.preview.json"
---

<objective>
Finish what the owner asked for on 2026-09-26, verbatim: "be sure to get the animations in, like the blue lines on hero, etc.  do not miss anything.  remove all "draft" copy still listed.  review all areas in responsiveness before redeploying updates".

Purpose: the live site (production dpl_BtqorTQ8xtMiHNiFVV6fZ2ChbQjQ, main f795174) still shows draft labels everywhere, some sketch animations do not visibly play (the hero strike and highlighter), main CI is red, and no one has reviewed every template at every size the owner uses.

Output: draft copy gone (markers that guard unconfirmed numbers kept and listed), CI fetch fixed with the check-site gate asserting no draft copy renders, every sketch animation proven to play frame by frame, a full 9-size responsive review with fixes, a green gate, and committed evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./AGENTS.md
@./CLAUDE.md
@./.claude/CLAUDE.md
@.planning/STATE.md
@.planning/sketches/001-a-plus-b-homepage/index.html
@.planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/260926-6g7-SUMMARY.md

Working tree: /Volumes/Satechi Hub/zinc-digital-web-worktrees/quick-260926-d0r, branch gsd/quick-260926-d0r-animations-complete-all-draft-copy-remov, forked from origin/main f795174. All scratch output goes to `.scratch/260926-d0r/` inside the worktree (gitignored, on the external drive). Every time written anywhere is America/Chicago, labelled CT. Never the section-sign character (U+00A7) in any file this task touches; never the three-letter abbreviation for generative search; no cursing.

## Task ordering and file ownership

T1 and T2 run in parallel in separate worktrees branched from this branch's head; the orchestrator merges both back, then T3 runs on the merged tree.

| File | T1 | T2 | T3 |
|------|----|----|----|
| src/layouts/PreviewLayout.astro | owns | no | after merge |
| src/components/SiteFooter.astro | owns | no | after merge |
| src/components/MockupPage.astro | owns | no | after merge |
| src/data/mockup.ts | owns | no | after merge |
| src/pages/404.astro | owns | no | after merge |
| scripts/check-site.mjs | owns | no | after merge |
| scripts/prepare-mockup.mjs | owns | no | after merge |
| .github/workflows/ci.yml | owns | no | after merge |
| src/styles/home.css | no | owns | after merge |
| src/components/CircuitThread.astro | no | owns | after merge |
| evidence/motion.mjs, evidence/motion-results.json | no | owns | reruns only |
| src/styles/base.css | no | no | owns |
| evidence/shoot.mjs, results.json, sheets/ | no | no | owns |
| src/components/home/HomeBands.astro | hunks below | hunks below | after merge |

HomeBands.astro exact hunk boundary (line numbers at f795174):
- T1 may edit ONLY these three markup elements: the intro band's `<p class="lede">Supporting line under editorial review. ...</p>` (line 84), the trailing marker `<span>` inside the OUABC `.home-case-top` lede `<p>` (line 173), and the `<p class="draft-tag" style="margin-top:.5em">` under the U.S. Oil h2 (line 202).
- T2 may edit ONLY: the hero `<section class="band home-hero">` (lines 22-34), the hero `<script is:inline>` fit script (lines 35-69), the Loop band `<section id="loop">` (lines 115-156), and the final module `<script>` (lines 267-515). T2 must not edit lines 70-114 or 157-266; if a hook is needed there, add it from the script or CSS.

## Measured facts (planner, 09:15-09:28 CT)

- Rendered draft copy locations (`grep -rn -i "draft|editorial review|under review" src scripts`): PreviewLayout.astro:23 (meta description "Public design draft: ... content and legal approval pending.") and :35 (nav tag span "PUBLIC DESIGN DRAFT"); SiteFooter.astro:33 (footer-status bracketed DRAFT marker) and :42 ("&middot; Public design draft"); MockupPage.astro:16, 24, 34, 35, 42, 46, 47, 52, 54, 56, 58, 61, 62, 64, 67, 69, 71, 78, 80; HomeBands.astro:84, 173, 202; mockup.ts:8 (`export const draft`), the bracketed DRAFT prefixes inside service/case strings (lines 19-31, 52-53), :58 (comment), and `status:'draft'` at :31, :81, :82; 404.astro:5 (`status:'draft'`).
- `status` is never read: `grep -rn "\.status\b|status:" src` returns only the type and the literal assignments above.
- posts.preview.json: all 9 "draft" hits are `posts[].blocks[].runs[].text` article prose ("first drafts", "Draft products", "verify the draft", ...). They are ordinary English in migrated articles, not draft labels. Do not edit them.
- scripts/check-site.mjs:299-301 assert `[LOGO FILES PENDING`, `[OWNER CONFIRMS EACH LINE]` and `[RECEIPT:` render; those markers stay, so those assertions stay. scripts/verify-mockup.mjs has no draft assertion. scripts/prepare-mockup.mjs:39 asserts the article draft notice exists (`'Draft migration preview'`); it is not in the gate but must be inverted so a re-run does not fail.
- ci.yml "Fetch origin/main" runs `git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main` after a `fetch-depth: 2` checkout. On a push to main HEAD equals origin/main, the depth=1 fetch marks HEAD shallow, HEAD~1 disappears, and check-site.mjs:130-137 falls closed to scanning every tracked file, which fails on section signs in 20 planning docs.
- Hero strike/highlighter: base.css:217-240 defines `.mark-strike::after` / `.mark-hit::before` with no transition and no transform-origin. home.css:116-126 declares the `transition` ONLY inside `html.anim .home-hero:not(.in) ...`. CSS transitions take their timing from the after-change style; once `.in` is added the `:not(.in)` rule no longer matches, so the bars jump from scaleX(0) to full with no draw, from the center, not the left. The sketch (index.html:68-73) declares the transition and `transform-origin:left` on the base rule and only the hidden transform on `:not(.in)`. The 6g7 evidence script only checked the END state (not scaleX(0)), which is why it passed. Treat this as the leading hypothesis: T2 confirms it with frame samples before fixing.
- Hero `.in` is added in a single `requestAnimationFrame` right after `html.anim` (HomeBands.astro:327-329 region); the sketch does the same, but the hidden state must be committed to style before `.in` lands.
- Loop below 750px viewport height: home.css `@media (max-height: 750px)` drops the pin (`height:auto`, `position:static`), and `onScroll` still computes `p = -rect.top / (offsetHeight - innerHeight)`, so the draw only advances while the band's top is above the viewport, or jumps to 1 when that span is 0 or less. 1280x720 and 667x375 are owner sizes.
- macOS Reduce Motion is not enabled on this Mac (`defaults read com.apple.universalaccess reduceMotion` reports the key does not exist), so an OS setting on this machine does not explain the missing hero lines.
- Available locally, no install needed: puppeteer-core and selenium-webdriver in node_modules, Chrome at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, sharp in node_modules, Python PIL 10.4.0. Longest article title: ai-search-results-and-generative-search-optimization (82 chars); next: shopify-google-merchant-center-checklist (76); shortest: when-web-design-trends-actually-matter (38).
- Estimate calibration: factor 1, 0 samples, confidence low.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Remove every rendered draft label, fix the CI origin/main fetch, and make check-site assert no draft copy renders</name>
  <files>src/layouts/PreviewLayout.astro, src/components/SiteFooter.astro, src/components/MockupPage.astro, src/data/mockup.ts, src/pages/404.astro, src/components/home/HomeBands.astro (three marker lines only), scripts/check-site.mjs, scripts/prepare-mockup.mjs, .github/workflows/ci.yml</files>
  <read_first>AGENTS.md; every file in files above in full except posts.preview.json (read only its structure: posts[].blocks[].runs[].text); scripts/check-site.mjs lines 80-180 (U+00A7 scan, HEAD~1 branch) and 270-310 (Task 3 home markers).</read_first>
  <action>
Tracer path: source copy, then build, then check-site gate, then CI, proven end to end on one route first (build, then confirm dist/index.html has no nav draft tag and check-site's new section passes) before sweeping the rest.

Owner rule (2026-09-26): remove all "draft" copy. Rules for every rendered string: (a) an element whose only content is draft status goes entirely, with no replacement copy; (b) where a bracketed DRAFT marker prefixes or suffixes real copy, keep the copy and drop the marker; (c) where the legal pages use "draft" as the noun for this site, swap it for "preview", the noun the same legal copy already uses in its own headings ("About this preview", "Purpose of this preview"), with minimal grammar edits and no new claims; (d) keep every [RECEIPT: ...], [OWNER CONFIRM], [OWNER CONFIRMS EACH LINE], [LOGO FILES PENDING ...], [LOGO PENDING] marker and "Receipt pending" redaction bar, because removing them would present an unconfirmed number or claim as fact (AGENTS.md: no invented numbers); (e) keep every non-sending demo disclosure ("Demo only", "does not send", noindex) because it is true and protects visitors; (f) CSS class names such as draft-tag / draft-notice are not rendered copy, so leave them; T3 removes orphan CSS. Do not write code comments that quote the removed markers.

Edits:
1. PreviewLayout.astro:35 delete the nav tag span. :23 meta description becomes the page title followed by " | ZINC Digital" (derived from existing data, no invented copy).
2. SiteFooter.astro:33 delete the footer-status element (and its wrapper if it becomes empty); :42 drop "&middot; Public design draft", keep the copyright. Keep "YouTube [OWNER CONFIRM]".
3. MockupPage.astro: :16 delete the eyebrow marker span. :24 and :56 delete the draft-notice paragraphs (article and blog index; their only content is draft status). :34 eyebrow becomes "Service specification"; :54 becomes "How an engagement runs"; :80 becomes "404". :35, :42, :46, :47, :52 drop the bracketed prefix, keep the sentence. :58 keep "Legacy search terminology expanded for this preview." and delete "Full editorial review remains pending.". :61 delete the bracketed OWNER/LEGAL REVIEW paragraph and keep the muted paragraph after it. Legal body lines :62, :64, :67, :69, :71, :78 apply rule (c); on :71 "Content marked as draft, receipt pending or owner confirmation" becomes "Content marked receipt pending or owner confirmation". Leave the non-draft words ("review before launch", "awaiting owner review", "Build and review", "Scope to confirm", "Results / awaiting source confirmation") and list them in the SUMMARY.
4. HomeBands.astro (T1 hunks only, see ownership table): delete the intro band's "Supporting line under editorial review." paragraph entirely (the actions div stays); drop the trailing marker span from the OUABC lede, keep its sentence; delete the "headline" marker paragraph under the U.S. Oil h2.
5. mockup.ts: delete `export const draft` (confirm by grep that nothing imports it); drop every bracketed DRAFT prefix inside service and case strings, keeping the copy; reword the :58 comment without the word; remove the unused `status` field from the Service and Route types, from `services` and `route()`, and from 404.astro:5 (measured: nothing reads `status`).
6. prepare-mockup.mjs:39 invert the assertion so it requires the article page NOT to contain the removed notice.
7. check-site.mjs: append a new SECTION "no draft copy renders" (keep lines 299-301). For every dist/**/*.html: drop script, style and comment blocks; collect text nodes plus the title and the values of content, alt, aria-label, title and placeholder attributes; decode entities (named and numeric); collapse whitespace. Build the exemption set at run time from src/data/posts.preview.json: every blocks[].runs[].text string that matches the pattern below, whitespace-collapsed. On article routes only (dist/blog/SLUG/index.html for each post slug) remove exact occurrences of those strings before scanning. Fail, naming file and 60 chars of context, on the case-insensitive pattern for the word draft, "editorial review", "under review", or a bracketed DRAFT marker. Print the number of files scanned and exempt runs matched.
8. ci.yml: change the fetch to `--depth=2` so HEAD~1 stays reachable when HEAD is main, and rewrite the comment block above it (it currently claims depth=1 is sufficient) to explain why depth 2 is required. Do not change the check-site fallback, the fetch-depth 2 checkout, or anything else in the workflow.

Proof the new gate has teeth: temporarily put a bracketed DRAFT marker into SiteFooter.astro, build, run check-site (must exit non-zero naming the footer on every page), revert, rebuild, rerun (must exit 0). Record both exit codes in the SUMMARY.

Proof the CI fix works: in `.scratch/260926-d0r/ci-repro`, `git clone --depth=2 --branch main file:///Volumes/Satechi%20Hub/zinc-digital-web` (or the path-quoted equivalent), run the OLD fetch command and show `git rev-parse --verify HEAD~1` fails; reclone, run the NEW fetch command and show it succeeds. Record both results with CT times. The live proof (the post-merge push-to-main run logging "scanning files changed by HEAD against HEAD~1") is measured by the orchestrator after merge.

Finally, from dist, list every remaining bracketed marker with its route (`grep -rhoE '\[[A-Z][A-Z /:-]+[^]]*\]' dist | sort | uniq -c`) and put that list in the SUMMARY for the owner.
  </action>
  <verify>
    <automated>mkdir -p .scratch/260926-d0r; npm run check > .scratch/260926-d0r/t1-check.log 2>&1; echo CHECK_EXIT=$?; npm run build > .scratch/260926-d0r/t1-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/260926-d0r/t1-site.log 2>&1; echo SITE_EXIT=$?; grep -rnE '\[DRAFT' dist src; echo MARKER_GREP_EXIT=$? ; grep -rniE 'draft|editorial review|under review' src/components src/layouts src/pages src/data/mockup.ts | sed -E 's/class="draft-(tag|notice)"//g' | grep -iE 'draft|editorial review|under review'; echo SRC_WORD_GREP_EXIT=$? ; LC_ALL=C grep -l $'\xc2\xa7' src/layouts/PreviewLayout.astro src/components/SiteFooter.astro src/components/MockupPage.astro src/data/mockup.ts src/pages/404.astro src/components/home/HomeBands.astro scripts/check-site.mjs scripts/prepare-mockup.mjs .github/workflows/ci.yml; echo SECTION_SIGN_GREP_EXIT=$?</automated>
  </verify>
  <done>CHECK_EXIT=0, BUILD_EXIT=0, SITE_EXIT=0 with the new section reporting files scanned and exempt runs matched; MARKER_GREP_EXIT=1, SRC_WORD_GREP_EXIT=1, SECTION_SIGN_GREP_EXIT=1; the injected-marker negative test exited non-zero and the reverted rerun exited 0; the CI reproduction shows HEAD~1 missing with the old fetch and present with the new one; the SUMMARY lists every remaining bracketed marker by route and the retained non-draft status phrases; committed on the T1 worktree branch with the Co-Authored-By trailer.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every sketch animation plays frame by frame in a real browser, and fix the ones that do not</name>
  <files>src/styles/home.css, src/components/home/HomeBands.astro (hero section, hero fit script, Loop band, module script only), src/components/CircuitThread.astro, .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion.mjs, .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion-results.json</files>
  <read_first>.planning/sketches/001-a-plus-b-homepage/index.html in full (CSS lines 40-190 and script lines 400-495 are the animation authority); src/styles/home.css in full; src/styles/base.css lines 205-245 and 429-434; src/components/home/HomeBands.astro in full; src/components/CircuitThread.astro; .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs lines 1-120 and 320-470 (server, puppeteer-core launch, motion-scroll test) as the pattern to copy.</read_first>
  <action>
Owner, 2026-09-26: "be sure to get the animations in, like the blue lines on hero, etc.  do not miss anything."

Step 1, inventory. From the sketch's CSS and script, list every animated element and the property that proves it plays. At minimum: A1 kinetic word rise (`.kin .w > i` transform, hero on load, OUABC and U.S. Oil h2 on scroll-in, footer "Start an inquiry." on the homepage); A2 hero strike (`.home-hero .mark-strike::after` transform scaleX, drawing from the left); A3 hero highlighter (`.mark-hit::before` scaleX, then height shrinking from full to the bar); A4 the Loop (`#loop-path` stroke-dashoffset, `#pulse`/`#glow` cx,cy from getPointAtLength, `.home-layer.on` index 0 to 1 to 2 with the active title scaled up, `.node.on`, `#loop-pct` text); A5 logo wall flip (`.home-logo-cell.flip` appearing on successive cells); A6 U.S. Oil stamps (`.home-stamp.in` count and opacity rising over time); A7 How we work (`.home-commit-typed` text length growing, then `li.locked`, then `.home-lock` opacity); A8 team shuffle (`[data-team]` child order or `.shuffling` observed); A9 circuit thread dot (`#thread-dot` top changing with scroll and its rect inside the viewport at top, middle and bottom of the page). Add anything else the sketch animates that is missing from this list.

Step 2, sampler (RED first). Write evidence/motion.mjs following the 6g7 shoot.mjs pattern (static server over dist on port 4323 so it never collides with shoot.mjs on 4322 or verify-mockup on 4329; puppeteer-core with the local Chrome; `emulateMediaFeatures` prefers-reduced-motion no-preference). For each animation, sample the proving property at fixed times after the trigger (hero: 0, 300, 700, 1000, 1200, 1500, 1900, 2600 ms after load; bands: scroll the band into view, then 0, 200, 450, 800, 1500, 3000, 6000 ms; Loop: at least 8 scroll positions through the band). Assert each passes through an intermediate value (for example 0 < scaleX < 1 on at least one sample for A2 and A3, a text length strictly between 0 and full for A7, at least two distinct stroke-dashoffset values below the full length for A4, and the dashoffset reaching 0 at the end) and ends in the finished state. Run the Loop and hero samples at 1440x900, 1280x720 and 390x844. Run the thread-dot sample on the homepage and on /blog/ at 1440x900 and 390x844. Save element-clipped PNG frames to `.scratch/260926-d0r/motion/` and copy one early, one mid and one final frame of A2/A3 and A4 into the evidence folder. Add a reduced-motion pass (`prefers-reduced-motion: reduce`) at 1440x900: html has no `anim` class, strike and highlighter show full bars immediately, every word, stamp, typed line (full text) and team card is visible, the Loop dashoffset is 0. Add a JavaScript-disabled pass with the same finished-state assertions. Write every sample value to evidence/motion-results.json and exit non-zero on any failure. Build from the unmodified tree and run it first; record which animations fail (expected at least A2 and A3, and A4 at 1280x720) in the SUMMARY as the RED result.

Step 3, fix what failed, matching the sketch.
- A2/A3: in home.css, declare the transition and `transform-origin: left` on the unconditional `html.anim .home-hero .mark-strike::after` / `.mark-hit::before` rules (the state the bars transition INTO) and keep only the hidden transform (and the highlighter's full height) on the `:not(.in)` rules, as the sketch does at index.html:68-73; match the sketch's bar geometry (strike at 43% of the line box, highlighter bar height) scoped to `.home-hero` so base.css stays untouched. In the module script, make sure the hidden state is committed to style before `.in` is added to the hero (for example two nested requestAnimationFrame calls, or adding `.in` after the post-wrap `__fitHero` layout read), and confirm the word-wrap spans do not clip the pseudo-elements.
- A4: when the Loop is not pinned (viewport 750px tall or less, or container 820px wide or less if the pin is dropped there), compute progress from the band entering the viewport to its bottom reaching the viewport bottom, so the draw, pulse and layer changes happen while the band is on screen. Keep the pinned behaviour unchanged where it already passes, and keep the TOC click handler's pinned check intact.
- Fix any other animation the sampler shows as not playing, following the sketch, inside T2's files and hunks only.
- Keep motion as progressive enhancement: content complete in the server HTML, `html.anim` added only by the HomeBands module script, reduced motion and no-JS show the finished state. Keep the page JS within check-site's 15360-byte gzip budget.

Rerun motion.mjs until it exits 0; read the saved A2/A3 and A4 frames with the Read tool and describe what each shows in the SUMMARY.
  </action>
  <verify>
    <automated>mkdir -p .scratch/260926-d0r; npm run build > .scratch/260926-d0r/t2-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/260926-d0r/t2-site.log 2>&1; echo SITE_EXIT=$?; node .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion.mjs > .scratch/260926-d0r/t2-motion.log 2>&1; echo MOTION_EXIT=$?; LC_ALL=C grep -l $'\xc2\xa7' src/styles/home.css src/components/home/HomeBands.astro src/components/CircuitThread.astro .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/motion.mjs; echo SECTION_SIGN_GREP_EXIT=$?</automated>
  </verify>
  <done>BUILD_EXIT=0, SITE_EXIT=0 (JS budget intact), MOTION_EXIT=0, SECTION_SIGN_GREP_EXIT=1; motion-results.json shows an intermediate sample and a finished end state for every inventoried animation at every sampled size, plus passing reduced-motion and no-JS finished-state passes; the RED run's failures and the fix for each are recorded in the SUMMARY with the frames read; no line outside T2's HomeBands hunks changed (`git diff` shows hunks only within lines 22-69, 115-156 and the final module script); committed on the T2 worktree branch with the Co-Authored-By trailer.</done>
</task>

<task type="auto">
  <name>Task 3: Review every band and template at the owner's nine sizes, fix what fails, run the full gate, and push the PR</name>
  <files>src/styles/base.css, src/styles/home.css, src/components/MockupPage.astro, src/components/home/HomeBands.astro, src/components/SiteFooter.astro, src/components/TeamBand.astro, src/components/CircuitThread.astro, src/layouts/PreviewLayout.astro, .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/shoot.mjs, .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/results.json, .planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence/sheets/</files>
  <precondition>T1 and T2 are both merged into gsd/quick-260926-d0r-animations-complete-all-draft-copy-remov (git log shows both task commits) before this task starts.</precondition>
  <read_first>The T1 and T2 commits (`git log -p` for the two task commits); .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs in full; src/styles/base.css and src/styles/home.css in full; src/components/MockupPage.astro; the list of built routes (`ls -R dist | grep index.html`).</read_first>
  <action>
Owner, 2026-09-26: "review all areas in responsiveness before redeploying updates", and sections must be broken down properly for responsiveness to be right. Standing owner corrections to preserve: the hero is the headline alone filling one screen below the masthead; the intro band follows; the thread runs the whole page with the dot always on screen; the Loop sits side by side on desktop; blog titles are capped to about a third of the screen.

1. Copy the 6g7 shoot.mjs into this task's evidence folder and extend it. Routes: /, /services/, /services/shopify/, /work/, /work/once-upon-a-book-club/, /work/us-oil-solutions/, /about/, /contact/, /thanks/, /blog/, /blog/ai-search-results-and-generative-search-optimization/, /blog/shopify-google-merchant-center-checklist/, /blog/when-web-design-trends-actually-matter/, the privacy and terms routes as built, and /404.html (confirm each path exists in dist). Sizes: 1920x1080, 1780x920, 1440x900, 1280x720, 1024x768 at deviceScaleFactor 1; 768x1024 at 2; 390x844 at 3; 375x667 and 667x375 at 2. Motion allowed by default; add a reduced-motion pass for / at 1440x900, 390x844 and 667x375.
2. Per route and size, record and fail on: horizontal overflow (documentElement scrollWidth greater than clientWidth); any visible element extending past the viewport's left or right edge (thread and dot excluded); clipped text (an element with overflow hidden or clip whose scrollWidth or scrollHeight exceeds its client box by more than 1px while holding text, except the kinetic `.w` masks at rest); overlap between sibling blocks inside each band (bounding-rect intersection of the direct children of each `.band__inner` and of grid/flex rows); tap targets under 44x44 on a, button, summary, input, select and label controls that are not inline in running text; text with computed font-size under 12px, and paragraph or list copy under 15px on phone sizes; content images whose naturalWidth is below 0.95 x rendered width x deviceScaleFactor; the thread dot outside the viewport at the top, middle and bottom scroll positions, or the thread's x-range intersecting any text element's rect; the hero headline bottom below the first screen, or anything other than the headline visible inside the hero band; the blog and article h1 taller than one third of the viewport height (plus 24px tolerance); the Loop SVG and layer list not side by side at widths 1024 and up.
3. Screenshots: first-screen and full-page PNG per route and size, plus one element screenshot per homepage band per size, to `.scratch/260926-d0r/responsive/`. Build one contact sheet per size for the homepage bands and one per size for the templates with sharp (already in node_modules), labelled route and size, into evidence/sheets/ (18 sheets). Read every sheet with the Read tool, open the full-size PNG for any cell that looks wrong or that a metric flagged, and note what you saw per sheet in the SUMMARY.
4. Fix every failure in the listed files, rebuilding and rerunning until results.json reports zero violations. Also remove CSS left orphaned by T1 (the `.nav-draft` rules in base.css and any selector no built page uses any more; confirm by grepping dist before deleting).
5. Section breakdown: while reviewing, note every band whose sections combine unrelated content (for example the intro band's meta row, actions and contents list; the article band's source sidebar and body; the case page's screenshots with the testimonial notice; the legal aside with the policy body; the contact aside with the form). Split or restructure the ones that cause a failure at these sizes; list the rest with file:line and the proposed split under "Section breakdown follow-ups" in the SUMMARY.
6. Gate on the exact tree to be pushed, each command's own exit code, in this order (npm run verify rebuilds dist, so the evidence scripts run after it on the same dist): npm run check; npm run build; node scripts/check-site.mjs; npm run verify; node evidence/motion.mjs; node evidence/shoot.mjs. Commit the fixes, shoot.mjs, results.json, motion-results.json and the sheets (raw PNGs stay in .scratch), with the Co-Authored-By trailer.
7. Push the branch, confirm with `git ls-remote origin` that the remote head equals the local HEAD, open a PR to main whose body lists what changed, the gate exit codes, the remaining non-draft markers, and the section follow-ups, with all times in CT and the Claude Code attribution line; then quote `gh pr checks`. Do not merge, do not run the Codex review and do not touch production: the orchestrator runs the Codex CLI review, merges, and measures production.
  </action>
  <verify>
    <automated>mkdir -p .scratch/260926-d0r; E=.planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/evidence; npm run check > .scratch/260926-d0r/gate-check.log 2>&1; echo CHECK_EXIT=$?; npm run build > .scratch/260926-d0r/gate-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/260926-d0r/gate-site.log 2>&1; echo SITE_EXIT=$?; npm run verify > .scratch/260926-d0r/gate-verify.log 2>&1; echo VERIFY_EXIT=$?; node $E/motion.mjs > .scratch/260926-d0r/gate-motion.log 2>&1; echo MOTION_EXIT=$?; node $E/shoot.mjs > .scratch/260926-d0r/gate-shoot.log 2>&1; echo SHOOT_EXIT=$?; ls $E/sheets | wc -l</automated>
  </verify>
  <done>CHECK_EXIT, BUILD_EXIT, SITE_EXIT, VERIFY_EXIT, MOTION_EXIT and SHOOT_EXIT are all 0 on the pushed tree; results.json covers 16 routes x 9 sizes plus the reduced-motion passes with zero violations; 18 contact sheets exist and each was read; every fix and every section follow-up is listed with file:line in the SUMMARY; the branch is pushed (ls-remote SHA equals local HEAD) and the PR is open with its checks quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor to static site | Visitors read copy and may type into the non-sending contact demo |
| CI runner to origin | The build job fetches origin/main to decide what check-site scans |
| source data to rendered copy | posts.preview.json and mockup.ts strings become public text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d0r-01 | Tampering | ci.yml fetch + check-site.mjs U+00A7 scan | medium | mitigate | Only the fetch depth changes; the fail-closed fallback stays; T1 reproduces old-fails / new-passes locally and the orchestrator reads the post-merge main run log |
| T-d0r-02 | Repudiation (misrepresentation) | Removing markers from copy | high | mitigate | Rule (d) keeps every RECEIPT, OWNER CONFIRM and LOGO marker and redaction bar; T1 lists every remaining marker by route for the owner |
| T-d0r-03 | Information disclosure | Contact demo disclosures | medium | mitigate | Rule (e) keeps "Demo only / does not send" copy and noindex; check-site's existing demo assertions stay |
| T-d0r-04 | Tampering | check-site no-draft exemption | low | mitigate | Exemption is built from exact posts.preview.json body runs at run time and applies only on article routes; negative test proves the gate fails on an injected marker |
| T-d0r-05 | Denial of service | Evidence scripts' local servers | low | accept | Loopback-only throwaway servers on fixed distinct ports (4322, 4323), never deployed |
| T-d0r-SC | Tampering | npm installs | low | accept | No package is installed; puppeteer-core, sharp and selenium-webdriver are already in node_modules |
</threat_model>

<verification>
- T1 done criteria met in its worktree; T2 done criteria met in its worktree; both merged with no conflict inside HomeBands.astro (hunks are disjoint by the ownership table).
- T3 gate exit codes all 0 on the pushed tree; results.json and motion-results.json show zero violations.
- Orchestrator (after this plan): Codex CLI review once (`/Applications/ChatGPT.app/Contents/Resources/codex-cli/bin/codex review --base origin/main -c model=gpt-6-sol -c model_reasoning_effort=high` after `git fetch origin`), CONFIRMED findings fixed, required check `build` and Vercel green, squash-merge, then the push-to-main CI run's check-site log shows "scanning files changed by HEAD against HEAD~1" and passes, and production serves the merge SHA with no draft copy (fetch the production homepage, an article and privacy, and grep).
</verification>

<success_criteria>
- Zero rendered draft labels on all built routes, the guarding markers intact and listed for the owner.
- Main CI green again on push, with the U+00A7 scan scoped to HEAD~1..HEAD.
- Every sketch animation measured passing through intermediate frames with motion allowed, and finished under reduced motion and without JS; the hero strike and highlighter draw from the left.
- Every band and template clean at all nine sizes, screenshots read, section follow-ups listed.
- PR open with a green gate and quoted checks.
</success_criteria>

<output>
Create `.planning/quick/260926-d0r-animations-complete-all-draft-copy-remov/260926-d0r-SUMMARY.md` when done, with all times in CT: the RED and GREEN motion results, every fix with file:line, the remaining non-draft markers by route, the retained non-draft status phrases, the section breakdown follow-ups, the gate exit codes, and the pushed SHA and PR number.
</output>
