---
phase: 01-design-system-font-pick
plan: "05"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - astro.config.mjs
  - src/layouts/PreviewLayout.astro
  - src/styles/pairings.css
  - src/data/mockup.ts
  - src/pages/design-preview/a.astro
  - src/pages/design-preview/[...path].astro
  - scripts/verify-mockup.mjs
  - scripts/gate-preview.mjs
files_deleted:
  - src/pages/design-preview/a.astro
  - src/pages/design-preview/[...path].astro
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All40 canonical sitemap pages, genuine404 and every mockup interaction remain after the selected pairing becomes the sole site font set."
    - "No comparison route or inactive pairing font is reachable/referenced in the final build; every page requests at most3 same-origin fonts and preloads exactly its display face."
    - "The owner's explicit recorded choice, rather than an agent default, controls the retained family and weights."
    - "All final template screenshots at1440/375, route/link/style/image/axe/performance/cold-CLS checks pass again for the retained complete site."
    - "The final full-site mockup remains public noindex at zinc-digital-web.vercel.app; custom domain, production submissions and final content approval remain later-phase responsibilities."
  artifacts:
    - path: astro.config.mjs
      provides: "Only the owner's selected3 font registrations"
    - path: src/layouts/PreviewLayout.astro
      provides: "Single-pairing full-site layout, retained draft identification and noindex"
    - path: src/pages/[...path].astro
      provides: "Preserved complete canonical sitemap via shared manifest"
    - path: .planning/phases/01-design-system-font-pick/01-05-SUMMARY.md
      provides: "Decision provenance, final route counts, gates, reviewed deployment and exact remaining draft obligations"
  key_links:
    - from: 01-04 owner decision
      to: Astro fonts/layout/pairings
      via: "Chosen family/weight tuple preserved verbatim"
    - from: retained canonical routes
      to: unchanged content manifest
      via: "Only pairing namespace changes; all40 content destinations survive"
---

<objective>
**As a** site owner, **I want to** keep my selected font across the entire reviewed site **so that** subsequent phases can finish production work on the approved full-site design.

Collapse typography and comparison navigation only after the actual owner decision. Retain every rendered page, article body, draft label, service preselection and demo state.
</objective>

<execution_context>
@/Users/zincdigital/.codex/gsd-core/workflows/execute-plan.md
@/Users/zincdigital/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-font-pick/01-CONTEXT.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-VALIDATION.md
@.planning/phases/01-design-system-font-pick/01-04-SUMMARY.md
@src/data/mockup.ts
@src/layouts/PreviewLayout.astro
@astro.config.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task1: Apply the owner's actual selection to the complete canonical site</name>
  <files>astro.config.mjs, src/layouts/PreviewLayout.astro, src/styles/pairings.css, src/data/mockup.ts</files>
  <read_first>01-04-SUMMARY.md exact owner choice and reviewed deployment; source font config; full-site route/content inventory; prior selected-pairing screenshots and computed font metrics.</read_first>
  <precondition>01-04-SUMMARY.md contains an explicit owner A/B/C selection tied to the reviewed complete-site deployment, and its full readiness gate passed.</precondition>
  <action>
    Assert the owner decision before editing. Switch canonical pages' default pairing to that recorded value; keep the selected exact families and actual weights. Run the real home→service→contact-demo→thanks path immediately to prove the selection travels through the existing full site. Remove losing registrations from Astro config, losing mappings from pairings.css and inactive Font branches from PreviewLayout only after preserving the selected declarations. The final layout still contains three active roles, one display preload, optimized fallbacks, noindex metadata, site navigation and visible draft identification. During this task, comparison sources may temporarily remain but are not a reason to keep unwanted font downloads: remove their build enumeration if they need inactive fonts, then Task2 removes those explicit files. No canonical route may disappear.

    Keep all40 manifest records, all18 source articles and all11 services unchanged. Fix only the now-obsolete comparison navigation; canonical links continue to use the same path helper with an empty prefix. This is configuration/glue work on already tested rendering, not a new content/design implementation. Preserve the exact hero clamp, top placement, image variants, marks on both grounds, tokens, all-nine home bands and demo safety.
  </action>
  <acceptance_criteria>The canonical site uses precisely the owner-selected font tuple. All40 destinations and the real tracer remain. Retained code contains only3 font registrations and one active pairing mapping.</acceptance_criteria>
  <verify><automated>npm run build && npm run check && node scripts/verify-mockup.mjs --mode quick --selected --tracer</automated><fails_when>The command exits nonzero on missing/ambiguous owner-selection evidence, build/type error, changed canonical inventory, wrong selected font tuple or failed tracer/demo path.</fails_when><failure_direction>If the choice is absent/ambiguous or the gate was not met, stop before editing and report that exact missing decision. If a page disappears, restore its canonical record/template before continuing.</failure_direction></verify>
  <done>The selected pairing works end to end across the complete canonical site with every content destination preserved.</done>
</task>

<task type="auto">
  <name>Task2: Retire comparison routes, remeasure every retained template and publish the selected full site</name>
  <files>src/pages/design-preview/a.astro, src/pages/design-preview/[...path].astro, scripts/verify-mockup.mjs, scripts/gate-preview.mjs</files>
  <read_first>Task1 selected font config and complete manifest; verified01-04 evidence; scripts/gate-preview.mjs target/protection assertions; UI consideration/edge ledger.</read_first>
  <action>
    Remove only the two explicit comparison route files and their generated route outputs using a clean build. Do not delete the shared MockupPage, MockupBands, fixtures, canonical catch-all,404, brand assets or any content page. Losing font files are generated build output; rebuild cleanly and prove they are absent rather than deleting shared/source assets indiscriminately. Any href still pointing at a removed comparison namespace is a defect to fix in the shared navigation.

    Finalize --selected mode in the existing scripts: derive the selected value from the recorded owner decision/current config and assert they agree; expected content inventory is40 canonical URLs plus genuine404. No comparison namespace is generated, referenced or returns a successful page. Preserve gate assertions rather than removing them to accommodate failures. Count actual per-page WOFF2 network requests<=3, active config entries3 and exactly1 preload. The selected page's semantic content must match its previously reviewed counterpart, with only comparison controls/labels removed.

    Run the complete local crawl/state/style/image/axe/performance/font/screenshot matrix using --selected. Recapture EVERY template at1440/375, plus the longest service, both cases,18 article route checks, all contact/filter states and static/reduced-motion views. Require fresh independent visual review of all final screenshots and actual changed functions, since removing the comparison strip can move top content. Reapply the original successful-page thresholds, including cold CLS0 and the explicit noindex-only Lighthouse SEO adjustment. The shared canonical404 retains its separate error-page contract: genuine404,1440/375 images, the same applicable visual/accessibility/performance/link/noindex checks and raw SEO reporting without a successful-page100 gate.

    Deploy with the exact already authorized command vercel deploy --prod --scope zincdigitalofmiamis-projects only after current target identity and local acceptance pass. Check the public zinc-digital-web.vercel.app alias and raw preview protection separately; ensure only that domain is attached and retain noindex header/meta on all paths. Repeat the full matrix against the deployed artifact and record deployment identity/source SHA. Phase completion is not final site launch or proof of landed main: hand the verified source/deployment evidence to the orchestrator for its separate repository landing workflow. Never solve the separate Pages/SPF questions by changing hosting, DNS or mail here.

    Preserve required screenshots/reports, safe source fixtures and admitted assets. Delete only task-created temporary browser profiles/extraction copies after the accepted evidence is saved, respecting exact cleanup authorization; otherwise report their locations/sizes for owner disposition. Record kept/deleted paths and sizes. The summary lists the full sitemap counts and remaining final copy/receipts/photos/legal, mail/anti-abuse, polished migration, SEO/redirects/analytics and launch obligations so later phases cannot mistake a demo for finished production.
  </action>
  <acceptance_criteria>The selected complete site has40 canonical destinations and real404, zero broken internal links, no comparison namespace, no inactive fonts, complete passing final gates and independently reviewed screenshots. Public noindex deployment identity is recorded without a false main/production-launch claim.</acceptance_criteria>
  <verify><automated>npm run build && npm run check && node scripts/verify-mockup.mjs --mode quick --selected && node scripts/gate-preview.mjs --mode full --selected --base https://zinc-digital-web.vercel.app</automated><fails_when>The command exits nonzero when comparison routes/fonts remain, a canonical page or body is lost, a link/state/screenshot is missing, any final threshold fails or target/noindex/protection differs from the authorized contract.</fails_when><human-check>Independent reviewer accepts the final full-site images and unchanged route/content coverage.</human-check><failure_direction>Do not accept a reduced route set, changed semantic content, missing screenshots or relaxed performance/protection thresholds. Fix the exact regression and rerun current checks.</failure_direction></verify>
  <done>The owner's selected font is the only pairing on the complete verified mockup, with all pages preserved and exact final evidence handed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Owner choice → configuration deletion | Only losing font/comparison files can be removed |
| Build → public review alias | Retained draft site must preserve noindex and non-sending behavior |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05-A | Tampering | Collapse cleanup | high | mitigate | Explicit owner precondition, named comparison files only, canonical manifest/content parity assertions |
| T-01-05-B | Spoofing | Completion/thanks copy | high | mitigate | Retain demo/draft markers and distinguish mockup from production/content approval |
| T-01-05-C | Information Disclosure | Final review deployment | high | mitigate | Fresh domain/protection/noindex and no-network submission tests |
</threat_model>

<verification>
All final checks apply to the retained full sitemap, not only/. Run the local full matrix before deploy and the complete deployed matrix afterward. Record exact fonts, route/link/screenshot counts, noindex adjustment, reviewer and deployment evidence.
</verification>

## Artifacts Produced
Single-pairing full-site mockup; updated strict selected-mode gates; final complete screenshots/reports in .scratch/phase01-mockup/; 01-05-SUMMARY.md with decision, exact retained inventory, validation, cleanup and outstanding production obligations.

<success_criteria>Owner's selected font is retained across every approved page and interaction; all alternatives are removed without losing any part of the full site.</success_criteria>

<output>Create .planning/phases/01-design-system-font-pick/01-05-SUMMARY.md and hand verified artifacts to the orchestrator. Do not present a branch deployment as merged-main delivery.</output>
