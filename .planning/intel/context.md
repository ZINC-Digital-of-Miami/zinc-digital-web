> **CORRECTION (2026-09-25 8:09 PM CT), owner, verbatim:** "I never said to remove B." The "Pairing A only, Pairings B and C are dropped" statement below was the orchestrator's misstatement, not an owner decision. It is withdrawn. Font selection stays open; Pairing B is kept. The owner asked for a mockup of A and B together. The BLOCKER built on that premise is void.

> Generated by gsd-doc-synthesizer during ingest (2026-09-25 CT). Topic-keyed notes from DOC-classified sources, plus the owner corrections supplied directly to this ingestion run. Heading names are used in place of the section-sign character throughout, per owner instruction (see "Owner corrections," item 3, below).

## Owner corrections postdating every ingested doc (2026-09-25 CT)

- source: supplied directly to this ingestion run by the orchestrating agent (not a classified file) — treated as LOCKED, overriding any ingested-doc statement it contradicts
- content:
  1. **Design direction (approved 12:08 PM CT, verbatim):** "A's loop as the signature, set in B's type discipline. Black and white alternate section by section. The editorial typography keeps it legible and senior for the C-suite, and the loop with its magenta pulse is the unexpected, ownable moment. C gets cut." A = The Loop direction; B = The Annual Report direction (editorial condensed type, magenta highlighter/redaction bar, kinetic type). No contradiction found against ingested content — the design spec's Visual system section already states this same direction ("the Loop as the signature, set in editorial ('annual report') type discipline"). See "Auto-resolved" bucket note only for traceability; no action required.
  2. **Fonts (chosen ~5:30 PM CT):** [WITHDRAWN — see correction at top: the owner never dropped B.] This directly contradicts multiple ingested and existing-context statements that describe the font pick as a still-pending three-pairing comparison. See BLOCKER and INFO entries in `INGEST-CONFLICTS.md`.
  3. **Section-sign character forbidden:** the owner forbids U+00A7 anywhere in this project; refer to spec parts by heading name, never by section sign. Every ingested source document (the design spec and both handoff docs) uses the section-sign character extensively (e.g. "section 8", "section 18", "section 6.1"), as does the project's own existing `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md`. This synthesis avoids the section-sign character throughout. See WARNING entry in `INGEST-CONFLICTS.md` for the existing-file cleanup this implies.
  4. **GSD project governance:** the project runs as a GSD project — requirements by ID, milestone, phases. The design spec is a source to ingest, not an authority above GSD. No contradiction found; every ingested source already treats the spec this way (the spec itself says "Source for `/gsd-new-project`"; the requirements/roadmap already exist as GSD artifacts).
  5. **Measured live, done today:** SPF on Kinsta DNS is now `v=spf1 include:_spf.google.com include:relay.kinstamailservice.com ~all`; all Zoho DNS records removed (Zoho unused for 2 years). CI workflow `ci.yml` (Node 24 install/check/build, job `build`) is on `main` via PR #3. The `main` branch ruleset requires the `build` check. The Vercel project is now connected to the GitHub repo with production branch `main`. Go-live is not near — the owner says design needs a lot of work first. This contradicts several "live state" facts recorded in both handoff docs below (no Git connection; SPF still broken; GitHub Pages workflow still undecided) and the "5-7 days" delivery framing in the design spec and existing `.planning/PROJECT.md`. See INFO entries in `INGEST-CONFLICTS.md` — the existing `.planning/STATE.md` Quick Tasks table already independently confirms the CI-workflow fact (commit `959adeb`), so that specific item is doubly confirmed, not merely asserted.

---

## Turnover handoff (docs/handoff/2026-09-25-turnover.md)

- source: /Volumes/Satechi Hub/zinc-digital-web-worktrees/phase-01/docs/handoff/2026-09-25-turnover.md
- written: 2026-09-25, 3:10 PM CT, by Kirk Musick

### Owner direction, in order of arrival that day
- content: (1) Clean slate; Astro on Vercel; core + migrated blog at launch; 5-7 days. (2) Fresh voice, no cursing, no sales devices, no legacy lines; core line approved. (3) Big headers, big moving parts, black and white, little color but loud; snow white never warm; dark teal on white, magenta only on black. (4) "Resolve all conflicts" — done in the design spec's Resolved decisions section. (5) "You need to start doing a visual check before you stop" — every hand-off needs the agent's own 1440 + 375 screenshots. (6) "I do not like the copy/wordsmithing" — supporting line under the core line rejected; links must not be underlined; page must not start with empty space. (7) Chose public Vercel URL, noindex, no custom domain yet. (8) "Our live site is way more crisp, tight, high def" — judging waits until the site is up. (9) "I have not even seen the first complete mockup yet" — next deliverable must be the complete nine-band homepage, not a lone hero. (10) Chose "Replan from scratch" for plans 01-02..01-05; that replan was started twice and interrupted twice, and (as of this doc's writing) not done.

### Live state (measured 3:03 PM CT, this doc)
- content: Public site `https://zinc-digital-web.vercel.app` → 301 → `/design-preview/a/` (HTTP 200), one page (Pairing A hero only), `x-robots-tag: noindex, nofollow` on every path. Vercel project `zinc-digital-web`, team `zincdigitalofmiamis-projects`, Pro plan paid, **no Git connection** (CLI-linked from the phase worktree). Protection `prod_deployment_urls_and_all_previews`; domains: vercel.app only; 3 deployments (1 Production, 2 Preview). zincdigital.co untouched, still WordPress on Kinsta behind Cloudflare; DNS in MyKinsta (Route 53 nameservers); registrar GoDaddy. GitHub `main` = `f5c7a90`; phase branch `gsd/phase-01-design-system-font-pick` = `e7fb6a5` pushed; PR #1 merged; PR #2 open (owner-opened). Local main checkout on `main` @ `1256025`. Email/SPF: zincdigital.co publishes two SPF records (permerror), neither authorizes Google Workspace; DMARC `p=none`. Copilot PR-review quota exhausted on this account.
- NOTE: superseded in part by "Owner corrections" item 5 above (Git connection now made; SPF now fixed).

### What is built — Phase 1, plan 01-01 (complete, per this doc)
- content: Commits `266e4fd`→`a8022b2` on the phase branch. Astro 7.3.5 + `@astrojs/vercel` 11.0.11, TypeScript, Node 24, `output: 'static'`. Tokens in `src/styles/tokens.css`, band theming in `themes.css`, `base.css`, `pairings.css`. Fonts via Astro's native Fonts API: Pairing A = Big Shoulders Display 800 / Inter 400 / JetBrains Mono 400, 3 woff2 files, metric-matched fallbacks. `PreviewLayout.astro`, `MockupBands.astro`, `Hero.astro`, `design-preview/a.astro`. `vercel.json` site-wide noindex header; `astro.config.mjs` `/` → `/design-preview/a/` redirect, both marked PRE-LAUNCH. Hero Display size `clamp(3.5rem, 10vw + 1rem, 12.5rem)` — do not shrink. Known open defects: supporting-line copy rejected; underlined-link check pending re-verification; crispness bar is the live WordPress site.

### What is NOT done (per this doc)
- content: Plans 01-02..01-05 stale — committed versions build a dark band + spec sheet + pairing routes + gates, which the owner rejected. Next deliverable per this doc: the complete nine-band homepage at `/` in Pairing A, deployed, then Pairings B/C on that full page, then gates + font pick, then collapse. Interrupted partial rewrite of 01-02 moved to `_TRASH_ZINC_CLEANUP`. Phases 2-7 not started. Owner inputs still needed: real testimonials (real vs placeholder), case-study receipts, "How we work" commitments, Jaymie + Wendy photos, logo permission confirmation.
- NOTE: this doc's framing of the next deliverable (homepage only, then font pairings B/C, then font pick) is superseded twice over by later project history: `.planning/phases/01-design-system-font-pick/01-CONTEXT.md` (existing context) records a further owner correction requiring the *entire* site mockup before any font judgment, and "Owner corrections" item 2 above records that the font pick itself is now already made. See `INGEST-CONFLICTS.md`.

### Things needing a decision or a human (per this doc)
- content: (1) PR #2 merge sequencing. (2) `.github/workflows/astro.yml` on `main` (owner commit `f5c7a90`) — the stock GitHub Pages workflow (Node 20) conflicts with the Vercel/Node 24 plan; decide delete or replace. (3) SPF repair in MyKinsta DNS — contact form must not go live before this. (4) Copilot review unavailable this period; use GSD's code-review agent instead. (5) Font pick and complete-homepage review are owner gates (Day 1 was planned as Sat 2026-09-26 CT).
- NOTE: items (2) and (3) are superseded by "Owner corrections" item 5 above (both now done and measured live); item (5)'s font-pick framing is superseded by "Owner corrections" item 2.

### Resume path and cleanup (per this doc)
- content: Resume by fetching/merging `origin/main`, running `npm ci && npm run build && npx astro check`, replanning Phase 1 from scratch with the brief in this doc's own section 1 items 5-10 plus the spec's Homepage section, executing, screenshotting at 1440/375 before any owner showing, then landing via PR and deleting the branch/worktree. Cleanup done at turnover: stopped local preview server, removed temp launch.json, moved researcher scratch/planner font probe/interrupted 01-02 partial to `_TRASH_ZINC_CLEANUP`; kept the npm cache, a scaffold temp dir, and the repo's gitignored `.scratch/`.

### Incident log (per this doc)
- content: First deploy to the Git-disconnected Vercel project auto-promoted to Production; the draft hero was briefly publicly reachable three times for seconds each (no secrets exposed); executor removed those deployments and changed protection. Owner then chose the public noindex production URL, applied in `6c04e71`.

---

## Complete Turnover handoff (docs/handoff/2026-09-25-turnover-complete.md)

- source: /Volumes/Satechi Hub/zinc-digital-web-worktrees/phase-01/docs/handoff/2026-09-25-turnover-complete.md
- written: 2026-09-25, 3:20 PM CT, by Kirk Musick
- notes: This document is a superset of the turnover handoff above (same author, 10 minutes later), structured in four parts: A (decision register), B (verbatim design spec + verbatim requirements register), C (next-build brief), D (live state, largely identical to the turnover handoff's live-state section). Per its own classification, its primary purpose is handoff/resumption, so it is filed here as DOC content, not routed to `constraints.md` or `requirements.md` even though Parts B1-B3 embed spec- and requirements-shaped material verbatim.

### Part A — Decision register (owner interview, 2026-09-25, 24 numbered items)
- content: Baseline: clean slate, WordPress/Framer-notes/July-prototype are fact sources only. Platform: Astro on Vercel in the existing empty repo, no CMS. Launch scope: core pages + migrated blog + full 301 map at cutover. Job of the site, services list (kept: business intelligence, Google Search Ads, local SEO; dropped: branding/graphic design), target client (OUABC primary, U.S. Oil second), core message and proof story, proof-allowed content and banned sales devices, voice rules (kirk-voice and zinc-author-voice retired/repurposed as noted), look direction (Loop as signature + editorial "annual report" type; black/white alternating), color (magenta on black, dark teal/#057E7E on white), motion (native CSS, no library, no 3D), structure (three layers, one flat URL per service), inquiry form fields and budget tiers, team roster and locations, blog/SEO scope, timeline ("no fixed date; sooner the better; 5-7 days"), case study order, research-conflict resolutions (matches the design spec's Resolved decisions section), process (GSD adaptive profile, "get this together this week"), deployment (public noindex vercel.app URL, no custom domain yet), visual QA requirement (1440+375 screenshots every hand-off), hero feedback (supporting line rejected, no underlines, no empty lead-in, crispness bar), and the next-deliverable correction: "I have not even seen the first complete mockup yet" → complete nine-band homepage, "Replan from scratch" chosen for plans 01-02..01-05, not yet done as of this doc.
- NOTE: item 24's framing (homepage-only next deliverable, three-pairing font pick still pending) carries the same supersession as the turnover handoff above — see `INGEST-CONFLICTS.md`.

### Part B1-B3 — Verbatim design spec, requirements register, and roadmap summary
- content: This section reproduces the full design spec (identical to the classified SPEC source `docs/superpowers/specs/2026-09-25-zinc-site-redesign-design.md` — already fully extracted into `constraints.md`) and a full 57-requirement register (v1 requirements DSGN-01..LNCH-04, v2 requirements CNTN-01..04, out-of-scope table, traceability table mapping every requirement to one of 7 phases) plus a one-paragraph roadmap summary per phase.
- NOTE: the embedded requirements register's DSGN-01 reads "Owner can compare three open-license variable font pairings rendered on the real homepage hero and a service spec sheet, and picks one" — an earlier wording than the current, already-amended DSGN-01 in the existing `.planning/REQUIREMENTS.md` (which requires the full clickable mockup first, per a documented owner correction). This embedded copy is a stale, superseded snapshot; do not use it as a requirements source. See `INGEST-CONFLICTS.md`.

### Part C — Next-build brief ("Replan from scratch," Phase 1 plans 01-02..01-05)
- content: Wave 2 — complete homepage at `/` in Pairing A, deployed and screenshot-verified at 1440/375 before any owner showing; replaces the `/`→`/design-preview/a/` redirect with a real `index.astro`; all nine bands per Part B2 with alternating `data-theme`; specific asset filenames named from the July prototype zip for OUABC, U.S. Oil, and team members; latest-articles band sourced live from the WordPress REST API for now. Wave 3 — Pairings B (Barlow Condensed/Public Sans/IBM Plex Mono) and C (Oswald/IBM Plex Sans/Space Mono) rendered on the same complete homepage at `/design-preview/b/` and `/c/`. Wave 4 — gates on the deployed URL (axe 0 violations, Lighthouse mobile 100 except is-crawlable, CLS 0, ≤15 KB JS, zero underlined links, band-theme check), then the owner's font pick. Wave 5 — collapse to the chosen pairing, remove extra routes, re-measure.
- NOTE: Wave 3 (render Pairings B and C) and Wave 4 (font pick as a future gate) directly contradict "Owner corrections" item 2 above (font pick already made, Pairing A only, B/C dropped). This is the same BLOCKER surfaced against the existing `.planning/phases/01-design-system-font-pick/01-CONTEXT.md` locked Type decision — see `INGEST-CONFLICTS.md`. This brief also predates the existing-context correction requiring the entire site (not just the homepage) before any font judgment.

### Part D — Live state, owner actions, incidents, resume (per this doc)
- content: Identical in substance to the turnover handoff's live-state, "what is NOT done," "things needing a decision," resume-path, cleanup, and incident-log sections above (written 10 minutes later by the same author). Same supersession notes apply: Git-connection and SPF facts are superseded by "Owner corrections" item 5; the GitHub Pages workflow decision point is resolved (ci.yml via PR #3, per existing `.planning/STATE.md`).

---

## Cross-document note: existing GSD project state (merge-mode reference, not re-extracted here)

- source: .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/01-design-system-font-pick/01-CONTEXT.md (existing context, read for comparison only — not modified by this agent)
- content: The project already has a live GSD state as of this ingest: `01-CONTEXT.md` records a further owner correction (full 40+ page site mockup required before any font judgment) that supersedes the homepage-only framing in the "Complete Turnover" Part C brief above, and locks a Type decision describing three font pairings still to be compared and picked. `STATE.md` shows Phase 1 actively executing plan 01-03 ("complete-site font comparisons") as of its last update (2026-09-25T21:38:05Z / 4:38 PM CT). `REQUIREMENTS.md` DSGN-01 already reflects the full-mockup-first correction but still frames the font pick as a three-way comparison to be decided later. None of this existing content yet reflects "Owner corrections" item 2 above (font already picked, Pairing A only), because that correction (~5:30 PM CT) postdates the existing state's last update (~4:38 PM CT). See the BLOCKER entry in `INGEST-CONFLICTS.md`.
