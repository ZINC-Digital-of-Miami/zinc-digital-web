---
phase: 01-design-system-font-pick
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - astro.config.mjs
  - src/data/mockup.ts
  - src/data/posts.preview.json
  - src/components/MockupPage.astro
  - src/components/MockupBands.astro
  - src/layouts/PreviewLayout.astro
  - src/pages/[...path].astro
  - src/pages/404.astro
  - src/styles/base.css
  - scripts/prepare-mockup.mjs
  - scripts/verify-mockup.mjs
  - scripts/gate-preview.mjs
  - public/mockup/kirk-musick.webp
  - public/mockup/bethany-mckinzie.webp
  - public/mockup/priya-nahar.webp
  - public/mockup/dr-basset.webp
  - public/mockup/martin-stewart.webp
  - public/mockup/ouabc-hero.webp
  - public/mockup/ouabc-site.webp
  - public/mockup/ouabc-mobile.webp
  - public/mockup/uos-app-desktop.webp
  - public/mockup/uos-app-phone.webp
  - public/mockup/mark-black.webp
  - public/mockup/mark-white.webp
  - public/mockup/wordmark-black.webp
  - public/mockup/wordmark-white.webp
  - public/favicon.png
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The owner can navigate the complete Pairing A site before any font-choice checkpoint: 40 sitemap pages, plus a genuine 404 response."
    - "Home has all nine bands in approved order; services has all 11 literal slugs; both case studies, about, contact, thanks, blog, all 18 articles, privacy and terms have full rendered bodies."
    - "Every navigation item, card, local article link, related-service link and internal CTA resolves; demo contact supports all 11 service preselection values without sending or retaining inquiry data."
    - "EP-DSGN02-adjacency and EP-DSGN03-adjacency: adjacent home bands meet without gaps, overlaps or nested theme changes; the sequence is light/dark/light/dark/light/dark/light/dark/light."
    - "EP-DSGN02-empty and EP-DSGN03-empty: every rendered band declares exactly one valid theme; missing or invalid theme fails verification."
    - "EP-DSGN02-encoding: decoded DOM text preserves the approved core line, accented names, curly punctuation and budget en dashes; comparisons do not use byte length as rendered width."
    - "EP-DSGN02-ordering and EP-DSGN03-ordering: stable home/service/layer order is independent of OS theme; only the seven-member team order changes, without loss or duplication."
    - "UI-02/UI-03/UI-04: 320–2560px layouts support the longest actual service names and zero/one/two/six-item lists; empty article filters show a visible recovery state."
    - "UI-05/UI-06: circuit mark and wordmark preserve native ratios and stay at or below half source width; every displayed image has reserved dimensions and a 2x-capable source."
    - "UI-07: explicit data-theme controls ground and semantic colors in both OS color schemes; bright teal is decorative only and meaningful text uses AA-compliant ink."
  artifacts:
    - path: src/data/mockup.ts
      provides: "Typed sitemap, service/case/team fixtures, source references and explicit draft states"
    - path: src/data/posts.preview.json
      provides: "18 source-backed article identities and complete safe preview content"
    - path: src/components/MockupPage.astro
      provides: "Shared full-body templates and local demo interactions"
    - path: src/pages/[...path].astro
      provides: "Static routes generated from the complete manifest"
    - path: scripts/verify-mockup.mjs
      provides: "Manifest/build/browser/link/style/image/screenshot checks with failing exit codes"
    - path: scripts/gate-preview.mjs
      provides: "Public noindex target and project-protection assertions"
  key_links:
    - from: src/pages/[...path].astro
      to: src/data/mockup.ts
      via: "getStaticPaths produces the exact manifest, including root"
    - from: src/components/MockupPage.astro
      to: src/data/posts.preview.json
      via: "Article cards and article bodies share source IDs, slugs and dates"
    - from: service inquiry CTA
      to: /contact/?service=<slug>
      via: "Allowlisted service preselection, local validation and non-sending /thanks/ navigation"
    - from: src/components/MockupBands.astro
      to: src/components/bands/Hero.astro
      via: "Pass approved core line context and a visible [DRAFT] supporting slot; preserve hero clamp"
  prohibitions:
    - id: PROH-01
      statement: "A mockup must not imply that an inquiry was sent or that draft claims, receipts, commitments, testimonials or legal text are owner approved."
      status: unverified
      flagged: true
    - id: PROH-02
      statement: "Font comparison must not omit any sitemap destination or secretly change content and layout between pairings."
      status: unverified
      flagged: true
    - id: PROH-03
      statement: "Public preview exposure must not imply production launch or expand access to internal client applications."
      status: unverified
      flagged: true
---

<objective>
**As a** site owner, **I want to** browse the complete approved site in Pairing A **so that** I can judge its design before choosing type.

The latest full-site correction in 01-CONTEXT.md supersedes the turnover's homepage-only next build and the UI-SPEC's original two-surface scope. Retain completed 01-01. This plan alone delivers the entire clickable mockup, locally and on the named public noindex review URL. Later phases retain final copy approval, production motion refinement, mail/anti-abuse, polished migration, SEO, analytics and cutover.
</objective>

<execution_context>
@/Users/zincdigital/.codex/gsd-core/workflows/execute-plan.md
@/Users/zincdigital/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system-font-pick/01-CONTEXT.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-RESEARCH.md
@.planning/phases/01-design-system-font-pick/01-PATTERNS.md
@.planning/phases/01-design-system-font-pick/01-01-SUMMARY.md
@docs/superpowers/specs/2026-09-25-zinc-site-redesign-design.md
@docs/handoff/2026-09-25-turnover-complete.md
@astro.config.mjs
@src/components/bands/Hero.astro
@src/layouts/PreviewLayout.astro
</context>

<authority>
Goal: complete full-site design mockup. Scope: static rendered pages and local demo behavior. Remote: https://github.com/ZINC-Digital-of-Miami/zinc-digital-web.git; eventual landing target main, working only in /Volumes/Satechi Hub/zinc-digital-web-worktrees/phase-01 on gsd/phase-01-design-system-font-pick. Deployment target: existing zinc-digital-web project under zincdigitalofmiamis-projects, https://zinc-digital-web.vercel.app, owner-authorized in turnover and current CONTEXT. Verify current project identity before deployment. No custom domain, DNS, mail, secrets, paid add-on or live WordPress change belongs to this plan. Do not wait for the separate GitHub Pages workflow decision or SPF repair to render locally. Branch updates, if authorized and necessary, use rebase; never merge main into the work branch. Storage, browser profiles, caches and evidence stay under this external worktree's .scratch/.
</authority>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render the home → Shopify → contact-demo → thanks path from the real fixture pipeline</name>
  <files>scripts/prepare-mockup.mjs, src/data/mockup.ts, src/components/MockupPage.astro, src/pages/[...path].astro, astro.config.mjs</files>
  <read_first>astro.config.mjs; src/layouts/PreviewLayout.astro; src/components/MockupBands.astro; src/components/bands/Hero.astro; package.json; 01-PATTERNS.md; approved spec §§4–6,13; current 01-CONTEXT.md. Read only the zip asset entries listed below, never its HTML/CSS.</read_first>
  <behavior>
    A built root page reaches /services/shopify/, whose CTA reaches /contact/?service=shopify with that checkbox checked; a valid demo advances to /thanks/ with an explicit “Demo only — nothing was sent” message. Unknown service values cannot become HTML or select another service. No-JS shows all fields and a clearly labeled confirmation-preview link without sending field values.
  </behavior>
  <action>
    Start by making prepare-mockup.mjs --verify-tracer assert the built HTML path, then implement it. Keep five authored files; the content JSON and raster outputs below are deterministic generated inputs, not additional implementation modules. Define typed route records with path, template, title, draft status, layer, source and publication metadata. Use a temporary explicit active-route allowlist for these four destinations; Task 2 expands it to the complete manifest in this same plan. getStaticPaths returns path: undefined for root and slash-free segment strings elsewhere. Remove only the root redirect from Astro config; retain its adapter, font entries and static output.

    Author the complete fixture records now: home, services index, the eleven service slugs shopify, web-design, apps, seo, local-seo, ai-search-optimization, google-search-ads, shopping-ads, social-ads, tiktok-ads, business-intelligence; work index and once-upon-a-book-club/us-oil-solutions; about; contact; thanks; blog index; privacy; terms. Layer assignments are Build 3, Demand 7, Intelligence 1. Each service has distinct draft deliverables/cadence/ownership/reporting, 4–6 buyer FAQs, relevant proof and related-service edges. Both cases follow result strip → situation → lit loop → work by layer → device screenshots → attributable quote only when confirmed → next case. Unconfirmed numbers use [RECEIPT: …]; commitments/testimonials use [OWNER CONFIRM], unapproved body/legal copy [DRAFT].

    prepare-mockup.mjs reads the public WordPress REST posts endpoint with per_page=100, status=publish and source author/date/slug/content fields; read response pagination/count headers, reconcile the expected 18 IDs and full bodies, and stop/report a source-count mismatch instead of inventing a post or silently dropping one. Fetch authors through public REST when required. Preserve source IDs, canonical URLs, original timestamps and source retrieval time in src/data/posts.preview.json. Produce safe structured paragraphs/headings/lists/links from the complete article bodies with the already installed browser DOMParser, dropping executable elements/attributes and unsafe URLs; never inject raw provider HTML. Flag each article “Draft migration preview — editorial review pending.” This proves 18 source-led destinations, not the Phase 5 polished migration. Assign draft layer/related-service mappings from each article's actual subject; no legacy taxonomy import. Missing author details get [OWNER CONFIRM], not invented attribution. Date-descending order uses numeric source ID as deterministic tie-breaker. Homepage's latest three link to these local article routes. Builds read the committed snapshot, not a live fetch.

    Extract only assets/kirk-musick.png, assets/bethany-mckinzie.png, assets/priya-nahar.png, assets/dr-basset.png, assets/martin-stewart.png, assets/ouabc-hero.png, assets/ouabc-site.jpg, assets/ouabc-mobile.jpg, assets/uos-app-desktop.png and assets/uos-app-phone.png from /Volumes/Satechi Hub/ZINC Digital Agency/ZINC Digital Agency Website V2.zip. Prepare dimensioned WebP copies at public/mockup/{matching-basename}.webp using the already installed sharp. Copy/convert the four brand sources from UI-SPEC to public/mockup/mark-black.webp, mark-white.webp, wordmark-black.webp, wordmark-white.webp and create public/favicon.png from the supplied mark. Record source hashes/dimensions and output dimensions in the fixture metadata; no upscaling. Existing sharp is a transitive package: resolve it locally; do not add or install packages. Responsive width variants may be generated alongside these exact basenames and must be enumerated in the output manifest. Browser profiles/raw REST/zip extraction scratch stay in .scratch/phase01-mockup/, never the internal disk. Generated public assets and the safe content snapshot are committed; raw responses remain ignored.

    Define the interface now: MockupPage receives a typed route record, pairing ('a' initially) and basePath (empty for canonical pages). Export a toSitePath(path, basePath, query?, fragment?) helper from mockup.ts; every local navigation/CTA/confirmation link uses it. This makes the later B tracer work by passing a pairing prefix without a new routing architecture. Render the tracer as real semantic static markup using PreviewLayout and these props. Contact is labeled “Demo inquiry — no message will be sent” before its fields and action. Include all specified fields, grouped services and exact monthly budget options. Use type=button for demo advancement, local constraint validation and an explicit /thanks/ navigation containing no personal input; prevent Enter/default form submission. Do not write form input to storage, cookies, URL, logs or network. With scripts disabled show a single page of fields, a demo explanation and a direct preview-confirmation link. No email provider, submission endpoint, telemetry or Turnstile is connected here.
  </action>
  <acceptance_criteria>Four tracer routes contain usable bodies; the service CTA and non-sending contact transition connect end to end. Fixture inventory contains all 18 actual source articles and all 11 approved service IDs. Asset hashes/dimensions and visible content-status labels are present. Source failure is an actionable blocker, never fabricated content.</acceptance_criteria>
  <verify><automated>node scripts/prepare-mockup.mjs --check && npm run build && npm run check && node scripts/prepare-mockup.mjs --verify-tracer</automated><fails_when>The command exits nonzero if source inventory is not18 real articles/11 services, an asset fails provenance or dimensions, build/typecheck fails, or any tracer destination/CTA/demo contract is absent.</fails_when><failure_direction>If source count, parsing, asset dimensions or tracer navigation fails, repair that input/route before expanding; never reduce the full-site inventory.</failure_direction></verify>
  <done>The four-page real path works; the complete source-backed fixture and approved assets are ready for the remaining templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the proven route renderer into every approved page and interaction</name>
  <files>src/data/mockup.ts, src/components/MockupPage.astro, src/components/MockupBands.astro, src/layouts/PreviewLayout.astro, src/styles/base.css</files>
  <read_first>Task 1 fixture/interfaces; src/styles/tokens.css; src/styles/themes.css; src/styles/base.css; src/components/bands/Hero.astro; UI-SPEC superseding full-site section; approved spec §§5–9.</read_first>
  <behavior>
    The manifest enumerates exactly 40 sitemap pages: root + services index/11 details + work index/2 cases + about/contact/thanks + blog index/18 articles + privacy/terms. All list/detail navigation resolves. Filtering supports all three layers and no-results recovery. Team permutation preserves seven unique identities. Service preselection supports all eleven values and safely ignores unknown/repeated hostile values.
  </behavior>
  <action>
    Expand the active-route set to the complete manifest. MockupPage dispatches on a discriminated template value; shared navigation/footer/mini-loop can remain local render helpers within this component to bound file ownership. Use typed props and the same records throughout, not independently copied pages. Keep rendering logic grouped by template for later extraction; page components do not fetch.

    MockupBands renders all nine homepage bands in exact order and alternating grounds: Hero light; logo wall dark; full linked Loop light; OUABC dark; U.S. Oil light; 4–5 marked commitments dark; seven-person team light; latest three source articles dark; footer light. Pass a visible [DRAFT] supporting slot to the existing Hero so its rejected default sentence never renders. Preserve clamp(3.5rem, 10vw + 1rem, 12.5rem), the core line and teal decorative marks. Footer carries a draft closing line, local inquiry entry, phone, hello@zincdigital.co, both approved addresses and source-confirmed social destinations. Omit an unconfirmed social URL as a link and mark [OWNER CONFIRM]. Logo wall includes all seven named clients; where an authorized logo asset is unavailable, use a clean monochrome name with [LOGO PENDING]. No invented emblem.

    Render full services and work indexes; full service template with hero, linked mini-loop, all four spec fields, proof, FAQs, preselected inquiry; both full case narratives with result markers and real screenshot device frames. Public client links alone may leave the site; internal reporting/app surfaces are screenshots only. About shows all seven confirmed names/roles and locations, plus marked engagement copy. Jaymie/Wendy use sized [PHOTO PENDING] tiles; five authorized portraits render B&W. Shuffle the complete seven-person set on load and once on entering the home team band, with stable reserved geometry and without changing focused elements; reduced-motion mode uses a still complete arrangement.

    Blog index renders all 18 source records, real dates/titles, All/Build/Demand/Intelligence filters, a visible result count and clear-filter recovery. Without JS all cards remain usable. Article routes render the full safe preview body, author card with source-grounded facts, related services and return navigation; cards never link back to WordPress. Privacy/terms are substantial designed draft legal layouts with headings and [DRAFT — OWNER/LEGAL REVIEW REQUIRED], no claim of accepted terms. Thanks explicitly states that nothing was sent and offers return/contact links.

    Make the site navigation keyboard-usable with a mobile menu only if needed, accurate aria-expanded, Escape close and visible focus. Every preview link resolves now; remove the existing “All pairings” link until 01-03 creates it. Change PreviewLayout's “Not public” description to an accurate public draft description and replace its lone-preview strip with compact integrated site navigation/draft identification. Keep page content at the top: no empty intro, hero top padding at most the existing 32px, no fixed-height spacer. All anchor states, including button hover/focus, use text-decoration-line:none with meaningful non-underline hover and focus cues.

    Extend existing plain CSS only. Token colors remain centralized; each band has one data-theme. Bright teal is decorative, underlying hero ink stays dark, small teal is #057E7E; magenta appears only on dark. Use responsive grids, intrinsic image ratios, 44px targets, max-width/min-width handling and word wrapping that preserve actual long names. Native view transitions and lightweight native CSS scroll reactions may demonstrate approved motion, always with immediately readable finished states under prefers-reduced-motion and unsupported scroll timelines. Loop links/text and meaningful lines meet contrast independently of decorative bright teal. No animation library, framework, paid service or new dependency.
  </action>
  <acceptance_criteria>All 40 complete page bodies and every required demo state are reachable. Every home band is present. The article set is source-backed and complete; legal/content uncertainty is visible. Desktop/mobile layout, keyboard operation, static fallbacks, exact type clamp, palette and top placement meet the UI contract.</acceptance_criteria>
  <verify><automated>npm run build && npm run check && node scripts/prepare-mockup.mjs --verify-site</automated><fails_when>The command exits nonzero if build/typecheck fails, any of the40 canonical routes or required template sections is absent, an article body is incomplete, or a service/query/link contract is invalid.</fails_when><failure_direction>If a destination/body/state is missing, finish it within 01-02; do not move it behind the font pick or replace it with a card-only summary.</failure_direction></verify>
  <done>The entire Pairing A site renders locally with complete navigation, content and demo behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove every route and template, add genuine 404, and publish the complete A review</name>
  <files>src/pages/404.astro, scripts/verify-mockup.mjs, scripts/gate-preview.mjs</files>
  <read_first>Task 1–2 source and manifest; package.json; vercel.json; .vercel/project.json (identity only, never credentials); turnover Part D public-alias correction; existing installed selenium-webdriver/axe-core/lighthouse. Do not reuse old pass claims.</read_first>
  <behavior>
    An unknown path yields HTTP 404 and the designed recovery page. The all-route crawl fails for missing bodies, broken destinations/fragments or incomplete inventory. Browser checks fail for submission/network leakage, missing UI states, underlined links, image upscaling, overflow, color violations or absent screenshots. Demo failures are visible and keyboard-announced.
  </behavior>
  <action>
    Add 404.astro through the shared layout/template with a marked draft recovery message and working Home/Services/Contact links. Ensure Vercel serves unknown paths with status 404, not a catch-all success response. This is one shared canonical error surface outside the A/B/C comparison; unknown canonical and prefixed paths use it. Capture it at1440/375 with the same applicable visual, accessibility, performance, link and noindex checks. Record its raw Lighthouse SEO score, including the expected http-status-code failure: the successful-page SEO100 gate does not apply to a genuine404. Do not turn the response into200 or hide that failure to raise its score.

    Create verify-mockup.mjs with --self-test, --mode quick, --mode full, --pairings, --base, --tracer, --parity and --selected options. Use existing selenium-webdriver, Chrome/driver and axe-core; no package installation. It manages a local Astro preview child process when --base is absent, uses external .scratch profiles/evidence and always closes only its own process. --self-test must prove failures on a deliberately missing route, wrong theme, invalid service and unsafe submission. Implement --tracer as the four-route path only and --selected as canonical routes using the selected config; --parity is extended in01-03. The --pairings parser accepts only a,b,c. Tracer A always uses canonical /, /services/shopify/, /contact/?service=shopify and /thanks/; tracer B/C use their explicit /design-preview/b/ or /design-preview/c/ prefixes. Thus01-02 --pairings a needs no prefixed detail routes, and01-03 Task1 --pairings a,b --tracer checks canonical A plus the B path that task has just created. A requested B/C route missing from that build fails; never infer a fallback from route availability. Full comparison/parity checks in01-03 Task2 explicitly add all three complete prefixed families, including A, alongside the40 canonical routes. Quick mode runs manifest/build assertions and the tracer in under 60 seconds; full mode runs the complete crawl and browser matrix. Derive expected URLs independently from the approved literal sitemap plus the 18 source IDs; do not accept the route generator's own output as the expected set. Crawl all40 canonical pages and links, fragments, asset loads, with404 tested separately. The existing /design-preview/a/ is a compatibility homepage alias at this stage; report and crawl it separately from the40-page canonical count; require nonempty template-specific sections and article body parity against the snapshot.

    Capture full-page screenshots at 1440 and 375 CSS pixels, DPR 2, for EVERY successful-page template: home, services index, service detail, work index, case study, about, contact, thanks, blog index, article, privacy and terms. Capture the shared canonical404 at both widths separately; it is outside pairing comparisons. Add longest-service and both case variants plus each distinct contact step/error/confirmation and blog no-results state. Capture static/reduced-motion views of motion scenes. Emit screenshots.json mapping route/template/width/pairing/artifact and a report with exact route/link/image/assertion counts. Check no horizontal overflow at 320,375,768,1024,1440,2560; font fallback and long-name/list cases; computed no-underlines in default/hover/focus; content top; each selected image's intrinsic/currentSrc dimensions >=2x CSS bounds; native aspect ratios; every band theme/accent; OS-light/dark invariance. Run axe against every template with WCAG2A/AA,2.1A/AA,2.2AA tags and require zero violations. Intercept network during all contact states: zero submission requests, no PII in URL/storage, no fake “sent” wording. Render no-JS fallback. Inspect screenshots before handing them to the independent reviewer; a script pass alone is not visual acceptance.

    Create gate-preview.mjs --check-target to read current Vercel project/deployment metadata via the authenticated installed CLI and assert project zinc-digital-web, team zincdigitalofmiamis-projects, Node24, no Git connection change, only project custom-domain entry zinc-digital-web.vercel.app, existing free Vercel Authentication protection for preview/raw deployment URLs, no paid add-on. It must fail closed when metadata is unavailable. An unauthenticated GET to the public review alias must return the expected page and noindex/nofollow in header AND meta; arbitrary path must stay 404 with noindex. A raw protected deployment URL must redirect to Vercel SSO. Protection does not make the public alias private; never store private data in the build.

    After local full verification and independent screenshot review, run the already authorized vercel deploy --prod --scope zincdigitalofmiamis-projects from this worktree. Keep all site-wide noindex headers. Record returned deployment ID/URL and source SHA or explicit dirty-source state in .scratch/phase01-mockup/deployment.json; never call that landed main. Run target checks and the complete route/browser matrix against https://zinc-digital-web.vercel.app. If deploy authorization context is unavailable, keep the fully verified local deliverable and report that specific deployment blocker; do not hold local rendering for SPF/Pages decisions. Show the complete A site with the evidence index; this is a design preview, not the font-choice checkpoint.
  </action>
  <acceptance_criteria>All 40 routes, genuine 404, every local link, required states and every template's 1440/375 images pass. Public alias, noindex, protected raw previews and domain restriction are freshly measured. Independent reviewer accepts completeness before owner handoff.</acceptance_criteria>
  <verify><automated>node scripts/verify-mockup.mjs --self-test && node scripts/verify-mockup.mjs --mode quick --pairings a && node scripts/gate-preview.mjs --check-target --base https://zinc-digital-web.vercel.app</automated><fails_when>The self-test must detect injected faults; quick verification exits nonzero on route/browser/style/demo safety failure; target verification exits nonzero on wrong project/domain/protection/noindex or unavailable metadata.</fails_when><human-check>Review every full-matrix screenshot and publish the evidence index only when the entire site is present.</human-check><failure_direction>Fail closed on missing evidence, incorrect target/protection/noindex, real submission, absent content or visual defect; fix the scoped source and repeat affected checks before handoff.</failure_direction></verify>
  <done>Complete A is locally validated, independently reviewed and available on the named public noindex alias, with measurable evidence and any real blocker reported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| WordPress REST/zip → committed preview fixtures | Source content is data, not executable markup or design authority |
| Browser form → network/storage | Public demo accepts no live inquiry and retains no entered fields |
| Local build → Vercel public alias | Draft material is public; private client applications/secrets remain excluded |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-02-A | Tampering | Article conversion/query preselection | high | mitigate | Safe structured DOM conversion; allowlisted slugs; escape data; verify unsafe-input cases |
| T-01-02-B | Information Disclosure | Demo contact | high | mitigate | Button-only local demo, prevent submission/Enter, no persistence or PII URL; browser network interception |
| T-01-02-C | Spoofing | Draft receipts/legal/thanks | high | mitigate | Visible status markers and explicit nothing-sent confirmation on all states |
| T-01-02-D | Information Disclosure | Public deployment | high | mitigate | Project identity/domain/protection checks, no secrets/internal links, noindex header+meta on every route |
| T-01-02-E | Denial of Service | Build-time REST source | medium | mitigate | Bounded public fetch, explicit failures, committed snapshot, no runtime dependency |
</threat_model>

<verification>
Quick: npm run build; npm run check; node scripts/verify-mockup.mjs --mode quick --pairings a.
Before any visual handoff: node scripts/verify-mockup.mjs --mode full --pairings a, followed by the same command with --base https://zinc-digital-web.vercel.app, and gate-preview --check-target. Full matrix may exceed 60 seconds; quick feedback remains bounded. These are required execution checks, not claims that planning ran them.
</verification>

## Artifacts Produced
- Complete static site and safe committed preview content/assets; reusable page renderer and manifest.
- .scratch/phase01-mockup/ contains ignored raw input, dimension provenance, route results, screenshots.json, full-page images and deployment evidence.
- 01-02-SUMMARY.md records actual checks, reviewer findings, source counts, uncertainty and deployment identity.

## Source Audit
| Source | Item | Coverage |
|---|---|---|
| GOAL | Entire site before owner's font decision | 01-02 all pages; 01-03 A/B/C; 01-04 owner gate; 01-05 retained site |
| REQ | DSGN-01 | 01-02 complete A; 01-03 same-site B/C; 01-04 pick; 01-05 chosen family |
| REQ | DSGN-02/03 | 01-02 exact tokens, grounds, full-site style/state assertions; 01-04 complete matrix |
| REQ | DSGN-04 | Retained 01-01 native Fonts; 01-03 real per-family output/cold-load check; 01-04 gates; 01-05 sole pairing |
| REQ | DSGN-05 | 01-02 original raster provenance/2x limits/both grounds; 01-04 screenshot/ratio checks |
| CONTEXT | Locked stack, hosting, branch, palette, theming, type, copy, mark, review gates | 01-02 tasks 1–3; 01-03 font-only variation; 01-04 and 01-05 measured retention |
| CONTEXT | Latest full-site correction and visible draft states | 01-02 tasks 1–3, overrides historical homepage/two-surface language |
| RESEARCH | Native Fonts API, provider fallback, one display preload, optimized metrics, no extra font tooling | Retained 01-01; 01-03 tasks 1–2; 01-04 font gates |
| RESEARCH | Protected previews/noindex/free auth and scaffold collision pitfalls | 01-01 scaffold retained; 01-02 exact public-alias correction + raw preview protection; 01-04 target gate |
| RESEARCH | Per-route Lighthouse/axe/CLS/budget, B/C static caveats | 01-02 screenshot/axe checks; 01-03 pairings; 01-04 measured gates |
| CONTEXT | Production integrations, polished copy/migration, SEO/redirects, cutover | Explicitly later-phase requirements; their visible page designs are fully included here |
No D-NN IDs were assigned in the source CONTEXT; the rows above cite its locked section names rather than inventing decision IDs. No in-scope item is omitted.

## Edge/Prohibition Ledger
The 12 supplied edge items are preserved: seven explicit DSGN-02/03 truths above; three DSGN-01 items and one DSGN-04 item are carried in 01-03; DSGN-05/unclassified is a flagged assumption in 01-04. Precisely: DSGN-02 has four items; DSGN-03 has three (seven here), DSGN-01 has three, DSGN-04 one, DSGN-05 one =12.
Prohibition recall considered completeness, comparison manipulation, false proof, demo deception, internal-app exposure, paid scope, tracking, broken links, unsafe HTML and clipping. Routine correctness belongs to tests; injection/privacy canon belongs to the ASVS1 register. Three bespoke product prohibitions survive as flagged-unverified, descriptor-less must_haves.prohibitions. No wired check is fabricated. Human judgment must resolve them; automated pass does not silently accept them.

<success_criteria>
A complete clickable full-site mockup is available before any font pick, with 40 content destinations, 404 and every required state. Incomplete full-site rendering cannot be called done because the home page looks finished.
</success_criteria>

<output>
Create .planning/phases/01-design-system-font-pick/01-02-SUMMARY.md. Keep reusable artifacts and required evidence, report actual kept/deleted scratch sizes, and hand off to 01-03.
</output>
