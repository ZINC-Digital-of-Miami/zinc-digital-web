---
phase: quick-260926-6g7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/layouts/PreviewLayout.astro
  - src/components/CircuitThread.astro
  - src/components/SiteFooter.astro
  - src/components/TeamBand.astro
  - src/components/home/HomeBands.astro
  - src/components/MockupPage.astro
  - src/pages/[...path].astro
  - src/pages/404.astro
  - src/styles/tokens.css
  - src/styles/themes.css
  - src/styles/base.css
  - src/styles/home.css
  - src/data/mockup.ts
  - scripts/verify-mockup.mjs
  - scripts/check-site.mjs
  - .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs
files_deleted:
  - src/pages/design-preview/[...path].astro
  - src/pages/design-preview/a.astro
  - src/styles/pairings.css
  - src/components/MockupBands.astro
  - src/components/bands/Hero.astro
autonomous: true
requirements: [quick-260926-6g7]

estimate:
  tokens: 210000
  raw_tokens: 210000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Vercel build of the branch succeeds (PR preview deployment state Ready), with the root cause of FONT_OUTPUT_INCOMPLETE on 404.html identified from evidence and fixed, and the font check still enforcing exactly three self-hosted Pairing A families (Big Shoulders Display 800, Inter 400, JetBrains Mono 400) with exactly one font preload, the display face."
    - "Every built route (home, services index, 11 services, work index, 2 cases, about, contact, thanks, blog index, 18 articles, privacy, terms, 404) renders on the snow #F5F6F7 ground with ink #0A0A0B type, 3px ink rules between bands, and no band with a black ground; html and body are painted snow."
    - "Every page carries the circuit thread: a 2px #thread line in the left gutter plus the magenta #thread-dot that moves down in proportion to scroll; both have pointer-events none, the dot and its halo stay inside the gutter at 375px and 1440px, and under prefers-reduced-motion or with no JavaScript the dot sits still at the top of the thread."
    - "The homepage shows the sketch's nine bands in order (hero, logo wall, Loop, OUABC, U.S. Oil, How we work, team, field notes, footer) with the sketch's content and markers, and every link on it resolves to a built route, an in-page anchor, sms:+17865754837, mailto or an external https URL (no bare '#')."
    - "With JavaScript off, in a hidden tab, and under prefers-reduced-motion, every piece of homepage content is visible (all three Loop layers and their service links, all three How-we-work lines, all headline words); motion arms only through the .anim class; client JS per page is at most 15360 bytes gzip and uses no library."
    - "The /design-preview/ routes, the Pairing B and C font families, pairings.css and the pairing selector are gone; noindex (meta and X-Robots-Tag), the non-sending contact demo, every [DRAFT]/[RECEIPT]/[OWNER CONFIRM]/[LOGO FILES PENDING]/[PHOTO PENDING] marker and the 404 remain."
    - "No link is underlined anywhere; links and buttons show a visible hover and a visible focus-visible outline; every non-inline tap target measures at least 44px tall and 44px wide."
  artifacts:
    - path: "astro.config.mjs"
      provides: "Three Pairing A font families only; completeFonts check keeps its intent and reports measured values on failure"
      contains: "--font-display"
    - path: "src/components/CircuitThread.astro"
      provides: "#thread and #thread-dot markup, CSS and the scroll-progress script"
      contains: "thread-dot"
    - path: "src/layouts/PreviewLayout.astro"
      provides: "Shared frame: skip link, masthead, CircuitThread, main slot, SiteFooter, menu toggle; noindex meta"
      contains: "CircuitThread"
    - path: "src/components/home/HomeBands.astro"
      provides: "Nine sketch bands and the progressive-enhancement motion script"
      contains: "getPointAtLength"
    - path: "src/styles/base.css"
      provides: "All-white design system: band, label, display, rule, btn, draft, masthead, footer, every non-home template"
    - path: "scripts/check-site.mjs"
      provides: "Static dist gate: thread on every page, fonts, no dark band, no pairing, no bare '#', link integrity, JS budget, U+00A7 byte scan of changed files"
    - path: ".planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/"
      provides: "12 screenshots (6 pages x 1440/375) plus results.json from shoot.mjs"
  key_links:
    - from: "src/layouts/PreviewLayout.astro"
      to: "src/components/CircuitThread.astro"
      via: "rendered once inside the .frame wrapper on every page, including 404"
      pattern: "<CircuitThread"
    - from: "src/components/home/HomeBands.astro script"
      to: "html.anim class"
      via: "the ONLY code that adds .anim; it also owns the IntersectionObserver that adds .in to every .kin on the page (footer included)"
      pattern: "classList.add\\('anim'\\)"
    - from: "astro.config.mjs completeFonts"
      to: "every dist HTML file"
      via: "astro:build:done hook asserting three sources and one display preload per page"
      pattern: "FONT_OUTPUT_INCOMPLETE"
    - from: "src/components/home/HomeBands.astro links"
      to: "src/data/mockup.ts toSitePath / services / posts / cases"
      via: "every internal href is built from route data, never hand-typed"
      pattern: "toSitePath"
---

<objective>
Restyle the whole site to the owner-picked sketch 001 variant 3 "All white", put the sketch's circuit thread on every page, fix the failing Vercel build, and hand the orchestrator a green PR to land on Vercel production (vercel.app only, no custom domain).

Owner, 2026-09-26 CT, verbatim: "land option 3, all white with this thing on the side.  dont connect domain yet, just land on vercel deploy" and "all pages, blogs, etc..". Earlier owner decisions: "keep the white, that dark was C" (no black bands) and Pairing A fonts (Big Shoulders Display / Inter / JetBrains Mono).

Requirement IDs used for traceability in this plan (from the orchestrator's planning context):
- R1 all-white language on every route; R2 homepage rebuilt from the nine sketch bands; R3 circuit thread on every page; R4 motion is progressive enhancement, JS at most 15 KB gzip, no library; R5 remove design-preview routes and Pairing B/C; R6 never the U+00A7 character, never the three-letter abbreviation for generative search; R7 gate with captured exit codes plus screenshot evidence; R8 landing (PR, checks, squash-merge, production measurement).
- Codex notes to honor (CX): no underlined links; "Text (786) 575-4837" is an sms:+17865754837 link; every tap target at least 44px; the inquiry CTA points to /contact/; html and body painted snow.

Output: all-white site on one font pairing, thread on every page, rebuilt homepage, dist gate script, screenshot evidence, a pushed branch with an open PR whose `build` check and Vercel preview are green.

Out of scope for the executor (the orchestrator does these after verification, see the landing block in verification): Codex CLI review, merge, production measurement, branch and worktree deletion. No custom domain, no DNS, no change to Vercel deployment protection, no new npm dependency.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Working tree (ALL paths relative to it; never touch any other checkout): /Volumes/Satechi Hub/zinc-digital-web-worktrees/quick-260926-6g7 on branch gsd/quick-260926-6g7-restyle-every-page-to-the-owner-picked-a, forked from origin/main 196756c. All times shown to the owner are America/Chicago, labelled CT. main is the product; this branch reaches main only through a squash-merged PR (ruleset: squash only, required check `build`).

@AGENTS.md
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/sketches/001-a-plus-b-homepage/index.html
@.planning/sketches/themes/default.css
@.planning/sketches/001-a-plus-b-homepage/README.md
@.planning/sketches/MANIFEST.md
@astro.config.mjs
@src/layouts/PreviewLayout.astro
@src/components/MockupPage.astro
@src/components/MockupBands.astro
@src/components/bands/Hero.astro
@src/styles/tokens.css
@src/styles/themes.css
@src/styles/base.css
@src/data/mockup.ts

Measured at planning time (2026-09-26, about 4:40 AM CT), from the working tree and live APIs:
- Build output goes to dist/ (the main checkout has dist/404.html, dist/_astro, route folders). `npm run build` builds 162 pages locally and in CI; CI workflow job `build` runs npm ci, npm run check, npm run build on Node 24.
- Commit 196756c on GitHub: check run `build` = success; commit status context `Vercel` = failure. Vercel is Git-connected and posts a status per commit. Failing deployment: dpl_Mrt42EZVEgGABUpLFS4gqdh5bFQW, error `FONT_OUTPUT_INCOMPLETE: 404.html must have three active sources and one preload`. Root cause NOT known. Note from reading the hook: it walks `readdir(dir,{recursive:true})`, where 404.html sorts before the letter-named route folders, so 404.html may simply be the first HTML file checked; do not assume the cause is specific to the 404 page, and do not assume it is not.
- main ruleset 24024093: deletion and non-fast-forward blocked, PR required, allowed merge method squash only, required status check `build` (integration 15368).
- astro.config.mjs configures nine font families (a/b/c) with cssVariables --font-display-a ... --font-mono-c; the completeFonts hook matches `data-pairing="([abc])"`. PreviewLayout renders Font components per pairing and a pairing-selector aside plus a script that rewrites pairing links; pairings.css maps [data-pairing] to --font-display/--font-body/--font-mono and --weight-heavy.
- Files that mention design-preview or pairing: src/components/MockupPage.astro, src/components/MockupBands.astro, src/data/mockup.ts (Pairing type, pairings array, toSitePath design-preview prefix), src/layouts/PreviewLayout.astro, src/pages/[...path].astro, src/pages/404.astro, src/pages/design-preview/[...path].astro, src/pages/design-preview/a.astro, src/styles/pairings.css, scripts/verify-mockup.mjs.
- data-theme="dark" occurrences: 15 in MockupPage.astro, 3 in MockupBands.astro. Only tokens.css contains hex colors in src/.
- MockupPage renders MockupBands three ways: home (full), part="team" (about page), part="footer" (every non-home page). Hero.astro is used only by MockupBands home.
- Data: services by layer in mockup.ts: Build = shopify, web-design, apps (3); Demand = seo, local-seo, ai-search-optimization, google-search-ads, shopping-ads, social-ads, tiktok-ads (7); Intelligence = business-intelligence (1). The sketch lists Business Intelligence under Build as well and says "4 services"; the data is the fact source, so Build shows 3. posts.preview.json has 18 posts; the first three are ai-search-results-and-generative-search-optimization (2026-06-09), google-shopping-ad-management-2026 (2026-05-23), shopify-seo-launch-checklist (2026-05-19), which match the sketch's field notes. clients in mockup.ts: Porsche, Home Depot, John Deere, YMCA, General Shale, Once Upon a Book Club, U.S. Oil Solutions.
- public/mockup assets and intrinsic sizes (assets.preview.json keys): wordmark-black 892x221, ouabc-site 1000x4841, ouabc-mobile 460x9940, kirk-musick 600x600, bethany-mckinzie 1160x1450, priya-nahar 2048x2050, martin-stewart 1020x1275, dr-basset 2048x2050.
- Tools present: puppeteer-core 24.43.1 resolves from node_modules (transitive, not in package.json); Google Chrome at /Applications/Google Chrome.app; python3 3.12.8; Vercel CLI 53.1.1 with `vercel curl --deployment`, `vercel inspect --logs --format json --wait`; codex at /Users/zincdigital/bin/codex. .vercel/ and .scratch/ are gitignored; .planning/ is tracked; .vercelignore excludes .planning/ and docs/.
- Sketch facts the executor must carry over and adjust: tokens --ink/--snow/--teal/--teal-text/--magenta/--gray-on-light/--rule-on-light map to tokens.css names; sketch sizes use cqi units against a #frame container (container-type inline-size); the sketch's 52px offsets (body padding-top, .loop-sticky top, the onScroll +52) exist only for the sketch's own variant nav and must be removed; the sketch's .typed spans are empty until JS runs and its Loop layers are dimmed/collapsed by default, both of which hide content without JS and must be inverted (see Task 3).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): one font pairing, all-white canvas and circuit thread in the shared layout, green under a Vercel build</name>
  <files>astro.config.mjs, src/layouts/PreviewLayout.astro, src/components/CircuitThread.astro, src/styles/tokens.css, src/styles/themes.css, src/styles/pairings.css (delete), src/data/mockup.ts, src/pages/[...path].astro, src/pages/404.astro, src/pages/design-preview/[...path].astro (delete), src/pages/design-preview/a.astro (delete), src/components/MockupPage.astro, src/components/MockupBands.astro, scripts/verify-mockup.mjs, scripts/check-site.mjs</files>
  <read_first>astro.config.mjs; src/layouts/PreviewLayout.astro; src/styles/tokens.css; src/styles/themes.css; src/styles/pairings.css; src/data/mockup.ts lines 1-20; .planning/sketches/001-a-plus-b-homepage/index.html lines 31-34, 188-191, 483-489; .planning/sketches/themes/default.css</read_first>
  <action>
Goal of the slice: one route path proven end to end, config (fonts) to layout (canvas, frame, thread) to every built page to a Vercel build that passes.

A. Diagnose the Vercel failure FIRST, from evidence (R8 precondition, owner: measure, do not guess).
1. Capture the failing log: `npx vercel inspect dpl_Mrt42EZVEgGABUpLFS4gqdh5bFQW --logs --scope zincdigitalofmiamis-projects > .scratch/vercel-fail.log 2>&1; echo EXIT=$?`. Read every font-provider and warning line before the error (download failures, fallbacks, provider warnings).
2. Reproduce locally: `npx vercel link --yes --project zinc-digital-web --scope zincdigitalofmiamis-projects`, then `npx vercel pull --yes --environment=preview --scope zincdigitalofmiamis-projects`, then `npx vercel build > .scratch/vercel-build-before.log 2>&1; echo EXIT=$?` on the unchanged tree. Delete .vercel/.env.*.local immediately after each build (keep .vercel/project.json until landing; it is gitignored).
3. Compare the font sources (url("...woff2")), preload links and the @font-face order in dist/404.html from `npm run build` against .vercel/output/static/404.html (or the file the hook actually read; print the hook's dir), and against one other page. If the local Vercel build does NOT reproduce the failure, push the branch (no PR yet) to get a Vercel preview build of the same tree and read its log with `npx vercel inspect <preview-url> --logs --scope zincdigitalofmiamis-projects`. Candidate causes to test, none assumed: a provider download that differs on Vercel's build image, a different output dir or file set in the hook's dir, HTML serialization differences (quotes, attribute order) that break the hook's regexes, a preload whose href differs from the first @font-face src.
4. Write the proven cause, with the log lines and file comparisons that prove it, into the task's SUMMARY section. Fix the cause itself.

B. Collapse to Pairing A (R5). In astro.config.mjs keep only three families: Big Shoulders Display 800 (fontsource), Inter 400 (google), JetBrains Mono 400 (google), with cssVariable renamed to --font-display, --font-body, --font-mono so no mapping file is needed. Keep one weight per family so exactly three woff2 files exist (the sketch's Inter 500 usages render at 400; state this in SUMMARY). Rewrite completeFonts without the data-pairing lookup while keeping its intent: exactly fontFamilies.length woff2 files in _astro/fonts; every HTML declares all three cssVariables with their family names; exactly three distinct woff2 sources, each a real wOF2 file; exactly one font preload, and its href equals the src of the @font-face whose font-family is the display family (find it by name, do not rely on the first @font-face). Every thrown message names the file and the measured values (sources.size, preload count, preload href, display src) so a future failure explains itself. Delete src/styles/pairings.css; move --weight-heavy: 800 into tokens.css.

C. Remove comparison routes (R5): delete src/pages/design-preview/[...path].astro and a.astro. In mockup.ts remove the Pairing type, the pairings array and the design-preview prefix branch of toSitePath (keep its route validation, query and fragment handling). Remove pairing and basePath props from [...path].astro, 404.astro, MockupPage.astro and MockupBands.astro (mechanical; link helpers call toSitePath with no basePath). In PreviewLayout remove the pairing-selector aside, its style and the part of the script that rewrites pairing links; keep the menu-toggle script, the noindex meta and the description. The "PUBLIC DESIGN DRAFT / A" masthead tag becomes "PUBLIC DESIGN DRAFT". In scripts/verify-mockup.mjs remove the comparison-only paths (the --pairings b/c values, --parity, --tracer, every /design-preview/ prefix, the pairing-selector and data-pairing assertions); this script is not in package.json scripts or CI, so the bar is `node --check` plus zero hits for design-preview, pairing-selector and data-pairing.

D. Canvas, frame and thread (R1, R3, CX canvas). tokens.css: add the sketch tokens under site names (--gutter as clamp(1.5rem, 4cqi, 4rem), --t-mega, --t-display from default.css, --rule as the ink colour, --space-* already exist), and re-comment --color-magenta as "circuit thread dot only". Keep tokens.css the only file with hex values: every other colour, including the dot halo, the photo-pending grey and shadows, is a token or a color-mix of tokens. themes.css: delete the dark block; keep a single light mapping applied to html and to [data-theme="light"] (bg snow, fg ink, muted gray-on-light, accent teal, accent-text teal-text). Paint html and body with the snow ground and ink text. In PreviewLayout wrap masthead, main and footer in a div.frame (position relative, container-type inline-size, overflow-x clip) exactly as the sketch's #frame, with the skip link outside it. Create src/components/CircuitThread.astro rendered once as the first child of .frame: #thread and #thread-dot as in the sketch lines 189-190 (thread: absolute, top 0, bottom 0, left calc(var(--gutter)/2 - 1px), 2px wide, snow with mix-blend-mode difference; dot: magenta with a color-mix magenta 22% halo), both aria-hidden and pointer-events none, displayed on every page (drop the sketch's variant gate). The gutter minimum is 1.5rem (sketch 1rem) so the dot lane fits: under a container width of 820px or less the dot is 10px with a 4px halo, above it 14px with a 6px halo, so dot plus halo stays left of the gutter's inner edge at 375px and 1440px. Script (TypeScript, no library, no innerHTML): if prefers-reduced-motion is reduce, do nothing (the dot stays at top 0, CSS default, which is also the no-JS state); otherwise on scroll (passive, rAF-throttled) and resize set dot top = progress times (frame height minus dot size), where progress = scrollY / (scrollHeight - innerHeight) clamped to 0..1 and 0 when the page is not scrollable. The thread script must NOT add the .anim class (only the homepage script arms motion, Task 3).

E. Create scripts/check-site.mjs (Node, no dependency) that reads dist/ and exits non-zero with a list of every failure. Tracer assertions now: every dist HTML file contains id="thread" and id="thread-dot", a robots meta with noindex, no data-pairing, no /design-preview/ substring, exactly one font preload and exactly three distinct woff2 URLs; dist/design-preview does not exist; a byte scan (0xC2 0xA7, the UTF-8 form of U+00A7) of every file from `git diff --name-only --diff-filter=d origin/main` plus `git ls-files --others --exclude-standard` finds nothing, printing how many files it scanned (R6). Structure the script so Tasks 2 and 3 append assertion groups. Never type the U+00A7 character in any file; build the needle from its bytes.

Then run: `npm run check`, `npm run build`, `node scripts/check-site.mjs`, and `npx vercel build` again (each `cmd > .scratch/<name>.log 2>&1; echo EXIT=$?`), deleting .vercel/.env.*.local afterwards. Commit on the branch.
  </action>
  <verify>
    <automated>cd "/Volumes/Satechi Hub/zinc-digital-web-worktrees/quick-260926-6g7" && npm run check > .scratch/t1-check.log 2>&1; echo CHECK_EXIT=$?; npm run build > .scratch/t1-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/t1-site.log 2>&1; echo SITE_EXIT=$?; npx vercel build > .scratch/t1-vercel-build.log 2>&1; echo VERCEL_BUILD_EXIT=$?; rm -f .vercel/.env.*.local; node --check scripts/verify-mockup.mjs; echo VERIFY_SYNTAX_EXIT=$?; grep -rlE "design-preview|data-pairing|pairing-selector" src scripts astro.config.mjs | wc -l</automated>
  </verify>
  <done>CHECK_EXIT=0, BUILD_EXIT=0, SITE_EXIT=0, VERCEL_BUILD_EXIT=0, VERIFY_SYNTAX_EXIT=0, and the final count is 0. astro.config.mjs lists exactly three families. SUMMARY states the proven root cause of the Vercel FONT_OUTPUT_INCOMPLETE failure with the quoted log lines and file comparison, and the fix. Every built page has the thread and dot. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: all-white design system, masthead, footer, team band and every non-home template</name>
  <files>src/styles/base.css, src/layouts/PreviewLayout.astro, src/components/SiteFooter.astro, src/components/TeamBand.astro, src/components/MockupPage.astro, src/components/MockupBands.astro, scripts/check-site.mjs</files>
  <read_first>.planning/sketches/001-a-plus-b-homepage/index.html lines 36-61, 155-176, 178-186, 193-222, 237-243, 365-399; src/styles/base.css (whole file, once); src/components/MockupPage.astro (whole file, once); src/components/MockupBands.astro (team and footer parts)</read_first>
  <action>
Apply the sketch's variant 3 language to every non-home template (R1) and fix the Codex notes (CX).

1. base.css: rewrite to the sketch system. Define each primitive once, and where MockupPage already uses a class name, put that name in the same selector list rather than duplicating declarations (or rename it in the markup, executor's choice, one rule set per primitive): .band (padding var(--space-5xl) var(--gutter); var(--space-4xl) under 820px container width) and `.band + .band { border-top: 3px solid ink }`; .label with .t-label and .eyebrow (mono, --t-label, 0.06em tracking, uppercase, muted); .display with .page-title, .section-title, .case-title, .layer-title (Big Shoulders 800 via --font-display and --weight-heavy, line-height .92, -0.012em, uppercase, sizes from --t-display/--t-heading/--t-mega); .draft with .draft-tag (mono .72rem, teal-text); .rule (3px ink) and .rule.thin; .btn with .button and .btn.solid (44px min-height, 2px border, mono uppercase; solid is ink fill; hover as sketch lines 57-61 with the variant-3 override at line 184); .sms (inline-flex, min-height 44px, mono). Colour rule: teal #07B2B2 (--color-teal) only on display-size glyphs (2rem and up) and non-text graphics; every smaller accent text uses --color-teal-text #057E7E. No black band anywhere: no band, card or panel gets an ink background; only .btn.solid and the focused skip link are ink-filled.
2. Links (CX): no text-decoration underline and no underline-imitating bottom border on any link, including the sketch's .sms and .layer li a hover borders. Hover = colour to teal-text (or ink fill for .btn); focus-visible = 2px solid ink outline with 3px offset on every a, button, summary, input, select, textarea. Article prose links stay teal-text (4.0:1 against ink body text, above the 3:1 non-colour distinction threshold). Keep `[hidden]{display:none !important}`, the skip link, @view-transition and the reduced-motion rule (extend it with the sketch's lines 216-222 so all transitions and animations stop).
3. Masthead in PreviewLayout, sketch lines 52-56 and 237-243: wordmark-black image 26px tall (width/height attributes from assets), the PUBLIC DESIGN DRAFT tag as a .draft marker, nav links Work /work/, Services /services/, About /about/, Articles /blog/ (mono uppercase, each inline-flex min-height 44px) and a .btn "Start an inquiry" to /contact/ (CX). Under 820px keep the existing menu-toggle disclosure (restyled as a 44px .btn) so every route stays reachable, with the inquiry .btn always visible.
4. Create src/components/SiteFooter.astro in the sketch's footer language (lines 172-176, 389-399) and render it from PreviewLayout after main on EVERY page, including 404: data-home-band="footer", display heading "Start an inquiry." with class kin, actions (.btn.solid "Open the form" to /contact/, .sms "Text (786) 575-4837" to sms:+17865754837), a 3px-ruled foot-grid carrying the existing facts from MockupBands' footer (Miami HQ and Panama City addresses, hello@zincdigital.co, the four social links and the "YouTube [OWNER CONFIRM]" marker, the [DRAFT] marker), and a fine row with the wordmark, "(c) 2026 ZINC Digital" written with the copyright sign, "Public design draft" and the Privacy /privacy/ and Terms /terms/ links. Every footer link is at least 44x44.
5. Create src/components/TeamBand.astro in the sketch's team language (lines 155-163, 366-377), data from mockup.ts team: 7 columns (2 under 820px), 4:5 photo tiles with grayscale images (width/height from assets, loading lazy, alt empty because the name follows), Jaymie and Wendy as initials tiles (JW, WF) that carry the existing [PHOTO PENDING] marker, name and mono role. Props: an optional heading label. No script here (the shuffle lives in Task 3's homepage script); the about page shows the data order.
6. MockupPage.astro: change every data-theme="dark" band to the light ground (remove the attribute or set light), give each page-intro the sketch hero header language (a .label meta row such as "ZINC Digital / {template or layer} / [DRAFT]", a .rule, then the h1 as .display), render TeamBand for the about page, drop the footer render (the layout owns it). Restyle, do not rewrite, the content of services index, service pages (layer grid, spec grid, FAQ details, related case, CTA to /contact/?service=slug), work index (client names as sketch logo cells with [LOGO PENDING] kept), case pages (receipts as .redact bars carrying their [RECEIPT: ...] text, device frames), about, contact demo (inputs on white, 2px ink border, 48px min-height; the demo notice and noscript text unchanged; it still never sends), thanks, blog index (filter buttons 44px, cards in the sketch field-notes style), articles (sidebar, prose with Inter body and display-face h2, [DRAFT] migration notice), privacy, terms, and 404. Every marker text stays verbatim. MockupBands.astro keeps only its home part until Task 3 removes it (delete its team and footer parts now).
7. check-site.mjs: append assertions, no dist HTML contains data-theme="dark"; no href="#" exactly; every internal href (starting with /, query and hash stripped) resolves to a dist file or dist folder with index.html; every page contains the footer's sms:+17865754837 link and a /contact/ link; the built CSS in dist/_astro contains no "underline"; neither dist HTML text nor src/ contains the uppercase three-letter abbreviation for generative search as a whole word (build the token from its letters in code so the script itself never contains it).

Run the gate commands and commit.
  </action>
  <verify>
    <automated>cd "/Volumes/Satechi Hub/zinc-digital-web-worktrees/quick-260926-6g7" && npm run check > .scratch/t2-check.log 2>&1; echo CHECK_EXIT=$?; npm run build > .scratch/t2-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/t2-site.log 2>&1; echo SITE_EXIT=$?; grep -c 'data-theme="dark"' src/components/MockupPage.astro src/layouts/PreviewLayout.astro src/components/SiteFooter.astro src/components/TeamBand.astro</automated>
  </verify>
  <done>CHECK_EXIT=0, BUILD_EXIT=0, SITE_EXIT=0, and every grep count is 0. Every non-home page shows the masthead, sketch-style bands on snow with 3px ink rules, and the shared footer with the sms link; no underline in built CSS; no bare '#' links; all internal links resolve. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: homepage nine bands from the sketch, motion as progressive enhancement, gate, screenshot evidence, push and open the PR</name>
  <files>src/components/home/HomeBands.astro, src/styles/home.css, src/components/MockupPage.astro, src/components/MockupBands.astro (delete), src/components/bands/Hero.astro (delete), src/data/mockup.ts, src/styles/base.css, scripts/check-site.mjs, .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs</files>
  <read_first>.planning/sketches/001-a-plus-b-homepage/index.html lines 46-49, 63-153, 179-186, 194-222, 245-387, 407-492 (already read in full once for Task 1 and 2 context; read only these ranges again if they are no longer in context); src/components/TeamBand.astro; src/data/mockup.ts exports services, cases, posts, clients, team, assets, toSitePath, dateLabel</read_first>
  <action>
Rebuild the homepage from the sketch (R2) with motion that only enhances (R4), then produce the gate and evidence (R7) and open the PR (R8).

1. Create src/components/home/HomeBands.astro rendered by MockupPage for template home; delete MockupBands.astro and bands/Hero.astro. Nine bands in order, each with data-home-band (hero, clients, loop, once-upon-a-book-club, us-oil-solutions, commitments, team, articles; the footer band comes from SiteFooter), all on the white ground, sketch content verbatim unless noted:
   - Hero (sketch 245-275): .label meta row (ZINC Digital / Operating report / Miami HQ, Working nationwide / Build, Demand, Intelligence), .rule, the only h1 on the page as .display.kin in four lines with .strike on "the scope." and .hl on "the business.", supporting line with [DRAFT], actions: .btn.solid "Start an inquiry" to /contact/ and .sms "Text (786) 575-4837" to sms:+17865754837 (44px, CX), and the Contents TOC whose counts come from services data (Build 3, Demand 7, Intelligence 1, The work 2 cases) linking to #loop (data-layer 0/1/2) and #work.
   - Logo wall (277-284): label "// Who's trusted us" plus the [LOGO FILES PENDING — NAMES SET AS WORDMARKS] marker, text wordmark cells from mockup.ts clients (data names win over the sketch's spelling), an aria-hidden empty eighth cell to complete the 4-column grid, variant-3 borders and teal-text flip colour.
   - Loop (286-309), id loop: the SVG with the sketch's exact path d strings, nodes, labels, #glow and #pulse, role img and the sketch's aria-label; layers list with the sketch's three subtitles; each layer lists its services from data as links to /services/{slug}/ built with toSitePath; the Intelligence layer adds "Reporting that feeds Build and Demand" as plain mono text (not a link). The status line (#loop-pct) server-renders "Loop complete / Intelligence feeds Build".
   - OUABC (311-339), id work: label, h2 "Almost three years." as .display.kin, mini loop, [DRAFT] line, three figures with .redact "Receipt pending" and the three [RECEIPT: ...] markers, service chips, .btn "Read the case" to /work/once-upon-a-book-club/, the two cropped screenshots (ouabc-site desk, ouabc-mobile phone with the variant-3 ink border and lighter shadow) with width and height attributes from assets and loading lazy.
   - U.S. Oil (341-353): label, h2 "A website became the business." as .display.kin, [DRAFT headline] marker, five stamps (Website, SEO, App one, App two, Next build / Being scoped in teal), plus a .btn "Read the case" to /work/us-oil-solutions/.
   - How we work (355-363): label with [OWNER CONFIRMS EACH LINE]; the sketch's three lines come from a new mockup.ts export homeCommitments (leave the existing commitments export untouched); each line's text is SERVER-RENDERED inside .typed and each li is rendered with class locked, so the no-JS state is complete.
   - Team: render TeamBand with the label "The people behind the work / Order shuffles on every visit".
   - Field notes (379-387), id notes: the first three posts from data as .note links to /blog/{slug}/ with dateLabel and layer, Inter 400 titles.
2. src/styles/home.css (imported by HomeBands): the sketch's band CSS (lines 46-49, 63-176 home parts, the variant-3 overrides 179-186 folded in as the only values, the container-query rules 194-214) with site token names, the 52px offsets removed (.loop-sticky top 0, height 100svh), .layer li a min-height 44px, no underline borders. Invert the sketch's hidden defaults so content shows without JS: by default .loop-band has auto height and .loop-sticky is static, every .layer is fully opaque with h3 at scale 1 and its list open, every node is filled; ONLY under html.anim do .loop-band become 340vh with a sticky inner, non-.on layers dim and collapse, words sit below their masks, the strike and highlighter start at scaleX 0, stamps start offset, and the typing cursor shows.
3. Homepage script inside HomeBands (TypeScript, no library, no innerHTML, textContent and createElement only), following sketch lines 407-492 with these changes: reduce = prefers-reduced-motion reduce OR document.visibilityState is not visible; when reduce, add nothing and run nothing that hides content (the team order may still shuffle once without animation). When not reduce, add class anim to document.documentElement (this script is the only place that does), wrap .kin words, observe every .kin on the page (the footer's included) plus the U.S. Oil, commitments, team and clients bands at threshold .25, flip logos, clear each .typed and type it then lock it when its band enters, animate the team reshuffle on entry (skip if focus is inside the grid), run the Loop from scroll (getTotalLength, strokeDasharray, the nodeAt sampling with getPointAtLength, setLoop moving #pulse and #glow, toggling node and layer .on, updating #loop-pct) with progress = clamp((-rect.top) / (band height - innerHeight)), and make the TOC data-layer links scroll to the layer's point in the pinned band. Scroll handling passive and rAF-throttled.
4. check-site.mjs: append home assertions: dist/index.html has the eight data-home-band values in order followed by footer; contains href="sms:+17865754837", "[LOGO FILES PENDING", "[OWNER CONFIRMS EACH LINE]", "[RECEIPT:", "[PHOTO PENDING]", the three homeCommitments texts inside .typed, and every service link; and per page, the gzip size of all its scripts (external /_astro/*.js it references, followed through static imports, plus inline scripts) is at most 15360 bytes, printing the largest page's figure.
5. Create evidence/shoot.mjs in the quick directory (puppeteer-core from node_modules, executablePath /Applications/Google Chrome.app/Contents/MacOS/Google Chrome; serve dist with `python3 -m http.server 4322 --directory dist` started and stopped by the script). Pages: /, /services/shopify/, /work/once-upon-a-book-club/, /blog/, /blog/ai-search-results-and-generative-search-optimization/, /404.html. For each page at 1440 and 375 wide, with prefers-reduced-motion reduce emulated (finished state), save a full-page PNG named {slug}-{width}.png in the evidence directory (JPEG quality 80 if a PNG exceeds 5 MB), and record in evidence/results.json: scrollWidth versus viewport width (375 must not exceed 375), html and body computed background rgb(245, 246, 247), #thread and #thread-dot present with pointer-events none, dot plus halo right edge left of the first band's content edge, every visible a, button, summary, input, select and textarea outside inline prose text at least 44x44 (list any violation), and whether the dot stays put after scrolling. Then with motion allowed on / at 1440: after scrolling top to bottom in steps, the dot's top moved, every .kin has class in, every .typed holds its full text, and #pulse moved. With JavaScript disabled on / at 375: all three Loop layers and every service link have non-zero boxes and full opacity, and all three commitments are present. Fix every violation in base.css or home.css and re-run until results.json shows none, then Read each PNG and check it for overlap, clipping and horizontal scroll; record what was checked in SUMMARY.
6. Gate on the exact tree to be pushed, each command's own exit code captured: `npm run check`, `npm run build`, `node scripts/check-site.mjs` (includes the U+00A7 byte scan of changed and untracked files), `node .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs`. Commit (message ends with the Co-Authored-By line from the session attribution), `git push -u origin gsd/quick-260926-6g7-restyle-every-page-to-the-owner-picked-a`, and open a PR to main with `gh pr create --base main` whose body summarises the restyle, the Vercel root cause and fix, the gate exit codes and the evidence file list, times in CT, ending with the Claude Code attribution line. Then wait for and measure: `gh pr checks <n> --watch` until `build` and `Vercel` both report, and `npx vercel inspect <preview-url> --scope zincdigitalofmiamis-projects --wait` shows Ready. If either fails, read its log, fix, re-run the gate, push again. Record the head SHA (`git ls-remote origin refs/heads/gsd/quick-260926-6g7-restyle-every-page-to-the-owner-picked-a`), PR number, check results and the preview deployment id and URL in SUMMARY with CT times. Do NOT merge.
  </action>
  <verify>
    <automated>cd "/Volumes/Satechi Hub/zinc-digital-web-worktrees/quick-260926-6g7" && npm run check > .scratch/t3-check.log 2>&1; echo CHECK_EXIT=$?; npm run build > .scratch/t3-build.log 2>&1; echo BUILD_EXIT=$?; node scripts/check-site.mjs > .scratch/t3-site.log 2>&1; echo SITE_EXIT=$?; node .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence/shoot.mjs > .scratch/t3-shoot.log 2>&1; echo SHOOT_EXIT=$?; ls .planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/evidence | grep -cE '\-(1440|375)\.(png|jpg)$'; gh pr checks "$(gh pr view --json number -q .number)" > .scratch/t3-pr-checks.log 2>&1; echo PR_CHECKS_EXIT=$?</automated>
  </verify>
  <done>CHECK_EXIT=0, BUILD_EXIT=0, SITE_EXIT=0, SHOOT_EXIT=0, screenshot count 12, PR_CHECKS_EXIT=0 with `build` pass and `Vercel` pass, the preview deployment Ready. results.json lists no horizontal scroll at 375, no tap target under 44x44, snow canvas, thread and dot on every captured page without covering content, full content with JS off and under reduced motion, and completed motion states. The PR is open, not merged, and SUMMARY records head SHA, PR number, preview deployment id and CT times.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host to font providers | Astro downloads Big Shoulders Display (fontsource) and Inter, JetBrains Mono (Google) at build time and self-hosts them |
| Visitor browser to static pages | Untrusted input only in the non-sending contact demo and URL query (?service=) |
| Local machine to Vercel project | `vercel pull` writes project environment values to .vercel/ locally |
| Repository to production | A merge to main deploys production on Vercel (SSO-protected vercel.app URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6g7-01 | Tampering | completeFonts build hook | medium | mitigate | Hook keeps rejecting builds with a missing family, a non-wOF2 file, more or fewer than three sources, or a wrong preload; messages carry measured values (Task 1 B) |
| T-6g7-02 | Information disclosure | .vercel/.env.*.local from vercel pull | medium | mitigate | .vercel/ is gitignored; env files deleted right after each local vercel build (Task 1 A2 and verify) |
| T-6g7-03 | Tampering (XSS) | kinetic word split, typing, ?service= prefill | medium | mitigate | textContent and createElement only, no innerHTML; ?service= accepted only if it matches a known service slug (existing toSitePath and form code kept) |
| T-6g7-04 | Information disclosure | contact demo form | low | mitigate | Form stays non-sending; no network request, no storage; demo notice and noscript text unchanged |
| T-6g7-05 | Information disclosure | search indexing of drafts | low | mitigate | noindex meta on every page (check-site asserts) and X-Robots-Tag in vercel.json unchanged |
| T-6g7-06 | Elevation of privilege | Vercel deployment protection, domains | high | mitigate | No change to SSO protection and no domain added; orchestrator measures aliases after merge (landing L4) |
| T-6g7-07 | Tampering | external links from case pages | low | accept | Existing rel="noopener noreferrer" kept on client-site links |
| T-6g7-SC | Tampering | npm installs | low | accept | No package is installed or added; puppeteer-core is already present transitively and used only by the evidence script, which never ships in the site build |
</threat_model>

<verification>
Executor gate (Task 3 step 6), every command with its own exit code: npm run check, npm run build, node scripts/check-site.mjs (thread and dot on all pages, fonts, no dark band, no pairing, no bare '#', link integrity, no underline, JS budget, U+00A7 byte scan of changed and untracked files, abbreviation check), evidence/shoot.mjs (12 screenshots plus results.json). PR checks `build` and `Vercel` pass; preview deployment Ready.

Landing, run by the orchestrator after the plan checker and verifier pass (owner-approved landing; times recorded in CT; SHAs recorded):
- L1 Codex CLI review, once, from this worktree before merging: `git fetch origin && codex review --base origin/main -c model=gpt-6-sol -c model_reasoning_effort=high > .scratch/codex-review.log 2>&1; echo EXIT=$?`. Mark each finding CONFIRMED or REFUTED against the source; fix confirmed findings, re-run the gate, push, and re-run the review only after a code or behaviour fix.
- L2 Merge: confirm `gh pr checks <n>` shows `build` pass and `Vercel` pass on the current head SHA, then `gh pr merge <n> --squash` (no --delete-branch yet). Record the merge SHA from `git ls-remote origin refs/heads/main`.
- L3 Production: `npx vercel ls zinc-digital-web --prod --scope zincdigitalofmiamis-projects`, then `npx vercel inspect <newest production url> --scope zincdigitalofmiamis-projects --format json --wait` shows readyState READY and the Git commit SHA equal to the merge SHA; `npx vercel inspect https://zinc-digital-web.vercel.app --scope zincdigitalofmiamis-projects` resolves to that same deployment.
- L4 Served content: `npx vercel curl / --deployment https://zinc-digital-web.vercel.app --scope zincdigitalofmiamis-projects > .scratch/prod-home.html; echo EXIT=$?` contains id="thread", data-home-band="loop" and "Other agencies deliver"; `npx vercel curl /blog/ --deployment https://zinc-digital-web.vercel.app --scope zincdigitalofmiamis-projects` contains id="thread"; the production aliases list only *.vercel.app names (no custom domain).
- L5 Cleanup once L3 and L4 are measured: delete the remote and local branch and this worktree, move .vercel/ and .scratch/ leftovers to /Volumes/Satechi Hub/_TRASH_ZINC_CLEANUP/2026-09-26/ if uncertain, report what was deleted and kept.
</verification>

<success_criteria>
- Vercel builds the branch and production successfully; the font check's intent is intact.
- All 40+ routes plus 404 render all white with the sketch's masthead, band, label, display, rule, button and footer language, and the circuit thread on every page.
- The homepage matches the sketch's nine bands with real routes, real assets and every marker.
- Content is complete without JS, in a hidden tab and under reduced motion; JS per page at most 15 KB gzip, no library.
- No design-preview routes, no Pairing B/C, no U+00A7 character, no underlined links, all taps at least 44px, sms link correct, canvas snow.
- Production deployment Ready on the merge SHA and serving the new homepage on https://zinc-digital-web.vercel.app, no custom domain.
</success_criteria>

<output>
Create `.planning/quick/260926-6g7-restyle-every-page-to-the-owner-picked-a/260926-6g7-SUMMARY.md` when done (Vercel root cause with evidence, gate exit codes, evidence list, PR number, head SHA, preview deployment, CT times).
</output>
