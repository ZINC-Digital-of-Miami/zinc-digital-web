---
phase: 01-design-system-font-pick
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - scripts/verify-mockup.mjs
  - scripts/gate-preview.mjs
  - lighthouserc.json
  - lighthouserc.cls.json
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All canonical and A/B/C routes, links and interaction states pass; every template/pairing has reviewed 1440px and 375px screenshots before the owner is asked to choose."
    - "Every successful-page template/pairing records mobile Lighthouse Performance, Accessibility and Best Practices100; SEO is100 with only is-crawlable skipped for intentional noindex, recorded as an adjusted preview score. The shared canonical404 keeps error status and reports raw SEO without the successful-page100 gate."
    - "Measured LCP is at most1200ms, cold/throttled layout shift is0, page JavaScript totals at most15360 gzip bytes, axe violations are0 and controlled interaction latency stays below100ms."
    - "UI-01 through UI-07 are measured: font loading, hero overflow, longest service headings, varying lists, native image ratios, raster ceilings and OS-independent themes."
    - "Public review alias is reachable without login with noindex/nofollow header and meta; raw previews stay protected; custom domain list contains only zinc-digital-web.vercel.app."
    - "Only the owner's explicit A/B/C answer authorizes collapse; an automated gate or agent preference cannot supply that answer."
  artifacts:
    - path: scripts/gate-preview.mjs
      provides: "Complete gate runner, evidence index, readiness check and no silent-skip behavior"
    - path: lighthouserc.json
      provides: "Mobile metrics and 100-score preview gates with explicit noindex adjustment"
    - path: lighthouserc.cls.json
      provides: "Cold devtools-throttled font/layout metrics"
    - path: .planning/phases/01-design-system-font-pick/01-04-SUMMARY.md
      provides: "Actual independent review evidence and verbatim owner font decision"
  key_links:
    - from: gate-preview
      to: complete manifest and screenshot index
      via: "Fail on missing route/template/pairing/width/state coverage"
    - from: owner font answer
      to: 01-05 precondition
      via: "Verbatim A/B/C decision with CT timestamp and reviewed deployment ID"
  assumptions:
    - id: EP-DSGN05-unclassified
      status: unresolved
      flagged: true
      statement: "The supplied edge-probe classified DSGN-05 as unclassified. Do not auto-resolve it: independent human judgment must confirm crispness of both original raster marks on both grounds at DPR2 within half-source-width limits. If evidence is insufficient, report human_needed."
    - id: UI-06-raster-ceiling
      status: unresolved
      flagged: true
      statement: "The approved design can fit the supplied raster assets within mark1000px/wordmark446px ceilings. Any placement requiring more needs an owner-supplied vector; no silent upscale."
---

<objective>
**As a** site owner, **I want to** inspect the complete site after measured quality gates **so that** I can select A, B or C from trustworthy full-site evidence.

Keep the prior full-site boundary. Complete technical/independent visual review before the sole font-selection checkpoint. Do not turn this into another design-spec exercise.
</objective>

<execution_context>
@/Users/zincdigital/.codex/gsd-core/workflows/execute-plan.md
@/Users/zincdigital/.codex/gsd-core/references/checkpoints.md
@/Users/zincdigital/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-font-pick/01-CONTEXT.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-VALIDATION.md
@.planning/phases/01-design-system-font-pick/01-03-SUMMARY.md
@docs/superpowers/specs/2026-09-25-zinc-site-redesign-design.md
@scripts/verify-mockup.mjs
@scripts/gate-preview.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one deployed template through the complete audit and evidence path</name>
  <files>scripts/verify-mockup.mjs, scripts/gate-preview.mjs, lighthouserc.json, lighthouserc.cls.json</files>
  <read_first>Existing verifier and gate scripts; package.json and installed @lhci/cli/lighthouse/axe-core APIs; 01-03 full matrix; UI-SPEC considerations; approved spec §10.</read_first>
  <behavior>A deployed service page receives one complete style/image/keyboard/axe/font/network/performance audit with recorded raw evidence. Deliberate missing screenshot, wrong domain, extra font or failed metric causes nonzero exit. Reports cannot replace a missing measurement with a pass.</behavior>
  <action>
    Extend gate-preview to --mode quick|full, --pairings, --base, --ready-for-owner and --self-test, with strict argument validation. Route one real service detail through target checks, browser assertions, screenshot coverage, axe and Lighthouse, generating an evidence JSON entry keyed by deployment ID, route, template, pairing, viewport, timestampCT and command. Use already installed tooling without upgrades. A dedicated Chrome profile, output folders, Lighthouse reports and any caches live in .scratch/phase01-mockup/.

    Configure mobile Lighthouse for successful pages with filesystem output only, Performance/Accessibility/Best Practices100 and SEO100 after the single documented is-crawlable skip. Keep a distinct shared canonical404 error-page contract: genuine404,1440/375 screenshots and the same applicable visual/accessibility/performance/link/noindex checks, while reporting raw Lighthouse SEO including its expected http-status-code failure without a100 gate. Do not add a status-code skip or convert404 to200. Never report that adjusted score as launch SEO100; require actual noindex/nofollow header and meta separately. Capture original category/audit results so the adjustment is reviewable. Enforce LCP<=1200ms, CLS exactly0, script gzip total<=15360bytes per page including inline and external scripts, and image responsiveness/dimensions. Sum actual unique script payloads once per page, gzip with node:zlib, count inline scripts too.

    lighthouserc.cls.json uses devtools throttling and cold profile/cache on each run; install a layout-shift PerformanceObserver before page script execution and retain raw shift entries during delayed font arrival, menu/form/filter/team activity. Do not use CSS metrics or cached second loads as CLS proof. Measure actual controlled click/key interaction durations with PerformanceEventTiming under the test profile and require <100ms for the exercised interactions. Label this lab interaction evidence; it is not field INP. If unsupported, report an unavailable measurement and a concrete supported-browser command, not a substitute pass using TBT.

    Extend --self-test with synthetic failing evidence/threshold fixtures; these test the gate logic, not production measurements. Quick mode samples the tracer and structural evidence in <60seconds; full mode performs the whole matrix and can run longer.
  </action>
  <acceptance_criteria>The complete one-template audit produces traceable raw evidence and fails on deliberately invalid inputs. Performance calculations, noindex exception and cold-load method are explicit.</acceptance_criteria>
  <verify><automated>node scripts/gate-preview.mjs --self-test && node scripts/gate-preview.mjs --mode quick --pairings a --base https://zinc-digital-web.vercel.app</automated><fails_when>The command exits nonzero if a deliberately invalid gate fixture passes, a required measurement is missing, a threshold fails, or the deployed tracer/target contract fails.</fails_when><failure_direction>Fix the measurement or underlying page responsible for a failure; do not lower a threshold, add an exception or label an unavailable reading green.</failure_direction></verify>
  <done>One deployed end-to-end quality check is connected and its failure path is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Complete all route, template, state and independent visual gates</name>
  <files>scripts/verify-mockup.mjs, scripts/gate-preview.mjs</files>
  <read_first>Task1 raw reports; current built route manifest and source snapshots; screenshots.json; 01-02/03 outstanding findings; full UI-SPEC consideration table plus superseding IDs.</read_first>
  <action>
    Expand the full runner to every canonical and prefixed route and all twelve successful-page templates across all three pairings. Audit the shared canonical404 separately, outside A/B/C comparison, at1440/375; unknown paths under every prefix must still return that genuine404. Require expected40 canonical +120 pairing pages +1 chooser; audit404 separately on random absent URLs. Every template/pairing must have1440/375 screenshots and local links checked. Include both cases, longest service names, all18 article route bodies, all contact steps/validation/error/thanks, known/unknown service query, all blog filters/no-results/reset, keyboard mobile-menu states, reduced-motion and no-JS fallbacks. Treat URLs as parsed path/query/fragment, not raw string prefixes.

    Run local full checks, then full checks against the exact deployed public alias. Browser zero-violations axe, per-template Lighthouse, cold-font CLS, actual network font counts, script budgets and controlled interactions must be recorded for every successful-page template/pairing. Record the shared canonical404 with the same applicable checks and its honest raw SEO score under the separate error-page contract. Capture comparison chooser separately. Check browser console/resource failures and 404 HTTP statuses. Record the project domain/protection/noindex assertions from 01-02 and fresh deployment identity; use no auth bypass secret on the public alias, and store no credentials or traces in tracked files.

    Have the independently assigned reviewer inspect every screenshot plus full enclosing changed rendering/style/interaction functions and the actual report outputs. The implementer does not accept their own work. Compare the raw REST/safe snapshot, route bodies and source inventory instead of trusting the generated checklist. Reject broken links, empty pages, unseen body truncation, different content between pairings, unclear draft markers, real submission behavior, alias/protection drift and visual mismatches. Fix accepted in-scope findings before owner handoff, rerunning affected checks plus the complete final matrix when rendering changes. Do not silently expand file ownership: report a needed source fix to the executor/orchestrator for bounded repair before continuing.

    Resolve UI-02/03/04 with 320–2560px measurements and explicit zero/one/two/six-item fixture cases, while actual published service records retain4–6 FAQs. Resolve UI-05 through dimensions/aspect ratios and UI-07 by OS-light/dark computed styles. For UI-06 and EP-DSGN05-unclassified, present side-by-side original-source and DPR2 rendered mark/wordmark crops on both grounds to the independent reviewer; retain flagged-human status until judgment exists, and request a vector only if the actual placement exceeds the existing raster ceiling.

    Produce an evidence index showing all passed checks, exact counts, artifact locations, reviewer identity/outcome, remaining draft content, unresolved assumptions and actual reviewed deployment. --ready-for-owner fails if required evidence is absent/stale relative to that build, if any high ASVS1 threat remains open, or if any full-site destination/state is missing. Keep automated test status separate from font taste and final copy approval.
  </action>
  <acceptance_criteria>Complete deployed and local matrices pass; every screenshot is independently reviewed; all high threats are mitigated; the evidence index contains exact counts and the remaining owner decision without fabricated pass marks.</acceptance_criteria>
  <verify><automated>node scripts/gate-preview.mjs --mode full --pairings a,b,c --base https://zinc-digital-web.vercel.app && node scripts/gate-preview.mjs --ready-for-owner</automated><fails_when>The full runner exits nonzero on any missing route/template/pairing/state/screenshot, broken link, failed safety/style/accessibility/performance threshold, stale evidence, open high threat or absent independent review.</fails_when><failure_direction>Any missing measurement, screenshot, route, source body or independent review prevents the owner checkpoint. Repair that item, then regenerate current evidence.</failure_direction></verify>
  <done>The complete site is technically and independently visually ready for an informed font choice.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Owner selects a font from the complete verified site</name>
  <files>.planning/phases/01-design-system-font-pick/01-04-SUMMARY.md</files>
  <read_first>Current gate evidence index, complete screenshot matrix, independent review outcome and public full-site comparison URLs.</read_first>
  <action>Present https://zinc-digital-web.vercel.app/design-preview/ and direct A/B/C site links, with evidence covering all templates and brief family/weight caveats. Ask the owner for A, B or C, or specific corrections. Record the exact answer, CT timestamp, reviewed deployment ID and any corrections in the plan summary. Never infer a selection from silence, auto_advance, an agent preference or a prior sample-page reaction. If corrections are requested, implement authorized corrections and repeat affected/full readiness checks before the same gate resumes.</action>
  <decision>Which complete-site font pairing should the site retain?</decision>
  <options>
    <option id="a"><name>A — Big Shoulders Display800 / Inter400 / JetBrains Mono400</name><pros>Industrial/editorial display with the established A rendering.</pros><cons>Judge actual full-page line breaks against B/C.</cons></option>
    <option id="b"><name>B — Barlow Condensed900 / Public Sans400 / IBM Plex Mono400</name><pros>Heaviest condensed display of the three.</pros><cons>Display and mono are static single-weight fonts.</cons></option>
    <option id="c"><name>C — Oswald700 / IBM Plex Sans400 / Space Mono400</name><pros>Distinct contemporary display proportions.</pros><cons>Oswald's real ceiling is700; mono is static single-weight.</cons></option>
  </options>
  <acceptance_criteria>The actual owner explicitly selects A/B/C after access to the entire reviewed site. Corrections stay open until resolved. This selects type only; it does not approve draft facts, legal text, final migration or launch.</acceptance_criteria>
  <verify><automated>node scripts/gate-preview.mjs --ready-for-owner</automated><fails_when>The readiness command exits nonzero when any required full-site evidence is missing, stale or failing; the checkpoint separately remains blocked until the actual owner records a choice.</fails_when><human-check>Owner's explicit recorded answer is mandatory.</human-check><failure_direction>If the owner has not answered or requests changes, keep01-05 blocked and preserve all pairings and pages.</failure_direction></verify>
  <done>Verbatim owner font decision is recorded against the reviewed deployment.</done>
  <resume-signal>Owner replies A, B, C, or specific corrections.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Automated output → owner evidence | A generated pass must correspond to current raw measurements |
| Owner answer → type collapse | Only an explicit owner selection permits removal of alternatives |
| Public alias → protected previews | Public noindex exposure and authenticated raw URLs have different contracts |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04-A | Spoofing | Readiness report | high | mitigate | Fail missing/stale measurements, keep raw reports/deployment IDs and independent review |
| T-01-04-B | Information Disclosure | Evidence artifacts | high | mitigate | External ignored scratch, no credentials/PII/secret traces in reports or Git |
| T-01-04-C | Tampering | Owner choice | high | mitigate | Blocking human checkpoint, verbatim choice tied to deployment, no auto-approval |
| T-01-04-D | Information Disclosure | Public review target | high | mitigate | Fresh alias/noindex/domain/protection checks before the gate |
</threat_model>

<verification>
Full quality coverage precedes owner review. A structural plan validator cannot establish runtime performance, font CLS, safety or visual approval. This plan records actual execution measurements only when they happen.
</verification>

## Artifacts Produced
Gate configs/runners; per-route/template/pairing raw audits; independently reviewed full-page images; ready-for-owner index under .scratch/phase01-mockup/; 01-04-SUMMARY.md with owner's exact choice.

<success_criteria>All full-site gates pass and the owner explicitly picks one pairing. The source-probe DSGN-05 assumption remains visible until the human visual judgment resolves it.</success_criteria>

<output>Create .planning/phases/01-design-system-font-pick/01-04-SUMMARY.md. Pass its recorded owner selection to01-05; preserve complete site and alternatives until then.</output>
