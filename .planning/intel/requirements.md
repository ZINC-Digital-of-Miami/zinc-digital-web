> Generated by gsd-doc-synthesizer during ingest (2026-09-25 CT). One entry per requirement extracted from PRD-classified sources.

## No PRD-type documents in this ingest set

- source: absent — CLASSIFICATIONS_DIR contained zero documents classified `PRD`
- description: absent
- acceptance: absent
- scope: absent

Note: Requirement-shaped content exists but lives in a `DOC`-classified source and is filed accordingly per the type-routing rule, not here:
- `docs/handoff/2026-09-25-turnover-complete.md` embeds a full verbatim copy of a requirements register (57 v1 requirements, IDs DSGN/MOTN/LOOP/HOME/SERV/WORK/ABOU/CONT/BLOG/COPY/SEO/MIGR/QUAL/LNCH, plus v2 and out-of-scope) inside "Part B (verbatim) — Requirements." Because the containing document is classified `DOC` (handoff/resumption, not a requirements-specification source), this content is filed in `context.md`, not here.
- The project already has a live `.planning/REQUIREMENTS.md` (existing context, merge mode) that is the authoritative, GSD-native requirements file. The embedded copy in the DOC above is a point-in-time snapshot of an earlier version of that same file (its DSGN-01 wording differs from the current `.planning/REQUIREMENTS.md` DSGN-01 — see `INGEST-CONFLICTS.md`).

This agent does not write to `.planning/REQUIREMENTS.md`; that remains `gsd-roadmapper`'s responsibility, informed by the conflict entries in `INGEST-CONFLICTS.md`.
