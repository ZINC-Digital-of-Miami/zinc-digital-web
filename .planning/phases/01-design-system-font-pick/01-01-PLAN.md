---
phase: 01-design-system-font-pick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - astro.config.mjs
  - .vercelignore
  - src/styles/tokens.css
  - src/styles/themes.css
  - src/styles/base.css
  - src/styles/pairings.css
  - src/layouts/PreviewLayout.astro
  - src/components/MockupBands.astro
  - src/components/bands/Hero.astro
  - src/pages/design-preview/a.astro
autonomous: false
requirements: [DSGN-02, DSGN-03, DSGN-04]
user_setup:
  - service: vercel
    why: "Preview hosting on the existing Pro team; the owner must be a team member to pass Vercel Authentication"
    dashboard_config:
      - task: "Confirm the owner's Vercel account can open a protected preview URL of project zinc-digital-web"
        location: "Vercel dashboard -> team zincdigitalofmiamis-projects -> Members"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The owner, logged into Vercel, opens <preview-url>/design-preview/a/ and sees the light hero band set in Pairing A (Big Shoulders Display 800 over Inter 400 and JetBrains Mono 400)"
    - "An unauthenticated request to any preview URL of project zinc-digital-web gets HTTP 302 to https://vercel.com/sso-api and an x-robots-tag: noindex header (measured behavior of this team's protection, 2026-09-25 13:24 CT)"
    - "Project zinc-digital-web has ssoProtection.deploymentType = all_except_custom_domains (Standard Protection, Vercel Authentication), no passwordProtection, no Git link and no production target"
    - "Every color on the page resolves from src/styles/tokens.css through the band's data-theme; the hero section carries data-theme=\"light\" and no rule keys ground colors to the OS color scheme"
    - "The hero core line reads exactly: Other agencies deliver the scope. ZINC delivers the business. with the strike on the scope and the highlight on the business drawn as decorative layers in the light-band accent, never in magenta"
    - "The build self-hosts exactly 3 WOFF2 files for Pairing A, each with a generated size-adjust fallback, and exactly one font preload (the display face)"
    - "AGENTS.md, CLAUDE.md, README.md and .gitignore are byte-identical to their pre-plan state (the template's governance files never land)"
  artifacts:
    - path: "astro.config.mjs"
      provides: "output static, @astrojs/vercel adapter, fonts array (Pairing A: 3 entries)"
      contains: "fontProviders.fontsource()"
    - path: "src/styles/tokens.css"
      provides: "Color, spacing and type-scale tokens (the one token set)"
      contains: "#FC0781"
    - path: "src/styles/themes.css"
      provides: "[data-theme=dark|light] semantic token mapping: --bg --fg --muted --accent --accent-text"
      contains: "[data-theme=\"light\"]"
    - path: "src/styles/base.css"
      provides: "Reset, type roles, band layout, focus-visible, decorative mark classes"
      contains: ".mark-strike"
    - path: "src/layouts/PreviewLayout.astro"
      provides: "Noindex preview document shell with per-pairing Font tags"
      contains: "noindex"
    - path: "src/components/bands/Hero.astro"
      provides: "Band 1 hero, light ground"
      contains: "data-theme=\"light\""
    - path: "src/pages/design-preview/a.astro"
      provides: "Route /design-preview/a/"
    - path: ".vercelignore"
      provides: "Keeps .planning, .claude, docs, .scratch and .env files out of the deploy upload"
      contains: ".env"
  key_links:
    - from: "src/pages/design-preview/a.astro"
      to: "src/layouts/PreviewLayout.astro"
      via: "pairing=\"a\" prop selects the Pairing A Font tags and data-pairing attribute"
      pattern: "pairing=\"a\""
    - from: "src/styles/pairings.css"
      to: "astro.config.mjs fonts cssVariable"
      via: "[data-pairing=\"a\"] maps --font-display/--font-body/--font-mono to --font-display-a/--font-body-a/--font-mono-a"
      pattern: "--font-display-a"
    - from: "src/components/bands/Hero.astro"
      to: "src/styles/themes.css"
      via: "data-theme=\"light\" resolves --accent to the display teal"
      pattern: "var\\(--accent\\)"
---

<objective>
Walking skeleton for Phase 1 (the Phase-1 tracer): scaffold the Astro project, build ONE real page (the hero band on `/design-preview/a/`) on the full token system with Pairing A self-hosted fonts, and serve it from a Vercel preview protected by Vercel Authentication that the owner can open.

Purpose: prove every layer this phase touches (package install -> Astro Fonts API -> tokens and data-theme -> static route -> Vercel project and Deployment Protection -> owner's browser) on day zero, so the pairing work in Wave 2 only widens a path that already works.
Output: a scaffolded repo, the token system, the hero band, one protected preview URL, and the owner's confirmation that he can open it.
</objective>

## Phase Goal

**As a** ZINC owner, **I want to** see ZINC's visual system on a real homepage mockup, **so that** I can pick the font pairing every later page uses.

(Restated from the ROADMAP Phase 1 Goal line using only its own words; the ROADMAP line itself is not in story form.)

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-font-pick/01-CONTEXT.md
@.planning/phases/01-design-system-font-pick/01-RESEARCH.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-SKELETON.md
@AGENTS.md

Measured facts (planner probe, 2026-09-25 13:19 CT, scratch build with astro@7.3.5 + @astrojs/vercel@11.0.11 on Node v24.21.0):
- With the Vercel adapter, `astro build` writes BOTH `dist/` (HTML at `dist/design-preview/a/index.html`) and `.vercel/output/static/`. Every `dist/` check in this phase is valid.
- All nine pairing families resolve: Big Shoulders Display ONLY via `fontProviders.fontsource()`; Inter, JetBrains Mono, Barlow Condensed, Public Sans, IBM Plex Mono, Oswald, IBM Plex Sans, Space Mono via `fontProviders.google()`. Build log line: `[assets] Copying fonts (9 files)...`. Files 9.4-23.8 KB each. Fallbacks generated against Arial, Arial Bold and Courier New, each with `size-adjust`.
- `sharp@0.35.4` arrives as an optionalDependency of astro@7.3.5 (no separate install needed for astro:assets images).
- The build emits zero `.js` files.
- Team `zincdigitalofmiamis-projects` (id team_OBen4n9i3PybGdYsjENnrv1S) has no project named zinc-digital-web; team defaultDeploymentProtection is null; sibling projects carry `ssoProtection.deploymentType = all_except_custom_domains`.
- Unauthenticated GET on a protected deployment of this team returns `HTTP/2 302`, `location: https://vercel.com/sso-api?...`, `x-robots-tag: noindex`.
- Vercel docs (last_updated 2026-09-15): Password Protection is $20/month/project on Pro (never use); Vercel Authentication needs no paid add-on.
- `vercel` CLI 53.1.1 is logged in as `zincdigitalofmiami`; Chrome 153.0.8010.49 is installed; `chromedriver` npm `latest` is 154 (mismatch, see Task 2 install step).

Decision references (CONTEXT.md has no D-numbers; sections are cited by name): CONTEXT "Stack (locked)", "Color (locked)", "Theming (locked)", "Type (locked)", "Copy (locked)", "Gates that start here"; owner constraints from the planning brief (Hero Display clamp, accent rule, no paid services, Node 24, CT times).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner gate: package legitimacy and the go for the Vercel project</name>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-RESEARCH.md (section "Package Legitimacy Audit")
    - AGENTS.md (Invariants: no paid services, irreversible actions)
  </read_first>
  <what-built>Nothing yet. This gate precedes every install and every outward Vercel action in Phase 1.</what-built>
  <how-to-verify>
    Part A: package legitimacy (RESEARCH flags astro@7.3.5 and @astrojs/vercel@11.0.11 as SUS "too-new", a same-week point release of the official withastro packages; typescript is not in the audit table, so it is treated as ASSUMED):
    1. Open https://www.npmjs.com/package/astro/v/7.3.5 and confirm the repository is github.com/withastro/astro.
    2. Open https://www.npmjs.com/package/@astrojs/vercel/v/11.0.11 and confirm the same repository.
    3. Open https://www.npmjs.com/package/typescript and confirm the repository is github.com/microsoft/TypeScript.
    The executor prints the registry facts first with `npm view astro@7.3.5 repository.url time.modified` and `npm view @astrojs/vercel@11.0.11 repository.url` and `npm view typescript repository.url version` so the owner compares them against the pages.
    Part B: the go for these Vercel actions, all in team zincdigitalofmiamis-projects (Pro, already paid, $0 added):
    - create project `zinc-digital-web` (CLI-linked; NOT connected to the GitHub repo in Phase 1);
    - keep Deployment Protection at Standard Protection with Vercel Authentication (never Password Protection, which costs $20/month);
    - run preview (non-production) deploys of this branch's build during Phase 1;
    - in Plan 01-04, create one Protection Bypass for Automation secret that lives only in the gitignored `.env.local` for Lighthouse runs.
    Never in Phase 1: a production deploy, a domain or alias, a DNS change, a Git connection.
  </how-to-verify>
  <verify>
    <automated>npm view astro@7.3.5 repository.url && npm view @astrojs/vercel@11.0.11 repository.url && npm view typescript repository.url</automated>
    <fails_when>non-zero exit, or any printed URL does not contain github.com/withastro/astro (first two) or github.com/microsoft/TypeScript (third)</fails_when>
  </verify>
  <acceptance_criteria>
    - The owner replies "approved" covering both Part A and Part B, and the executor records the reply time in CT in the SUMMARY.
    - No npm install and no vercel command other than `vercel whoami` has run before the approval.
  </acceptance_criteria>
  <resume-signal>Type "approved" to approve both parts, or name the package or Vercel action you reject</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Tracer: scaffold, token system, Pairing A hero on /design-preview/a/, protected Vercel preview</name>
  <precondition>node --version prints v24.x and `vercel whoami` prints zincdigitalofmiami (runtime facts the plan cannot guarantee)</precondition>
  <reversibility rating="costly">Every later band reads --bg, --fg, --muted, --accent, --accent-text and the data-theme attribute; renaming them later touches every component in Phases 2-6.</reversibility>
  <files>package.json, package-lock.json, tsconfig.json, astro.config.mjs, .vercelignore, src/styles/tokens.css, src/styles/themes.css, src/styles/base.css, src/styles/pairings.css, src/layouts/PreviewLayout.astro, src/components/MockupBands.astro, src/components/bands/Hero.astro, src/pages/design-preview/a.astro</files>
  <read_first>
    - .gitignore (already ignores node_modules/, dist/, .astro/, .vercel/, .env, .env.*, .scratch/; do not edit it)
    - AGENTS.md, CLAUDE.md, README.md (must stay untouched)
    - .planning/phases/01-design-system-font-pick/01-UI-SPEC.md (Spacing Scale, Typography, Color, Draft Mockup Copy, Resolved Spec Conflict, UI Considerations)
    - .planning/phases/01-design-system-font-pick/01-RESEARCH.md (Pitfalls 1, 2, 3, 4, 5; Pattern 1; Pattern 3)
    - .planning/research/ARCHITECTURE.md (Pattern 3: single-attribute theming)
  </read_first>
  <action>
All commands run from the worktree root `/Volumes/Satechi Hub/zinc-digital-web-worktrees/phase-01`; quote every path (it contains a space). Scratch output goes to `.scratch/` (gitignored), never to /tmp or the home directory.

A. Scaffold without touching governance files (RESEARCH Pitfalls 1 and 2). Run `npm create astro@latest .scratch/scaffold -- --yes --template minimal --no-install --no-git`, then confirm `.scratch/scaffold/package.json` and `.scratch/scaffold/tsconfig.json` exist (create-astro silently redirects into a random sibling folder when the target is non-empty, so check the path). Copy ONLY `package.json` and `tsconfig.json` into the worktree root. Do not copy the template's AGENTS.md, README.md, CLAUDE.md symlink, .gitignore, .vscode, public/ favicons (Astro's logo) or src/pages/index.astro. Leave `.scratch/scaffold` in place (gitignored).

B. Edit package.json: name `zinc-digital-web`, `private: true`, `type: module`, `engines.node` = `24.x`, scripts exactly `dev` = `astro dev`, `build` = `astro build`, `check` = `astro check`, `astro` = `astro` (drop the template's preview script; the Vercel adapter serves previews on Vercel). Install with exact pins per CONTEXT "Stack (locked)": `npm install --save-exact astro@7.3.5 @astrojs/vercel@11.0.11`, then `DETECT_CHROMEDRIVER_VERSION=true npm install --save-dev --save-exact @astrojs/check@0.9.10 typescript @lhci/cli@0.15.1 @axe-core/cli@4.13.0` (the env var makes the chromedriver dependency download the driver that matches the installed Chrome 153 instead of npm `latest` 154). Record the exact typescript version npm resolved in the SUMMARY. Do not run `astro add vercel` (it would install an unpinned adapter); write the config by hand.

C. astro.config.mjs: import `defineConfig` and `fontProviders` from `astro/config` and the default export of `@astrojs/vercel`. Set `output: 'static'`, `adapter: vercel()`, and a `fonts` array with the three Pairing A entries (UI-SPEC Font Pairings; weights are exact, one weight each, `styles: ['normal']`, `subsets: ['latin']`):
- name `Big Shoulders Display`, cssVariable `--font-display-a`, provider `fontProviders.fontsource()` (google() cannot resolve this family, RESEARCH Pitfall 3), weights [800], fallbacks ['sans-serif'];
- name `Inter`, cssVariable `--font-body-a`, provider `fontProviders.google()`, weights [400], fallbacks ['sans-serif'];
- name `JetBrains Mono`, cssVariable `--font-mono-a`, provider `fontProviders.google()`, weights [400], fallbacks ['monospace'].
Leave `optimizedFallbacks` at its default (true); it generates the metric-matched `size-adjust` fallback that DSGN-04 needs. This replaces the UI-SPEC's pyftsubset/Fontaine pipeline per RESEARCH (CONTEXT lists the subsetting tool and fallback method as Claude's discretion). Note for the assumption-delta record: the word "fallback" here means the generated metric-matched local font face; it adds no second architectural path.

D. .vercelignore (new): exclude `.planning/`, `.claude/`, `docs/`, `.scratch/`, `.env`, `.env.*`, `node_modules/`, `dist/`, `.astro/`. The deploy upload is source only; the secret file used in Plan 01-04 must never be uploaded.

E. src/styles/tokens.css, one `:root` block, the only file in `src/` that may contain accent or ground hex values (CONTEXT "Color (locked)"; UI-SPEC Color):
- colors: `--color-near-black: #0A0A0B`, `--color-snow: #F5F6F7`, `--color-gray-on-dark: #A6A7AB` (8.24:1), `--color-gray-on-light: #5C5D61` (6.08:1), `--color-magenta: #FC0781`, `--color-teal: #07B2B2` (display and graphics only), `--color-teal-text: #057E7E` (small text);
- spacing (UI-SPEC Spacing Scale): `--space-xs` 0.25rem, `--space-sm` 0.5rem, `--space-md` 1rem, `--space-lg` 1.5rem, `--space-xl` 2rem, `--space-2xl` 3rem, `--space-3xl` 4rem, `--space-4xl` 6rem, `--space-5xl` 8rem, `--touch-min` 44px;
- type (UI-SPEC Typography, exact formulas): `--text-display: clamp(3.5rem, 10vw + 1rem, 12.5rem)` (owner: big headers), `--text-heading: clamp(1.75rem, 2vw + 1.25rem, 2.75rem)`, `--text-body: clamp(1rem, 0.3vw + 0.94rem, 1.125rem)`, `--text-label: 0.875rem`; `--leading-display: 1.05`, `--leading-heading: 1.15`, `--leading-body: 1.5`, `--leading-label: 1.4`; `--tracking-display: -0.01em`, `--tracking-label: 0.04em`.
Do not declare `--font-display`, `--font-body` or `--font-mono` here (pairings.css owns them now; in Plan 01-05 the Font component defines them directly, and a second declaration would fight it in the cascade).

F. src/styles/themes.css, exactly two rule blocks (CONTEXT "Theming (locked)"):
- `[data-theme="dark"]`: `--bg: var(--color-near-black)`, `--fg: var(--color-snow)`, `--muted: var(--color-gray-on-dark)`, `--accent: var(--color-magenta)`, `--accent-text: var(--color-magenta)`, `background-color: var(--bg)`, `color: var(--fg)`, `color-scheme: dark`;
- `[data-theme="light"]`: `--bg: var(--color-snow)`, `--fg: var(--color-near-black)`, `--muted: var(--color-gray-on-light)`, `--accent: var(--color-teal)`, `--accent-text: var(--color-teal-text)`, `background-color: var(--bg)`, `color: var(--fg)`, `color-scheme: light`.
The magenta token appears only in the dark block and the two teal tokens only in the light block; that is how "magenta only on black, teal only on white" holds by construction. No media query of any kind in this file; the OS color scheme never sets a ground.

G. src/styles/base.css: box-sizing reset, zero body margin, `body` font-family `var(--font-body, sans-serif)`, font-size `var(--text-body)`, line-height `var(--leading-body)`, `font-synthesis: none` (only one weight per role ships, so never fake bold or italic); type-role classes `.t-display` (var(--font-display, sans-serif), var(--text-display), weight from `--weight-heavy`, leading and tracking tokens, `text-wrap: balance`), `.t-heading` (display family, --text-heading, --weight-heavy, --leading-heading), `.t-body`, `.t-label` (var(--font-mono, monospace), --text-label, --leading-label, --tracking-label; uppercase only via a separate `.t-label--caps` modifier because the phone line is not all-caps); `.muted` sets color var(--muted); `.band` sets block padding `var(--space-4xl)` and inline padding `var(--space-md)`, switching to `var(--space-5xl)` and `var(--space-2xl)` at min-width 1024px; `.band__inner` max-width 90rem and centered; `:focus-visible` outline 2px solid var(--fg) with 3px offset; `.button` (inline-flex, min-height and min-width var(--touch-min), padding var(--space-sm) var(--space-lg), background var(--fg), color var(--bg), no border, body family, text-decoration none; hover and focus-visible add an underline); `.draft-tag` (mono label style, color var(--muted)). Decorative mark classes, all drawn with pseudo-elements that have empty content so they carry no text and stay out of text-contrast scope (UI-SPEC "Resolved Spec Conflict"): `.mark-strike` (position relative; `::after` is a bar of height 0.08em across the words at about 52% of the line box, background var(--accent)), `.mark-hit` (position relative, `isolation: isolate`; `::before` is a highlighter block behind the lower 0.4em of the glyphs with `z-index: -1`, background var(--accent)), `.mark-bar` (position relative; `::after` is a 0.12em bar just below the baseline, background var(--accent), used on dark bands so no text sits on the accent). Set `--weight-heavy` per pairing in pairings.css, not here.

H. src/styles/pairings.css: one block `[data-pairing="a"]` mapping `--font-display: var(--font-display-a)`, `--font-body: var(--font-body-a)`, `--font-mono: var(--font-mono-a)`, `--weight-heavy: 800`. Plan 01-03 appends the b and c blocks.

I. src/layouts/PreviewLayout.astro: typed Props `pairing: 'a'` and `title: string`. Import in order tokens.css, themes.css, base.css, pairings.css. Render `html lang="en"` with `data-pairing={pairing}`; head holds charset, viewport `width=device-width, initial-scale=1`, `<title>`, a meta description ("Draft design preview for the ZINC website. Not public."), `<meta name="robots" content="noindex, nofollow">`, `<link rel="icon" href="/favicon.png" type="image/png">` (the PNG arrives in Plan 01-02), and for pairing a the three `Font` components from `astro:assets`: cssVariable `--font-display-a` with `preload`, then `--font-body-a` and `--font-mono-a` without preload (RESEARCH Pitfall 5: preload only the display face, which carries the LCP hero line). Body: a `header` with `data-theme="dark"` and class `preview-strip` holding a mono label "Pairing A: Big Shoulders Display 800 / Inter 400 / JetBrains Mono 400" followed by `[DRAFT PREVIEW]` and a link "All pairings" to `/design-preview/`; then `main id="main"` wrapping the default slot. Use a header element, not a section, so it never counts as a band.

J. src/components/bands/Hero.astro: Props with defaults `supporting` = "One team runs the machine, the traffic into it, and the numbers that prove it worked." (UI-SPEC draft copy), `ctaHref` = `/contact/` (the route arrives in Phase 4), `draft` = true. Markup: a `section` with `data-theme="light"`, class `band band--hero`, `aria-labelledby="hero-title"`; inside `.band__inner`: an `h1 id="hero-title" class="t-display"` holding two block-level spans, one per sentence, which fixes the break between the two sentences (UI-SPEC overflow row). The locked copy is verbatim (CONTEXT "Copy (locked)"): first span "Other agencies deliver " followed by a `span class="mark-strike"` wrapping "the scope" and then the period; second span "ZINC delivers " followed by a `span class="mark-hit"` wrapping "the business" and then the period. Join the two words inside each mark span with a non-breaking space (U+00A0, written as the HTML entity for a non-breaking space) so a mark never splits across lines and neither sentence can end on a single orphaned word (UI-SPEC overflow row, checked at 375 px and up in Plan 01-04). Then a `p class="t-body muted"` with the supporting line and, when draft is true, a `span class="draft-tag"` reading "[DRAFT]"; then an actions row holding `a class="button" href={ctaHref}` "Start an Inquiry" and `a class="t-label" href="tel:+17865754837"` containing an inline 16px phone-glyph SVG with `aria-hidden="true"` and `fill="currentColor"`, then the text "Text (786) 575-4837". Component-scoped styles only for layout (gaps from the spacing tokens); no hex values and no color tokens other than the semantic ones.

K. src/components/MockupBands.astro renders `Hero` only (Plan 01-02 adds the dark band and the spec sheet). src/pages/design-preview/a.astro renders `PreviewLayout` with `pairing="a"` and title "ZINC design preview: Pairing A [DRAFT]", wrapping `MockupBands`.

L. Local proof: `npm run build > .scratch/build.log 2>&1; echo EXIT=$?` then `npx astro check > .scratch/check.log 2>&1; echo EXIT=$?`. Both EXIT=0. Confirm the build log shows `Copying fonts (3 files)` and no line containing `No data found for font family`.

M. Vercel, inside the owner's Task 1 go, in this order, stopping on any mismatch:
1. `vercel project add zinc-digital-web --scope zincdigitalofmiamis-projects`.
2. Read `vercel project protection zinc-digital-web --scope zincdigitalofmiamis-projects --format json`. If `ssoProtection.deploymentType` is not `all_except_custom_domains`, set it by piping the JSON body `{"ssoProtection":{"deploymentType":"all_except_custom_domains"}}` to `vercel api /v9/projects/zinc-digital-web --scope zincdigitalofmiamis-projects -X PATCH --input -`, then read again. Never pass any password flag.
3. Pipe `{"framework":"astro","nodeVersion":"24.x"}` the same way (PATCH) so the remote build uses the Astro preset on Node 24.
4. `vercel link --yes --project zinc-digital-web --scope zincdigitalofmiamis-projects` (writes `.vercel/`, already gitignored).
5. Read `vercel api /v9/projects/zinc-digital-web --scope zincdigitalofmiamis-projects --raw`. If `link` is not null (the CLI auto-connected the GitHub repo), run `vercel git disconnect --yes` and read again. Phase 1 stays CLI-deployed so a merge to main can never create a production deployment.
6. `vercel deploy --yes > .scratch/preview-url.txt 2> .scratch/deploy.log` (preview target by default; never pass `--prod`). The file holds exactly one https URL.
7. Measure unauthenticated access: `curl -s -o /dev/null -D .scratch/unauth-headers.txt "$(cat .scratch/preview-url.txt)/design-preview/a/"`; expect the 302 status line, a location header starting https://vercel.com/sso-api, and x-robots-tag noindex.
Put the preview URL and the time of each Vercel step in the SUMMARY with times in CT (convert from UTC; label CT). Do not print or store any token.
  </action>
  <verify>
    <automated>npm run build && npx astro check && test -f dist/design-preview/a/index.html && test "$(ls dist/_astro/fonts/*.woff2 | wc -l | tr -d ' ')" = 3 && grep -q 'data-theme="light"' dist/design-preview/a/index.html && grep -q 'content="noindex, nofollow"' dist/design-preview/a/index.html && test "$(grep -o 'rel="preload"[^>]*as="font"' dist/design-preview/a/index.html | wc -l | tr -d ' ')" = 1 && grep -q 'size-adjust' dist/design-preview/a/index.html</automated>
    <fails_when>non-zero exit from build or astro check ("Result ... N errors" with N above 0), missing dist/design-preview/a/index.html, a WOFF2 count other than 3, a missing data-theme or noindex attribute, a font preload count other than 1, or no size-adjust fallback in the page</fails_when>
    <automated>git diff --quiet HEAD -- AGENTS.md CLAUDE.md README.md .gitignore && test ! -L CLAUDE.md && npm ls astro @astrojs/vercel --depth=0 | grep -q 'astro@7.3.5' && npm ls @astrojs/vercel --depth=0 | grep -q '@astrojs/vercel@11.0.11'</automated>
    <fails_when>non-zero exit: a governance file changed, CLAUDE.md became a symlink, or npm ls shows a version other than astro@7.3.5 / @astrojs/vercel@11.0.11</fails_when>
    <automated>vercel project protection zinc-digital-web --scope zincdigitalofmiamis-projects --format json | python3 -c 'import json,sys; d=json.load(sys.stdin); assert (d.get("ssoProtection") or {}).get("deploymentType")=="all_except_custom_domains", d; assert not d.get("passwordProtection"), d' && vercel api /v9/projects/zinc-digital-web --scope zincdigitalofmiamis-projects --raw | python3 -c 'import json,sys; d=json.load(sys.stdin); assert "production" not in (d.get("targets") or {}); assert d.get("link") is None; assert d.get("framework")=="astro"'</automated>
    <fails_when>AssertionError or non-zero exit: protection is not all_except_custom_domains, password protection is set, a production target exists, a Git link exists, or the framework is not astro</fails_when>
    <automated>curl -s -o /dev/null -D .scratch/unauth-headers.txt "$(cat .scratch/preview-url.txt)/design-preview/a/" && head -1 .scratch/unauth-headers.txt | grep -q ' 302' && grep -qi '^location: https://vercel.com/sso-api' .scratch/unauth-headers.txt && grep -qi '^x-robots-tag: noindex' .scratch/unauth-headers.txt</automated>
    <fails_when>status line is 200 (the draft is public) or anything other than 302, no sso-api location header, or no x-robots-tag noindex header</fails_when>
  </verify>
  <acceptance_criteria>
    - `dist/design-preview/a/index.html` exists; `dist/_astro/fonts/` holds exactly 3 .woff2 files; the page has exactly one font preload and at least one `size-adjust` declaration.
    - `src/styles/tokens.css` contains `#0A0A0B`, `#F5F6F7`, `#FC0781`, `#07B2B2` and `#057E7E` and the Display formula `clamp(3.5rem, 10vw + 1rem, 12.5rem)`.
    - `grep -rn -i 'FC0781\|07B2B2\|057E7E' src` lists only `src/styles/tokens.css` lines.
    - `grep -rn 'prefers-color-scheme' src` prints nothing.
    - In `src/styles/themes.css`, `--color-magenta` appears only inside the `[data-theme="dark"]` block and `--color-teal` / `--color-teal-text` only inside the `[data-theme="light"]` block.
    - `git diff --quiet HEAD -- AGENTS.md CLAUDE.md README.md .gitignore` exits 0.
    - `package.json` pins `astro` to `7.3.5` and `@astrojs/vercel` to `11.0.11` exactly (no caret) and declares `engines.node` `24.x`.
    - Project zinc-digital-web: ssoProtection `all_except_custom_domains`, no passwordProtection, `link` null, no `targets.production`, framework `astro`.
    - Unauthenticated GET on the preview URL returns 302 to `https://vercel.com/sso-api` with `x-robots-tag: noindex`.
  </acceptance_criteria>
  <done>The single happy path works end to end: a Pairing A hero built from the token system is served from a protected Vercel preview, and the URL is in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner opens the protected preview</name>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-RESEARCH.md (Open Question 2: owner team membership)
    - The deployment URL Task 2 wrote to the gitignored scratch file and to its task output
  </read_first>
  <what-built>The Phase 1 walking skeleton: the light hero band, set in Pairing A, on a Vercel preview protected by Vercel Authentication.</what-built>
  <how-to-verify>
    1. In a browser where you are logged into Vercel, open the preview URL the executor prints in this checkpoint message, with `/design-preview/a/` appended. You should see "Other agencies deliver the scope. ZINC delivers the business." very large, a teal strike through "the scope", a teal highlight behind "the business", the draft supporting line, "Start an Inquiry" and "Text (786) 575-4837".
    2. Open the same URL in a private window. You should land on a Vercel login page, not the site.
    3. If step 1 shows a Vercel "request access" page instead of the site, your account is not a member of team zincdigitalofmiamis-projects. Say so, so the invite goes out before Day 1 (Sat 2026-09-26 CT).
    The executor re-runs the unauthenticated curl check from Task 2 before presenting this checkpoint.
  </how-to-verify>
  <verify>
    <automated>curl -s -o /dev/null -D .scratch/unauth-headers.txt "$(cat .scratch/preview-url.txt)/design-preview/a/" && head -1 .scratch/unauth-headers.txt | grep -q ' 302'</automated>
    <fails_when>status line is not 302 (200 means the draft is publicly reachable)</fails_when>
  </verify>
  <acceptance_criteria>
    - The owner confirms he sees the hero logged in and the Vercel login wall in a private window; the executor records the reply and its CT time in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe what you see instead</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

Whole-phase inventory (this plan's items are marked 01-01):

| Artifact | Plan | Kind | Symbol / role |
|---|---|---|---|
| package.json, package-lock.json, tsconfig.json | 01-01 | config | exact pins astro 7.3.5, @astrojs/vercel 11.0.11; engines node 24.x; scripts dev/build/check/astro |
| astro.config.mjs | 01-01, 01-03, 01-05 | config | `fonts` array (3 entries, then 9, then the 3 chosen) |
| .vercelignore | 01-01 | config | deploy-upload exclusions |
| src/styles/tokens.css | 01-01 | CSS | `--color-*`, `--space-*`, `--text-*`, `--leading-*`, `--tracking-*`, `--touch-min` |
| src/styles/themes.css | 01-01 | CSS | `[data-theme]` blocks: `--bg --fg --muted --accent --accent-text` |
| src/styles/base.css | 01-01 | CSS | `.t-display .t-heading .t-body .t-label .t-label--caps .muted .band .band__inner .button .draft-tag .mark-strike .mark-hit .mark-bar` |
| src/styles/pairings.css | 01-01, 01-03 (deleted 01-05) | CSS | `[data-pairing="a" / "b" / "c" / "none"]` font and `--weight-heavy` mapping |
| src/layouts/PreviewLayout.astro | 01-01, 01-03 (deleted 01-05) | layout | Props `pairing`, `title` |
| src/components/MockupBands.astro | 01-01, 01-02 | component | composes Hero, BrandBand, SpecSheet |
| src/components/bands/Hero.astro | 01-01, 01-02 | component | Props `supporting`, `ctaHref`, `draft` |
| src/pages/design-preview/a.astro | 01-01 (deleted 01-05) | route | `/design-preview/a/` |
| scripts/check-bands.mjs | 01-02 | Node script | band, theme and accent contract gate, `--self-test` |
| src/assets/brand/zinc-mark-black.png, zinc-mark-white.png, zinc-wordmark-black.png, zinc-wordmark-white.png | 01-02 | image | brand rasters, byte-identical copies |
| public/favicon.png | 01-02 | image | 64x64 favicon from the black mark |
| src/components/BrandMark.astro | 01-02 | component | Props `kind`, `ground`, `width`, `alt`, `eager` |
| src/components/bands/BrandBand.astro | 01-02 | component | dark band, magenta mark |
| src/components/bands/SpecSheet.astro | 01-02 | component | Props `name`, `summary`, `deliverables[]`, `cadence`, `clientOwns`, `reported`, `ctaHref`, `draft` |
| src/pages/design-preview/stress.astro | 01-02 (deleted 01-05) | route | backstop overflow fixture |
| scripts/check-fonts.mjs | 01-03 | Node script | font-file budget gate, `--self-test` |
| src/pages/design-preview/index.astro, b.astro, c.astro | 01-03 (deleted 01-05) | routes | pairing index, B and C |
| scripts/check-overflow.mjs | 01-04 | Node script | zero-dependency Chrome DevTools Protocol overflow and orphan check, `--self-test` |
| lighthouserc.json, lighthouserc.cls.json | 01-04 | config | Lighthouse CI score and CLS assertions |
| scripts/gate-preview.sh | 01-04 | shell | one-command phase gate |
| src/layouts/BaseLayout.astro | 01-05 | layout | production shell, Props `title`, `description`, `noindex` |
| src/pages/index.astro | 01-05 | route | `/` in the chosen pairing |
| .planning/phases/01-design-system-font-pick/01-SKELETON.md | planner, updated 01-05 | doc | architectural contract |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to worktree | third-party code enters the build |
| worktree to Vercel upload | source files leave the machine on `vercel deploy` |
| internet to preview URL | anyone holding the URL reaches Vercel's edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | preview deployment (draft copy, unreleased direction) | high | mitigate | Standard Protection with Vercel Authentication (`all_except_custom_domains`) verified before the first deploy; unauthenticated curl must return 302 to vercel.com/sso-api; no alias, custom domain or Git connection in Phase 1 |
| T-01-02 | Information Disclosure | search indexing of preview routes | medium | mitigate | Vercel's `x-robots-tag: noindex` (measured on the 302) plus `meta robots noindex, nofollow` in PreviewLayout, asserted by grep on dist |
| T-01-03 | Information Disclosure | `vercel deploy` source upload | medium | mitigate | `.vercelignore` excludes .planning/, .claude/, docs/, .scratch/, .env and .env.* |
| T-01-04 | Tampering | template merge overwriting AGENTS.md / CLAUDE.md / README.md / .gitignore | medium | mitigate | scaffold to .scratch/scaffold and copy only package.json and tsconfig.json; `git diff --quiet` gate plus a CLAUDE.md symlink check |
| T-01-05 | Denial of Service (billing) | Vercel Password Protection ($20/month/project) | medium | mitigate | never enabled; protection JSON asserts passwordProtection is empty |
| T-01-06 | Elevation of Privilege | production deployment escaping preview-only scope | medium | mitigate | project stays CLI-linked (`link` null), no `--prod`; `targets.production` asserted absent |
| T-01-SC | Tampering | npm installs (astro, @astrojs/vercel flagged SUS too-new; typescript ASSUMED) | high | mitigate | blocking-human legitimacy checkpoint (Task 1) before any install; exact version pins; package-lock.json committed |
</threat_model>

<verification>
- `npm run build` and `npx astro check` exit 0.
- The dist checks in Task 2 pass: 3 WOFF2 files, 1 preload, size-adjust present, data-theme light, noindex.
- Governance files are unchanged.
- Vercel project state matches: Standard Protection, no password, no link, no production target, Astro framework.
- Unauthenticated preview request returns 302 to sso-api with x-robots-tag noindex.
- The owner confirms access (Task 3).
</verification>

<success_criteria>
The owner has opened `/design-preview/a/` on a protected Vercel preview and seen the light hero in Pairing A, and every automated check in Task 2 exits 0.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-font-pick/01-01-SUMMARY.md` when done. Include: the preview URL, the CT time of each Vercel action, the exact typescript version, the owner's replies with CT times, and the measured header lines from `.scratch/unauth-headers.txt`.
</output>
