---
phase: 01-design-system-font-pick
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - astro.config.mjs
  - src/layouts/PreviewLayout.astro
  - src/styles/pairings.css
  - src/pages/design-preview/[...path].astro
  - src/pages/design-preview/a.astro
  - src/components/MockupPage.astro
  - src/data/mockup.ts
  - scripts/verify-mockup.mjs
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A, B and C render the SAME entire 40-page site, including all article/service destinations and contact demo states; only fonts and pairing identification vary."
    - "Each page requests at most three same-origin WOFF2 files, preloads exactly its display font, and includes real generated metric fallbacks with measured cold-load CLS 0 (UI-01)."
    - "EP-DSGN01-adjacency: A/B/C chooser controls stay distinct, keyboard-operable and at least 44px even where labels wrap; no overlap at 320–2560px."
    - "EP-DSGN01-empty: missing or invalid pairing segments produce a designed 404, never an empty comparison page or silent substitution."
    - "EP-DSGN01-ordering: the chooser and manifest consistently order A then B then C; ties in post dates retain the source-ID order from 01-02."
    - "EP-DSGN04-concurrency: interrupting or running a font build cannot publish partial output; failed generation leaves prior reviewed deployment intact and the next clean build restores complete per-pairing assets."
  artifacts:
    - path: src/pages/design-preview/[...path].astro
      provides: "Comparison index plus full nested A/B/C route families from one manifest"
    - path: src/styles/pairings.css
      provides: "Three explicit CSS family mappings, correct heavy weights"
    - path: astro.config.mjs
      provides: "Nine font registrations with verified static/variable caveats"
    - path: scripts/verify-mockup.mjs
      provides: "Cross-pairing parity, actual font-request and cold-load checks"
  key_links:
    - from: comparison getStaticPaths
      to: canonical full-site manifest
      via: "Pairing prefix × complete routes, same template/records"
    - from: PreviewLayout Font components
      to: astro.config.mjs fonts
      via: "Matching cssVariable for exactly three active font roles"
    - from: all local links and contact query
      to: pairing-aware path helper
      via: "Preserve selected pairing, fragment and service query across navigation"
---

<objective>
**As a** site owner, **I want to** compare three fonts while browsing the entire same site **so that** the choice reflects real page layouts and content.

01-02 already delivered the complete A mockup. Extend it without reducing any route, content or state. There is no owner font checkpoint in this plan.
</objective>

<execution_context>
@/Users/zincdigital/.codex/gsd-core/workflows/execute-plan.md
@/Users/zincdigital/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-font-pick/01-CONTEXT.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-RESEARCH.md
@.planning/phases/01-design-system-font-pick/01-02-SUMMARY.md
@src/data/mockup.ts
@src/components/MockupPage.astro
@src/layouts/PreviewLayout.astro
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry Pairing B through one complete navigation path with genuine font output</name>
  <files>astro.config.mjs, src/layouts/PreviewLayout.astro, src/styles/pairings.css, src/pages/design-preview/[...path].astro, src/pages/design-preview/a.astro</files>
  <read_first>Current complete site and 01-02 verification results; UI-SPEC Font Pairings and Research Corrections; RESEARCH native Fonts API/provider resolution; existing a.astro route; astro.config.mjs.</read_first>
  <behavior>A built /design-preview/b/ renders the complete homepage and reaches /design-preview/b/services/shopify/ with B fonts, then B contact/thanks. Existing canonical A pages retain their content. Exactly three active same-origin font files are requested per page; a failed build cannot be deployed.</behavior>
  <action>
    Extend the layout pairing union to a|b|c and conditionally render only the active three Font components, with display preload alone. Append Barlow Condensed 900 / Public Sans 400 / IBM Plex Mono 400 and Oswald 700 / IBM Plex Sans 400 / Space Mono 400 to Astro's native fonts config. Keep Big Shoulders Display on fontsource and try the existing verified google-provider pattern for the other families; if a family fails to resolve, use the built-in fontsource provider for that exact family and record the warning/fix. Retain optimized metric fallbacks, Latin subset and generated unicode-range coverage; do not hand-enter metrics, fabricate a 900 Oswald weight, add a font-preparation package or preload inactive pairings. Barlow Condensed, IBM Plex Mono and Space Mono are static single-weight fonts, explicitly described as such on the chooser; the native Fonts API may emit single-weight subsets for the other fonts as already established in 01-01.

    Add the comparison catch-all router and render its index at /design-preview/ plus the B tracer path using existing MockupPage and fixture records. Reserve a documented basePath prop for all local hrefs. Update the existing exact a.astro route to call the full-site renderer and the same current record; it must not continue rendering an isolated sample or conflict with the catch-all output. The catch-all excludes the exact A root while including its descendants. Unknown segments must fall through to genuine 404. This tracer starts the route expansion, not a second skeleton.
  </action>
  <acceptance_criteria>The B tracer uses genuine B fonts with matched fallbacks through navigation. Nine configured families resolve and only three load per page. Canonical A is still the complete site, with unchanged content and the original display clamp.</acceptance_criteria>
  <verify><automated>npm run build && npm run check && node scripts/verify-mockup.mjs --mode quick --pairings a,b --tracer</automated><fails_when>The command exits nonzero on font resolution or typecheck failure, missing B tracer route/link, wrong active font role/weight, extra font requests or unsafe demo behavior.</fails_when><failure_direction>If a family fails resolution, fix that exact provider/weight and rebuild; do not substitute a different family or accept a system-font screenshot as a comparison.</failure_direction></verify>
  <done>The full render architecture carries a second font family through a real end-to-end path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand all three comparisons across every route and verify parity</name>
  <files>src/pages/design-preview/[...path].astro, src/components/MockupPage.astro, src/data/mockup.ts, src/layouts/PreviewLayout.astro, scripts/verify-mockup.mjs</files>
  <read_first>Task 1 router and font output; complete sitemap and template checks from 01-02; UI consideration IDs UI-01..UI-07; 01-02 screenshot manifest.</read_first>
  <behavior>Each pairing prefix contains every canonical destination. A→B→C switching keeps the same current page and service query. Article source IDs/text, cases, contacts and legal states match across pairings. All selectors, article links and service preselection remain functional.</behavior>
  <action>
    Generate every A/B/C prefixed route from the exact canonical manifest: 120 rendered content destinations plus the comparison index, alongside the 40 canonical A pages. Keep the existing A root route as described in Task 1 and count it once. Share the same component and data records for each corresponding route. Expose a build-time pairing-aware path helper for local hrefs, fragments and query strings; apply it consistently to navigation, logo/home link, service cards, FAQs' related links, case next links, article related links, footer, form confirmation and return actions. External public client/tel/mail links retain their schemes. The chooser at /design-preview/ lists A/B/C in that order with exact family/weight/static caveats; compact per-page switching preserves current canonical path and the allowlisted service query. Do not vary copy, spacing, images or page composition to favor a pairing.

    Extend the verifier before using --pairings b,c for full acceptance. Generic --pairings a,b,c selection and per-prefix --tracer behavior already exist from01-02 Task3 and are used by Task1. This task expands full-route/parity assertions; it does not create a flag that Task1 needed earlier. Unsupported options fail rather than silently reduce coverage. Compare normalized rendered DOM content/template section identities across A/B/C, excluding only pairing labels and font styles. Confirm each font role's computed family/weight, CSS fallback overrides, one preload and at most three requested WOFF2 URLs; a count of generated files alone is insufficient. Include delayed/blocked-font cold loads with browser layout-shift observation, long service names and every template. Keep data-pairing selection independent of OS color preference.

    Exercise EP-DSGN04-concurrency by performing a controlled failing font-build fixture in an isolated external scratch copy/output directory; confirm nonzero build exit and no deploy invocation, then restore valid config and require a successful clean build. Never interrupt another task's build or mutate the live deployment to test this. Store exact command/result evidence. Full capture repeats every successful-page template at1440 and375 for each pairing plus all route/link checks; the shared canonical404 is captured once at each width outside A/B/C, keeping its genuine error status and raw SEO score; inherited draft labels, no-submission behavior, ratio/contrast and reduced-motion gates apply equally. Inspect the images before independent acceptance. Deploy the all-pairing artifact only after those checks using the existing authorized --prod target procedure from 01-02, then recheck the public noindex alias and protected raw preview URLs.
  </action>
  <acceptance_criteria>All 161 rendered content/index URLs (40 canonical +120 pairing pages +1 chooser), their local links and genuine404 pass. All three pairings have full template screenshots and identical semantic content. Font-only variation is measured; rejected font/build inputs fail closed.</acceptance_criteria>
  <verify><automated>npm run build && npm run check && node scripts/verify-mockup.mjs --mode quick --pairings a,b,c --parity && node scripts/gate-preview.mjs --check-target --base https://zinc-digital-web.vercel.app</automated><fails_when>The command exits nonzero on build/type errors, missing canonical/paired destination, semantic parity mismatch, excess fonts, wrong preloads, or target/protection/noindex mismatch.</fails_when><human-check>Inspect the full screenshot matrix and have an independent reviewer confirm content parity and completeness before the font gate.</human-check><failure_direction>Any missing paired route, state, asset or parity mismatch returns to the shared renderer/path helper. Do not request a font pick until all pairings contain the entire site.</failure_direction></verify>
  <done>A/B/C are complete comparable sites, ready for the formal measured gates and owner choice in 01-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Build font provider → deployed static files | Only generated same-origin assets reach the visitor |
| Pairing path/query → shared renderer | Only allowlisted pairings/services select records |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03-A | Tampering | Pairing/query path helper | high | mitigate | Fixed pairing/route/service manifests; reject unknowns; parity and link crawl |
| T-01-03-B | Information Disclosure | Font loading | medium | mitigate | Same-origin WOFF2 URLs, browser network assertions, no provider CSS in visitor page |
| T-01-03-C | Spoofing | Comparison | high | mitigate | Shared records/templates and DOM parity; name static-weight caveats accurately |
| T-01-03-D | Tampering | Interrupted build/deploy | high | mitigate | Deploy only after complete successful build/check and full local artifact verification |
</threat_model>

<verification>
Run node scripts/verify-mockup.mjs --mode full --pairings a,b,c --parity locally and against the public alias before handoff. Every template, route and interaction from 01-02 remains covered. Font-swap CLS is observed under cold/delayed-font loading, not inferred from generated fallback CSS.
</verification>

## Artifacts Produced
Shared full-site A/B/C comparison routes; nine correctly configured families; per-page font/parity results; 1440/375 screenshots for every template/pairing in .scratch/phase01-mockup/; deployment evidence and 01-03-SUMMARY.md.

<success_criteria>The owner can compare type across the entire same site with no missing destination or state, and no family substitution or hidden content change.</success_criteria>

<output>Create .planning/phases/01-design-system-font-pick/01-03-SUMMARY.md with actual font identities, route counts, check results and remaining risks.</output>
