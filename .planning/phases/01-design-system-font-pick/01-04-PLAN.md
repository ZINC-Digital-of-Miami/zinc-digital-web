---
phase: 01-design-system-font-pick
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - scripts/check-overflow.mjs
  - lighthouserc.json
  - lighthouserc.cls.json
  - scripts/gate-preview.sh
  - astro.config.mjs
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-04, DSGN-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On the deployed preview, /design-preview/a/, /b/ and /c/ each score Lighthouse mobile 100 for Performance, Accessibility, Best Practices and SEO (median of 3 runs). The is-crawlable audit is skipped because noindex is a required control on previews"
    - "Each pairing measures CLS 0 under real devtools network throttling from a cold profile. The worst of 3 runs counts"
    - "Each pairing measures LCP of 1200 ms or less and 15 KB or less of script, and dist ships 15 KB or less of gzipped JS in total"
    - "axe finds zero WCAG 2.0, 2.1 and 2.2 A/AA violations on the index, a, b, c and stress routes. This covers magenta at 5.17:1 on black and teal text only where it passes"
    - "No horizontal scroll at 320, 375, 768, 1440, 1920 or 2560 px on a, b, c and the stress route. The hero line never ends on a single orphaned word at 375 px and up (UI-SPEC overflow rows, including the 12.5rem Display cap)"
    - "The protection bypass secret exists only in gitignored .env.local. git grep for its value finds nothing. Lighthouse reports stay in gitignored .scratch/ and are never uploaded to temporary-public-storage"
    - "The owner has viewed the three pairings and the brand marks on the protected preview and stated one pick (A, B or C), recorded with its CT time"
  artifacts:
    - path: "scripts/check-overflow.mjs"
      provides: "Zero-dependency Chrome DevTools Protocol overflow and orphan check with --self-test"
      contains: "--self-test"
    - path: "lighthouserc.json"
      provides: "Score, LCP, CLS and script-size assertions, filesystem upload"
      contains: "\"target\": \"filesystem\""
    - path: "lighthouserc.cls.json"
      provides: "Devtools-throttled cold-load CLS assertion"
      contains: "devtools"
    - path: "scripts/gate-preview.sh"
      provides: "One-command phase gate: build, astro check, bands, fonts, JS budget, headers, overflow, axe, Lighthouse, CLS"
      contains: "EXIT="
  key_links:
    - from: "scripts/gate-preview.sh"
      to: ".env.local"
      via: "reads VERCEL_AUTOMATION_BYPASS_SECRET into a run-only rc file under .scratch/, which is deleted on exit"
      pattern: "VERCEL_AUTOMATION_BYPASS_SECRET"
    - from: "scripts/gate-preview.sh"
      to: "lighthouserc.json and lighthouserc.cls.json"
      via: "merges the bypass headers into a temporary copy of each rc"
      pattern: "lighthouserc"
---

<objective>
Prove the three pairings meet the Phase 1 gates on the real deployed artifact, then put them in front of the owner for the Day 1 pick (Sat 2026-09-26 CT).

Purpose: CONTEXT "Gates that start here" (Lighthouse mobile 100 on all four categories, CLS 0 on a cold throttled load per pairing, WCAG 2.2 AA contrast, about 0 KB JS) and ROADMAP Phase 1 success criteria 1, 3 and 4. The owner's pick is DSGN-01's gate.
Output: scripts/check-overflow.mjs, two Lighthouse CI configs, scripts/gate-preview.sh (the phase's one gate command), a fresh protected preview, measured results, and the owner's recorded pick.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-font-pick/01-VALIDATION.md
@.planning/phases/01-design-system-font-pick/01-UI-SPEC.md
@.planning/phases/01-design-system-font-pick/01-01-SUMMARY.md
@.planning/phases/01-design-system-font-pick/01-02-SUMMARY.md
@.planning/phases/01-design-system-font-pick/01-03-SUMMARY.md
@astro.config.mjs

Measured facts:
- An unauthenticated preview request gets `HTTP/2 302` to `https://vercel.com/sso-api...` with `x-robots-tag: noindex`. It never gets a 401. VALIDATION.md's earlier "401" wording is superseded (measured 2026-09-25 13:24 CT).
- Lighthouse's SEO audit `is-crawlable` fails by design on any noindex page. The preview must stay noindex (threat T-01-02), so that one audit is skipped. The noindex header is asserted separately by the gate's header step. A skipped audit is not a waived gate: the opposite condition is measured.
- Lighthouse CI's default upload target, temporary-public-storage, publishes reports (with screenshots of draft pages) to a public URL. Both rc files set `upload.target` to `filesystem` under `.scratch/`.
- Lighthouse reports record `configSettings.extraHeaders`, so the bypass secret appears inside report files. Reports stay in gitignored `.scratch/`, and nothing from them goes into a SUMMARY beyond scores and metric values.
- Vercel injects its toolbar script into preview deployments. Every Lighthouse request sends `x-vercel-skip-toolbar: 1`, so the measured page is the site, not the toolbar. If the script-size assertion still exceeds 15 KB because of injected toolbar code, the executor sets the project's `enablePreviewFeedback` to false (PATCH through `vercel api`; $0; inside the owner's Plan 01-01 go) and re-measures.
- @axe-core/cli 4.13.0 pulls chromedriver `latest` (154), while Chrome is 153.0.8010.49. Plan 01-01 installed with `DETECT_CHROMEDRIVER_VERSION=true`. If axe still reports a ChromeDriver version mismatch, run `DETECT_CHROMEDRIVER_VERSION=true npm rebuild chromedriver`.
- Node 24 provides global `WebSocket` and `fetch`, so the overflow check needs no dependency.
- `astro preview` is not used (the Vercel adapter owns serving). Local checks serve `dist/` with `python3 -m http.server 4321 --bind 127.0.0.1 --directory dist` (Python 3.12.8 present).

Owner constraints (planning brief): the executor never runs the full suite from parallel agents. This plan is alone in Wave 4, and the orchestrator re-runs `scripts/gate-preview.sh` once for the wave. Hero Display stays `clamp(3.5rem, 10vw + 1rem, 12.5rem)` whatever the measurements show. It is an owner requirement, so a failing gate is fixed by other means and never by shrinking the headers.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Local layout and accessibility gates (overflow, orphan, axe)</name>
  <files>scripts/check-overflow.mjs</files>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-UI-SPEC.md (UI Considerations: overflow rows and the re-check widths)
    - src/components/bands/Hero.astro (the h1 structure the orphan rule reads: two block spans, non-breaking spaces inside the marks)
  </read_first>
  <behavior>
    - Self-test fixture with a 500 px-wide block at a 320 px viewport. Expected: reports horizontal overflow.
    - Self-test fixture with an h1 whose last line holds one word at 375 px. Expected: reports an orphaned last word.
    - Self-test clean fixture. Expected: no violation at any width.
    - Words joined by U+00A0 count as one unit, so "the scope." never reads as an orphan when the two words stay together.
  </behavior>
  <action>
A. scripts/check-overflow.mjs is an ESM Node 24 script. It imports only node: built-ins (`node:child_process`, `node:fs`, `node:path`, `node:timers/promises`) and uses the global `WebSocket` and `fetch`.

Usage: `node scripts/check-overflow.mjs --base <origin> [--widths 320,375,768,1440,1920,2560] <path>...` or `--self-test`.

Launch Chrome:
- Path from the `CHROME_PATH` env var, defaulting to "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome".
- Arguments: `--headless=new`, `--remote-debugging-port=0`, `--user-data-dir=.scratch/chrome-overflow`, `--no-first-run`, `--no-default-browser-check`.
- Poll the DevToolsActivePort file in that directory for up to 10 s to learn the port.
- Open a page target with an HTTP PUT to `/json/new?about:blank`.
- Drive it over its webSocketDebuggerUrl with a minimal id-to-promise CDP client.

For each path and width:
- `Emulation.setDeviceMetricsOverride` with that width, height 900, deviceScaleFactor 1, and mobile true below 768.
- `Page.navigate`, then wait for `Page.loadEventFired`.
- `Runtime.evaluate` with awaitPromise, awaiting `document.fonts.ready`, then return:
  - `scrollWidth` versus `innerWidth` (overflow when greater);
  - for the page's h1 at widths of 375 and up, the orphan result. For each block-level child span of the h1, split its text into words on ASCII whitespace only (never on U+00A0, which JavaScript's `\s` class would match), measure each word's rect with a Range, group rects by rounded top, and flag when the final group holds a single word.

Print "<path> @ <width>: OK" or the violation. Close Chrome, then finish with "check-overflow: OK" (exit 0) or "check-overflow: <n> violation(s)" (exit 1). `--self-test` runs the three fixtures as data: URLs and prints "self-test: <passed>/<total>".

B. Run the local gates against the current build: `npm run build`, then start the Python static server on 127.0.0.1:4321 in the background, then:
- `node scripts/check-overflow.mjs --base http://127.0.0.1:4321 /design-preview/a/ /design-preview/b/ /design-preview/c/ /design-preview/stress/`
- `npx axe http://127.0.0.1:4321/design-preview/ http://127.0.0.1:4321/design-preview/a/ http://127.0.0.1:4321/design-preview/b/ http://127.0.0.1:4321/design-preview/c/ http://127.0.0.1:4321/design-preview/stress/ --tags wcag2a,wcag2aa,wcag21a,wcag21aa,wcag22aa --exit`
Then stop the server.

C. If a violation appears, fix it in the component or base.css that causes it. Allowed fixes: wrapping, max-width, gaps, a non-breaking space. Never the Display size formula, never an accent on white text, never magenta on a light band. Re-run until both commands exit 0, and list every fix and its file in the SUMMARY (deviation rules apply to files outside this task's list).
  </action>
  <verify>
    <automated>node scripts/check-overflow.mjs --self-test</automated>
    <fails_when>non-zero exit, or "self-test: x/y" with x below y</fails_when>
    <automated>npm run build && (python3 -m http.server 4321 --bind 127.0.0.1 --directory dist > .scratch/http.log 2>&1 & echo $! > .scratch/http.pid) && sleep 1 && node scripts/check-overflow.mjs --base http://127.0.0.1:4321 /design-preview/a/ /design-preview/b/ /design-preview/c/ /design-preview/stress/ && npx axe http://127.0.0.1:4321/design-preview/ http://127.0.0.1:4321/design-preview/a/ http://127.0.0.1:4321/design-preview/b/ http://127.0.0.1:4321/design-preview/c/ http://127.0.0.1:4321/design-preview/stress/ --tags wcag2a,wcag2aa,wcag21a,wcag21aa,wcag22aa --exit; rc=$?; kill "$(cat .scratch/http.pid)"; exit $rc</automated>
    <fails_when>non-zero exit: check-overflow reports overflow or an orphan, axe reports one or more violations, or axe prints "This version of ChromeDriver only supports Chrome version" (fix with the rebuild command in context, then re-run)</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-overflow.mjs --self-test` prints `self-test: 3/3` or more and exits 0.
    - `grep -E "^import" scripts/check-overflow.mjs | grep -v "node:"` prints nothing.
    - check-overflow reports OK for 4 paths times 6 widths (24 lines) and exits 0.
    - axe reports 0 violations on all 5 URLs and exits 0.
    - `grep -n "clamp(3.5rem, 10vw + 1rem, 12.5rem)" src/styles/tokens.css` still matches.
  </acceptance_criteria>
  <done>Layout overflow, the hero orphan rule and WCAG A/AA are measured green on every preview route, locally and deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Deployed-preview gates: Lighthouse scores, throttled CLS, headers, one gate command</name>
  <files>lighthouserc.json, lighthouserc.cls.json, scripts/gate-preview.sh, astro.config.mjs</files>
  <precondition>The owner's Plan 01-01 Task 1 approval covering "one Protection Bypass for Automation secret stored only in .env.local" is recorded in 01-01-SUMMARY.md</precondition>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-01-SUMMARY.md (preview URL, the owner's approval scope)
    - .gitignore (confirms .env.* and .scratch/ are ignored)
    - .vercelignore (confirms .env.* never uploads)
    - astro.config.mjs (only touched by the CLS remedy below)
  </read_first>
  <action>
A. lighthouserc.json, pretty-printed JSON with a two-space indent (so `"target": "filesystem"` appears verbatim for the grep gate), with a top-level `ci` object:
- `collect`: `numberOfRuns` 3; `settings` with `skipAudits` ["is-crawlable"] and nothing else (mobile form factor and simulated throttling are Lighthouse defaults, which is what the 100 scores mean). No `url` key; URLs arrive from the gate script.
- `assert.assertions`:
  - `categories:performance`, `categories:accessibility`, `categories:best-practices` and `categories:seo`, each ["error", {minScore 1, aggregationMethod "median-run"}];
  - `cumulative-layout-shift` ["error", {maxNumericValue 0, aggregationMethod "pessimistic"}];
  - `largest-contentful-paint` ["error", {maxNumericValue 1200, aggregationMethod "median-run"}];
  - `resource-summary:script:size` ["error", {maxNumericValue 15360}].
- `upload`: `target` "filesystem", `outputDir` ".scratch/lhci".

B. lighthouserc.cls.json has the same shape, except:
- collect settings `throttlingMethod` "devtools" (real network throttling, so a late font swap shows up; simulated throttling hides it), `onlyCategories` ["performance"], `skipAudits` ["is-crawlable"];
- assertions only `cumulative-layout-shift` ["error", {maxNumericValue 0, aggregationMethod "pessimistic"}];
- upload filesystem to ".scratch/lhci-cls".
Each lhci run launches a fresh Chrome profile, which is the cold load.

C. scripts/gate-preview.sh: bash with `set -uo pipefail`, never `set -x`. Usage: `scripts/gate-preview.sh <base-url> <path>...`. It exits 2 with a usage line when arguments are missing.

Secret handling:
- If `VERCEL_AUTOMATION_BYPASS_SECRET` is unset, read that single key from `.env.local`. If it is still empty, exit 2 with "missing VERCEL_AUTOMATION_BYPASS_SECRET".
- Build a headers JSON object with `x-vercel-protection-bypass` (the secret), `x-vercel-set-bypass-cookie` "true" and `x-vercel-skip-toolbar` "1".
- Using `node -e` with JSON parse and stringify, write run-only copies of both rc files to `.scratch/lhci-run.json` and `.scratch/lhci-cls-run.json` with `ci.collect.settings.extraHeaders` set to that object. Register a `trap` that deletes both copies on exit.

Steps. Each runs as its own command with its exit code captured right after it (`cmd; rc=$?`, never through a pipe to grep) and printed as `STEP <name> EXIT=<rc>`:
1. `build`: `npm run build`.
2. `astro-check`: `npx astro check`.
3. `bands`: `node scripts/check-bands.mjs`.
4. `fonts`: `node scripts/check-fonts.mjs --max 3` over the dist file for each path (a path "/x/y/" maps to dist/x/y/index.html; "/" maps to dist/index.html), plus `--distinct` when more than one path is given.
5. `js-budget`: the sum of gzip -c byte counts over every .js file under dist must be 15360 or less. Zero files is a pass.
6. `headers`: for each path, an unauthenticated curl must return a 302 status line with a location starting https://vercel.com/sso-api, and a curl with the bypass header must return 200 with an x-robots-tag noindex header.
7. `overflow`: start the Python static server on 127.0.0.1:4321, run check-overflow over the paths, and stop the server (trap-protected).
8. `axe`: axe over the paths on the same local server, with the Task 1 tags and --exit.
9. `lighthouse`: `npx lhci autorun --config=.scratch/lhci-run.json` with one `--collect.url=<base><path>` per path.
10. `cls`: the same with `.scratch/lhci-cls-run.json`.

End with a table of step names and EXIT codes. Exit 0 only when every step is 0.

D. Create the bypass secret, inside the owner's Plan 01-01 go:
- Confirm the action verb with `vercel project protection --help`, then create one automation bypass secret for zinc-digital-web (the `--protection-bypass` option, `--scope zincdigitalofmiamis-projects`).
- Read the secret from `vercel api /v9/projects/zinc-digital-web --scope zincdigitalofmiamis-projects --raw`: it is the `protectionBypass` key whose `scope` is `automation-bypass`. Extract it with python3 and write `VERCEL_AUTOMATION_BYPASS_SECRET=<value>` to `.env.local`, then `chmod 600 .env.local`.
- Never print the value, never put it in a SUMMARY, never pass it to git.
- Confirm `git check-ignore -q .env.local` succeeds.

E. Deploy the current branch build: `vercel deploy --yes > .scratch/preview-url.txt 2> .scratch/deploy.log` (preview only; never `--prod`). Then run `bash scripts/gate-preview.sh "$(cat .scratch/preview-url.txt)" /design-preview/a/ /design-preview/b/ /design-preview/c/ > .scratch/gate.log 2>&1; echo EXIT=$?`.

F. Remedies, in this order, each followed by redeploy and re-run. Record every before and after value in the SUMMARY:
1. `cls` fails for a pairing: set that pairing's display-role entry in astro.config.mjs to `display: 'optional'` (a documented Astro Fonts API option). The metric-matched fallback stays; only the late swap is removed. Never change the Display clamp.
2. `resource-summary:script:size` fails from injected toolbar code: PATCH `enablePreviewFeedback` false through `vercel api`.
3. A category below 100: read the failing audit ids in the .scratch/lhci report JSON and fix the cause in the owning file (deviation rules).

G. Record in the SUMMARY: the preview URL; the CT time of each deploy; per route the four category scores, LCP, CLS (simulated and devtools) and script bytes; the final STEP table. No header values and no secret.
  </action>
  <verify>
    <automated>git check-ignore -q .env.local && ! git ls-files --error-unmatch .env.local >/dev/null 2>&1 && S="$(sed -n 's/^VERCEL_AUTOMATION_BYPASS_SECRET=//p' .env.local)" && test -n "$S" && ! git grep -q -F -e "$S" && grep -q '"target": "filesystem"' lighthouserc.json && grep -q '"target": "filesystem"' lighthouserc.cls.json</automated>
    <fails_when>non-zero exit: .env.local is not ignored or is tracked, the secret line is missing or empty, the secret value is found in any tracked file, or either rc file lacks the filesystem upload target</fails_when>
    <automated>bash scripts/gate-preview.sh "$(cat .scratch/preview-url.txt)" /design-preview/a/ /design-preview/b/ /design-preview/c/</automated>
    <fails_when>non-zero exit, or any "STEP ... EXIT=" line with a value other than 0 (lhci prints "Assertion failed" or a category score below 1; the headers step fails on a 200 without the secret or a missing x-robots-tag)</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/gate-preview.sh` over a, b and c prints ten `STEP ... EXIT=0` lines and exits 0.
    - Each route: Performance, Accessibility, Best Practices and SEO equal 1.00 (median of 3); LCP is 1200 ms or less; CLS is 0 under simulated and devtools throttling (worst of 3); script size is 15360 bytes or less.
    - `grep -c 'temporary-public-storage' lighthouserc.json lighthouserc.cls.json` prints 0 for each file.
    - `ls .scratch/lhci/*.json` exists; nothing under .scratch/ is tracked (`git ls-files .scratch` prints nothing).
    - The Display clamp is unchanged in tokens.css.
  </acceptance_criteria>
  <done>One command proves every Phase 1 gate on the deployed, protected artifact, and the numbers are in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Day 1 owner review: pick the pairing (Sat 2026-09-26 CT)</name>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-UI-SPEC.md (Font Pairings: what differs between A, B and C)
    - The Task 2 gate table and preview URL (from this plan's task output)
  </read_first>
  <what-built>Three complete mockups on identical markup (light hero, dark brand band, light spec sheet), one per font pairing, on a protected Vercel preview. All Phase 1 gates are measured green (Task 2 table).</what-built>
  <how-to-verify>
    1. Logged into Vercel, open the preview URL printed in this checkpoint with `/design-preview/` appended.
    2. Open Pairing A, B and C in turn on your phone and on a desktop screen. The hero line, the brand band and the spec sheet are the same on all three; only the type changes. C's headline weight stops at 700.
    3. Check the ZINC wordmark (hero, black on white), the circuit-brain mark and the white wordmark (dark band), and the black mark (spec sheet). Each should look crisp at phone width, tablet width and full desktop width.
    4. Check the color rule: teal marks on the white hero, the magenta bar only on the black band.
    5. Reply with one letter.
  </how-to-verify>
  <verify>
    <automated>curl -s -o /dev/null -D .scratch/unauth-headers.txt "$(cat .scratch/preview-url.txt)/design-preview/" && head -1 .scratch/unauth-headers.txt | grep -q ' 302'</automated>
    <fails_when>status line is not 302 (200 means the review index is public)</fails_when>
  </verify>
  <acceptance_criteria>
    - The owner's reply names exactly one of A, B or C. The executor records the letter, the three family names and the reply time in CT in 01-04-SUMMARY.md under a heading "Owner pick".
    - If the owner asks for a change instead of a pick, the executor records the request verbatim and stops. The orchestrator routes it; Plan 01-05 does not run.
  </acceptance_criteria>
  <resume-signal>Reply "A", "B" or "C", or describe what should change first</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This plan's new files and symbols: `scripts/check-overflow.mjs` (CLI; `--base`, `--widths`, `--self-test`); `lighthouserc.json`; `lighthouserc.cls.json`; `scripts/gate-preview.sh` (CLI; `<base-url> <path>...`; STEP/EXIT table), the phase gate command the orchestrator runs once per wave. Gitignored, never committed: `.env.local` (bypass secret) and `.scratch/lhci*`, `.scratch/gate.log`, `.scratch/preview-url.txt`. The full phase inventory is in 01-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate runner to protected preview | an automation secret crosses Vercel Authentication |
| Lighthouse CI to report storage | reports embed screenshots of draft pages and the request headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | Protection Bypass for Automation secret committed or uploaded | high | mitigate | Stored only in `.env.local` (gitignored by `.env.*`, excluded by `.vercelignore`, mode 600); run-only rc copies live in `.scratch/` and are trap-deleted; the verify step greps tracked files for the value and fails on a hit; never printed or written to a SUMMARY |
| T-01-13 | Information Disclosure | lhci temporary-public-storage publishing draft screenshots and headers | high | mitigate | `upload.target` is `filesystem` in both rc files, asserted by grep; reports go to `.scratch/` only |
| T-01-14 | Information Disclosure | review index reachable without login | high | mitigate | The Task 3 verify re-measures the 302 to sso-api on `/design-preview/`; the gate's headers step checks every path |
| T-01-15 | Tampering | Lighthouse measuring the Vercel toolbar instead of the site | low | mitigate | `x-vercel-skip-toolbar: 1` on every request; `enablePreviewFeedback` false as the measured remedy |
| T-01-16 | Denial of Service (billing) | extra Vercel usage from repeated preview deploys and Lighthouse runs | low | accept | Static preview traffic from a handful of runs is far inside Pro's included usage; no paid feature is enabled |
</threat_model>

<verification>
- `node scripts/check-overflow.mjs --self-test` exits 0.
- The local overflow and axe run exits 0.
- `bash scripts/gate-preview.sh "$(cat .scratch/preview-url.txt)" /design-preview/a/ /design-preview/b/ /design-preview/c/` exits 0 with ten EXIT=0 steps.
- The secret is absent from tracked files; the rc files upload to the filesystem only.
- The owner's pick is recorded with its CT time.
</verification>

<success_criteria>
All Phase 1 gates are measured green on the deployed, protected preview for all three pairings, and the owner has picked one pairing on Day 1.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-font-pick/01-04-SUMMARY.md` when done, with the per-route metrics table, the STEP/EXIT table, any remedies applied, and the "Owner pick" section.
</output>
