> Generated by gsd-doc-synthesizer during ingest (2026-09-25 CT). One entry per ADR-classified source.

## No ADR-type documents in this ingest set

- source: absent — CLASSIFICATIONS_DIR contained zero documents classified `ADR`
- status: absent
- decision: absent
- scope: absent

Note: The classified set (`/Volumes/Satechi Hub/zinc-digital-web-worktrees/phase-01/.planning/intel/classifications/`) contains one SPEC and two DOC entries only. No ADR-type source exists, so no locked decision records were extracted here.

Decision-shaped content does exist but lives in non-ADR sources and is filed accordingly per the type-routing rule:
- The design spec's "Resolved decisions" section (2026-09-25, owner: "resolve all conflicts") is extracted into `constraints.md` because its containing document is classified `SPEC`, not `ADR`.
- The decision register in the "Complete Turnover" handoff is extracted into `context.md` because its containing document is classified `DOC`.

Five owner corrections were supplied directly to this ingestion run (not sourced from a classified file) and are recorded verbatim, with full LOCKED-override treatment, in `context.md` under "Owner corrections postdating every ingested doc (2026-09-25 CT)". See `INGEST-CONFLICTS.md` for every place they contradict ingested or existing content.
