---
phase: 01-design-system-font-pick
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - astro.config.mjs
  - src/styles/tokens.css
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/design-preview/index.astro
  - src/pages/design-preview/a.astro
  - src/pages/design-preview/b.astro
  - src/pages/design-preview/c.astro
  - src/pages/design-preview/stress.astro
  - src/layouts/PreviewLayout.astro
  - src/styles/pairings.css
  - .planning/STATE.md
  - .planning/phases/01-design-system-font-pick/01-SKELETON.md
autonomous: true
requirements: [DSGN-01, DSGN-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "STATE.md Decisions and 01-SKELETON.md both record the owner's pick (letter, the three families, the reply time in CT)"
    - "/ renders the hero, the dark brand band and the spec sheet in the chosen pairing only, and dist holds exactly 3 WOFF2 files"
    - "No /design-preview/ route, PreviewLayout, pairings.css or losing-pairing font entry remains. Nothing named --font-display-a/-b/-c exists in src or astro.config.mjs"
    - "Every component reads type through --font-display, --font-body and --font-mono, which the Font component now defines directly. A future pairing change is a three-entry config edit"
    - "On a fresh protected preview, / scores Lighthouse mobile 100 on all four categories and the hero measures CLS 0 under devtools throttling from a cold profile (ROADMAP Phase 1 success criterion 4)"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Production document shell: chosen-pairing Font tags, noindex prop, favicon"
      contains: "preload"
    - path: "src/pages/index.astro"
      provides: "Route / rendering MockupBands on BaseLayout (noindex while copy is draft)"
      contains: "MockupBands"
    - path: "astro.config.mjs"
      provides: "Exactly the three chosen font entries, cssVariable --font-display / --font-body / --font-mono"
      contains: "--font-display"
  key_links:
    - from: "astro.config.mjs fonts"
      to: "src/styles/base.css"
      via: "the cssVariable names equal the var() names base.css already reads"
      pattern: "var\\(--font-display"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "noindex prop true until Phase 5 owns SEO"
      pattern: "noindex"
---

<objective>
Lock the owner's Day 1 pick into the codebase and measure the chosen pairing as the thing every later page inherits: record the decision, collapse the font config to the three chosen files, replace the throwaway preview scaffolding with a production layout and a `/` route, and prove the phase's performance criteria on that final artifact.

Purpose: ROADMAP Phase 1 success criterion 1 says the pick must be recorded. Criterion 4 says the chosen pairing must arrive in 3 or fewer files with CLS 0 on the hero. UI-SPEC says the losing pairings and the preview routes do not ship.
Output: BaseLayout.astro, index.astro, a three-entry fonts config, deleted preview files, updated STATE.md and 01-SKELETON.md, and a measured preview of `/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-design-system-font-pick/01-SKELETON.md
@.planning/phases/01-design-system-font-pick/01-04-SUMMARY.md
@astro.config.mjs
@src/layouts/PreviewLayout.astro
@src/styles/pairings.css
@src/styles/tokens.css
@src/styles/base.css

Pairing table (UI-SPEC, as built in 01-03):

| Pick | Display (cssVariable becomes --font-display) | Body (--font-body) | Mono (--font-mono) | --weight-heavy |
|---|---|---|---|---|
| A | Big Shoulders Display 800, fontsource() | Inter 400, google() | JetBrains Mono 400, google() | 800 |
| B | Barlow Condensed 900, google() | Public Sans 400, google() | IBM Plex Mono 400, google() | 900 |
| C | Oswald 700, google() | IBM Plex Sans 400, google() | Space Mono 400, google() | 700 |

Carry over any provider swap or `display: 'optional'` remedy that 01-03 or 01-04 applied to the chosen entries; their SUMMARYs record them.

Decision references: CONTEXT "Type (locked)" (the owner picks one; 3 or fewer files); UI-SPEC "Font/Mockup Preview Routing" (the losing pairings and route group are deleted after the pick); owner constraint (all times in CT).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the pick and collapse to the chosen pairing on /</name>
  <precondition>01-04-SUMMARY.md has an "Owner pick" section naming exactly one of A, B or C with a CT time</precondition>
  <reversibility rating="reversible">The pairing lives behind --font-display/--font-body/--font-mono; switching later is a three-entry config edit plus one --weight-heavy value, and the deleted preview files stay in git history.</reversibility>
  <files>astro.config.mjs, src/styles/tokens.css, src/layouts/BaseLayout.astro, src/pages/index.astro, src/pages/design-preview/index.astro, src/pages/design-preview/a.astro, src/pages/design-preview/b.astro, src/pages/design-preview/c.astro, src/pages/design-preview/stress.astro, src/layouts/PreviewLayout.astro, src/styles/pairings.css, .planning/STATE.md, .planning/phases/01-design-system-font-pick/01-SKELETON.md</files>
  <read_first>
    - .planning/phases/01-design-system-font-pick/01-04-SUMMARY.md ("Owner pick" section)
    - astro.config.mjs, src/layouts/PreviewLayout.astro, src/styles/pairings.css, src/styles/tokens.css (all being modified or removed)
    - .planning/STATE.md (Decisions section format)
    - .planning/phases/01-design-system-font-pick/01-SKELETON.md (the pending "Type pairing" row)
  </read_first>
  <action>
Let P be the picked letter from the "Owner pick" section.

A. astro.config.mjs: delete the six entries of the two losing pairings. In the three kept entries, rename cssVariable to exactly `--font-display`, `--font-body` and `--font-mono`. Keep each kept entry's provider, weight, subsets, fallbacks and any display option as built.

B. src/styles/tokens.css: add `--weight-heavy` with P's value from the context table to the `:root` block (it moves here from pairings.css). Change nothing else in the file.

C. src/layouts/BaseLayout.astro, new:
- Typed Props `title: string`, `description: string`, `noindex?: boolean` (default false).
- Import tokens.css, themes.css and base.css in that order.
- `html lang="en"`; head holds charset, viewport, title, meta description, `<meta name="robots" content="noindex, nofollow">` only when noindex is true, and the favicon link to /favicon.png.
- Font tags: `--font-display` with `preload`, then `--font-body` and `--font-mono` without preload.
- Body: `main id="main"` wrapping the slot.
- No preview strip and no data-pairing attribute.

D. src/pages/index.astro, new: BaseLayout with title "ZINC Digital [DRAFT]", description "ZINC runs the build, the demand and the intelligence behind established brands. Draft preview.", and noindex set to true (the copy is draft; Phase 5 owns SEO and removes the flag). It renders MockupBands.

E. Delete with `git rm`: the five files under src/pages/design-preview/, src/layouts/PreviewLayout.astro and src/styles/pairings.css.

F. Record the decision with the owner's reply time converted to America/Chicago and labeled CT:
- .planning/STATE.md: under "Recent decisions affecting current work", add a bullet "[Phase 1]: Owner picked Pairing P (<display> / <body> / <mono>) on <date time> CT; losing pairings and /design-preview/ removed". Update "Last activity" with the CT date.
- 01-SKELETON.md: set the "Type pairing" row's Choice to the three families and weights, and its Rationale to "Owner Day 1 pick, <date time> CT".
Use Edit, not Write, on both files.

G. `npm run build` and `npx astro check`, both clean. Then `node scripts/check-bands.mjs` and `node scripts/check-fonts.mjs --max 3 dist/index.html`.
  </action>
  <verify>
    <automated>npm run build && npx astro check && node scripts/check-bands.mjs && node scripts/check-fonts.mjs --max 3 dist/index.html && test "$(ls dist/_astro/fonts/*.woff2 | wc -l | tr -d ' ')" = 3 && test ! -e dist/design-preview && test "$(grep -o 'cssVariable:' astro.config.mjs | wc -l | tr -d ' ')" = 3</automated>
    <fails_when>non-zero exit; check-bands or check-fonts violations; a WOFF2 count other than 3; a dist/design-preview directory still present; or a font-entry count other than 3</fails_when>
    <automated>! grep -rnE -e '--font-(display|body|mono)-[abc]' src astro.config.mjs && ! test -e src/layouts/PreviewLayout.astro && ! test -e src/styles/pairings.css && grep -q 'Owner picked Pairing' .planning/STATE.md</automated>
    <fails_when>grep finds a per-pairing variable, a deleted file still exists, or STATE.md lacks the pick bullet (non-zero exit)</fails_when>
  </verify>
  <acceptance_criteria>
    - astro.config.mjs holds exactly 3 font entries, and their cssVariable values are `--font-display`, `--font-body` and `--font-mono`.
    - `dist/index.html` exists, holds 3 sections alternating light, dark, light, and passes check-fonts with --max 3 (3 files, 1 preload, size-adjust fallbacks).
    - `src/pages/design-preview/`, `src/layouts/PreviewLayout.astro` and `src/styles/pairings.css` are gone from the working tree and staged as deletions.
    - STATE.md and 01-SKELETON.md name the chosen families and carry a CT timestamp.
  </acceptance_criteria>
  <done>The codebase has one pairing, one production layout and a `/` route, and the owner's pick is on record.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the chosen pairing on a fresh protected preview</name>
  <precondition>.env.local holds VERCEL_AUTOMATION_BYPASS_SECRET (created in Plan 01-04) and `vercel project protection zinc-digital-web --scope zincdigitalofmiamis-projects --format json` still shows all_except_custom_domains</precondition>
  <files>astro.config.mjs</files>
  <read_first>
    - scripts/gate-preview.sh (the phase gate from Plan 01-04)
    - .planning/phases/01-design-system-font-pick/01-04-SUMMARY.md (per-pairing baseline metrics for comparison)
  </read_first>
  <action>
A. Preview deploy (inside the owner's Plan 01-01 go; never `--prod`): `vercel deploy --yes > .scratch/preview-url.txt 2> .scratch/deploy.log`.

B. Run the phase gate on `/`: `bash scripts/gate-preview.sh "$(cat .scratch/preview-url.txt)" / > .scratch/gate-final.log 2>&1; echo EXIT=$?`. Expect ten STEP lines with EXIT=0. This is the measurement for ROADMAP Phase 1 success criterion 4: the chosen pairing in 3 or fewer self-hosted subset files and CLS 0 on the hero under devtools throttling from a cold profile.

C. If the cls step fails on `/`, apply the same remedy as Plan 01-04 (set `display: 'optional'` on the display entry in astro.config.mjs), rebuild, redeploy and re-run. Never change the Display clamp. Record before and after values.

D. Record in 01-05-SUMMARY.md: the preview URL; the deploy time in CT; the four category scores, LCP, CLS (simulated and devtools) and script bytes for `/`; the STEP table; and the comparison with the same pairing's numbers in 01-04-SUMMARY.md. Also confirm project zinc-digital-web still has no production target and no Git link, using the same project-read assertion as Plan 01-01.
  </action>
  <verify>
    <automated>bash scripts/gate-preview.sh "$(cat .scratch/preview-url.txt)" /</automated>
    <fails_when>non-zero exit or any "STEP ... EXIT=" value other than 0</fails_when>
    <automated>vercel api /v9/projects/zinc-digital-web --scope zincdigitalofmiamis-projects --raw | python3 -c 'import json,sys; d=json.load(sys.stdin); assert "production" not in (d.get("targets") or {}); assert d.get("link") is None; assert (d.get("ssoProtection") or {}).get("deploymentType")=="all_except_custom_domains"'</automated>
    <fails_when>AssertionError or non-zero exit: a production target or Git link appeared, or protection changed</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate over `/` exits 0 with ten `STEP ... EXIT=0` lines.
    - `/` shows Performance, Accessibility, Best Practices and SEO all equal to 1.00; LCP of 1200 ms or less; CLS equal to 0 under both throttling methods; script size of 15360 bytes or less.
    - The Vercel project still has Standard Protection, no production target and no Git link.
  </acceptance_criteria>
  <done>The pairing every later page uses is measured green on the final Phase 1 artifact.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan's new files and symbols: `src/layouts/BaseLayout.astro` (Props `title`, `description`, `noindex`); `src/pages/index.astro` (route `/`); the `--weight-heavy` token in tokens.css; font CSS custom properties `--font-display`, `--font-body` and `--font-mono` now defined by the Fonts API. Removed: `/design-preview/` routes (index, a, b, c, stress), `PreviewLayout.astro`, `pairings.css`, and six losing font entries. Updated: `.planning/STATE.md` (pick decision) and `01-SKELETON.md` (Type pairing row). The full phase inventory is in 01-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet to preview URL | `/` now carries draft copy at the site root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Information Disclosure | draft copy at `/` on the preview | high | mitigate | Same Vercel Authentication (Standard Protection) re-asserted in the Task 2 precondition and verify; the gate's headers step proves the 302 to sso-api and noindex on `/`; the BaseLayout noindex prop is true |
| T-01-18 | Elevation of Privilege | a production deployment appearing before cutover | medium | mitigate | Project read asserts no `targets.production` and no Git link after the final deploy; `--prod` is never used |
</threat_model>

<verification>
- Build, astro check, check-bands and check-fonts (`dist/index.html`, --max 3) all exit 0; dist has 3 WOFF2 files and no design-preview directory.
- The gate over `/` on the fresh preview exits 0.
- STATE.md and 01-SKELETON.md record the pick in CT.
</verification>

<success_criteria>
The owner's chosen pairing is the only type system in the codebase, `/` renders the mockup in it, and the chosen pairing is measured at 3 or fewer files, CLS 0 and Lighthouse 100 on all four categories on the protected preview.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-font-pick/01-05-SUMMARY.md` when done, with the metrics for `/`, the STEP table and the comparison with 01-04.
</output>
